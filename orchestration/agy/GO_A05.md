# GO_A05 — W1.7: backfill Dukascopy idempotente que no degrada
## Identidad
- ID: A05 · Ola: A · Rama/worktree: JOSFER78/agy-A05 (Orca; la rama real lleva el prefijo del usuario) · Timebox: 45 min
- Variable de entorno obligatoria: AGY_AGENT=A05
- Python: `PY="C:/Users/yo/Pictures/Descargaspc/pro/UltrarentablePC/ultrarentable/.venv/Scripts/python.exe"`; TODO comando desde la raíz del worktree con `PYTHONPATH=.` (hay un editable-install que apunta al checkout principal; sin PYTHONPATH importarías código ajeno). Windows nativo, sin .venv propio. Sin red: PROHIBIDO descargar de Dukascopy.
## OBJETIVO (una frase verificable)
`run_dukascopy_backfill.py` salta chunks con manifiesto válido (sha256 del contenido), escribe en staging y solo sustituye si `hours_failed` no empeora y `bar_count` no baja (lo peor va a cuarentena con MANIFEST), con `--force` y `--dry-run`; probado con `tests/test_dukascopy_backfill_idempotente.py` sobre un par real del repo.
## TERRITORIO (únicas rutas donde puede ESCRIBIR; fuera, solo lectura)
- services/data_ingestion/run_dukascopy_backfill.py (existe, 142 líneas)
- services/data_ingestion/dukascopy_feed.py (existe; SOLO `ingest()` líneas 309-503: nuevo kwarg `output_dir` y extracción del escritor. NO tocar `download_hour`, `decode_ticks`, `aggregate_bars`, el bloque CSV 458-491 ni `main()`)
- tests/test_dukascopy_backfill_idempotente.py (nuevo)
- orchestration/results/agy/A05.md (nuevo; crear el directorio) · orchestration/agy/DONE_A05.md (nuevo; crear el directorio)
## ENTRADAS (leer antes de tocar nada; con ruta exacta)
- orchestration/state/contratos/W17_backfill_idempotente.md — entero (§3 reglas 1-5, §4 aceptación a-e). Cítalas en el informe.
- services/data_ingestion/run_dukascopy_backfill.py (entero) y services/data_ingestion/dukascopy_feed.py líneas 158-200 (`download_hour`: retries=6, backoff 2^n con techo 30 s) y 309-503 (`ingest`).
- Fixture REAL tracked en git (par verificado: 5 barras 1h de USA500IDXUSD del 2023-03-01, bar_count 5, hours_failed 0, checksum válido): cuarentena/datasets_superseded/normalized/ds_dukascopy_usa500idxusd_1h_1677628800000_1677643200000.json (1009 bytes, sha256 cfdbfea259441b3200bc60e7253bb7fbf7cb1724b51ffcdd8fe8907f6aba7dc9) y su `_manifest.json`.
- .gitignore líneas 179-183: `data/normalized/*` (salvo `*_manifest.json`), `data/quarantine/`, `data/dukascopy_backfill_progress.json` están ignorados. HECHO MEDIDO: este worktree tiene 355 manifiestos y CERO ficheros `.json` de datos en data/normalized; `data/quarantine/backfill_degradado_20260901/` del contrato NO existe aquí (por eso el test usa el par de `cuarentena/`).
## PASOS (numerados, cortos, en orden; qué comprobar antes de cambiar nada)
1. Comprobar: `git status --short` vacío; `ls data/normalized | grep -v manifest | wc -l` → 0; `PYTHONPATH=. "$PY" -m services.data_ingestion.run_dukascopy_backfill --help` funciona (sin `--force`/`--dry-run` todavía).
2. dukascopy_feed.py — extraer el escritor SIN cambiar bytes de salida. Mover las líneas 413-456 de `ingest()` a `def write_dataset_files(bars, spec, tf, out_dir, *, hours_empty, hours_failed, has_volume) -> Dict[str, object]`: mismo `dataset_id`, mismo `payload_json` (`json.dumps(..., separators=(",", ":"), sort_keys=True)`), `checksum = sha256(payload_json.encode("utf-8"))`, datos con `write_bytes(payload_json.encode("utf-8"))`, manifiesto con los MISMOS 18 campos de hoy (indent=2). Devuelve `{"dataset_id","data_path","manifest_path","bar_count","checksum_sha256"}`. En `ingest()` añadir el kwarg AL FINAL `output_dir: Optional[Path] = None`, `out_dir = Path(output_dir) if output_dir else OUTPUT_DIR`, sustituir `OUTPUT_DIR` (líneas 404, 430, 431) y llamar al escritor. `report["datasets"][tf]` conserva sus claves.
3. run_dukascopy_backfill.py — helpers de módulo (sin clases): `QUARANTINE_DIR = REPO_ROOT / "data" / "quarantine"`, `STAGING_SUBDIR = ".staging_backfill"`, `_sha256_file(path)` (bytes crudos), `_ms(dt)` = `int(dt.replace(tzinfo=timezone.utc).timestamp()*1000)`, `_find_existing(out_dir, symbol, tf, c_start, c_end)`: glob `ds_dukascopy_{symbol.lower()}_{tf}_*_manifest.json`, descartar `_consolidated`, quedarse con `_ms(c_start) <= m["start_time_utc_ms"] <= _ms(c_end + 1 día) - 1` (para 2023-Q1: 1672531200000..1680307199999); si hay varios, el de mayor `bar_count` y AVISO; devuelve `(data_path, manifest_path, manifest)` o None. `_estado_existente(...)` → `"valido"` (datos existen y `_sha256_file == manifest["checksum_sha256"]`), `"ausente"`, `"sin_datos"`, `"invalido"`. `_es_mejor_o_igual(nuevo, viejo)` = `nuevo["hours_failed"] <= viejo["hours_failed"] and nuevo["bar_count"] >= viejo["bar_count"]`. `_a_cuarentena(paths, quarantine_dir, motivo)`: `shutil.move` a `quarantine_dir / f"backfill_{motivo}_{YYYYMMDD}"` (motivo ∈ rechazado|sustituido; si el nombre ya existe, sufijo `.<YYYYMMDDTHHMMSS>`) y añade a `MANIFEST_SHA256.txt` de ese directorio una línea `<nombre> | <sha256>` por fichero.
4. `process_chunk(symbol, key, c_start, c_end, tfs, *, force=False, dry_run=False, concurrency=3, ingest_fn=ingest, out_dir=OUTPUT_DIR, quarantine_dir=QUARANTINE_DIR) -> {"estados": {tf: estado}, "hours_failed": int|None}`, estado ∈ saltado|pendiente|escrito|rechazado|no_data|error. Lógica: (a) por tf: si `valido` y no force → `SKIP <SYM> <KEY> <tf>: manifiesto valido (<dataset_id>, <bar_count> barras, <hours_failed> horas fallidas)` → saltado; si no, pendiente con motivo `sin manifiesto` | `manifiesto sin fichero de datos (<nombre>)` | `manifiesto invalido: sha256 no coincide con el contenido (<nombre>)` | `--force`. (b) sin pendientes → return; si `dry_run` → una línea `PENDIENTE <SYM> <KEY> <tf>: <motivo>` por tf y return SIN crear directorios ni llamar a `ingest_fn`. (c) para sin_datos/invalido: `AVISO <SYM> <KEY> <tf>: <motivo>, se trata como inexistente`; `staging = out_dir / STAGING_SUBDIR / f"{symbol}_{key}"` (mkdir parents); `DESCARGA <SYM> <KEY>: tfs pendientes=[...]`; `report = ingest_fn(symbol, c_start, c_end.replace(hour=23), tfs_pendientes, verbose=True, concurrency=concurrency, output_dir=staging)`; excepción → `ERROR ...` a stderr, estados=error (como hoy). (d) por tf pendiente: `ds = report["datasets"].get(tf)`; sin `dataset_id` → no_data. Si no, cargar `staging/<dataset_id>_manifest.json`; si el existente era `valido` (solo con force) y NO `_es_mejor_o_igual(staged, viejo)` → `_a_cuarentena([datos, manifiesto], "rechazado")` y `RECHAZADO <SYM> <KEY> <tf>: peor que el existente (barras <viejo>-><nuevo>, horas fallidas <viejo>-><nuevo>) -> <dir cuarentena>` → rechazado. En otro caso: si existe par viejo con NOMBRE distinto → `_a_cuarentena(par viejo, "sustituido")`; mover staged a `out_dir` (mismo nombre = sobrescribe) y `ESCRITO <SYM> <KEY> <tf>: <dataset_id> (<n> barras, <hf> horas fallidas, sha256 <16 chars>...)` → escrito. Al final `staging.rmdir()` SOLO si quedó vacío. Logs con `->` ASCII (la consola Windows cp1252 revienta con `→`).
5. `main()`: añadir `--force` y `--dry-run` (`store_true`); sustituir el cuerpo del bucle (líneas 110-135) por `process_chunk(...)`. El checkpoint `done` DEJA de saltar chunks (la autoridad es el manifiesto válido, §3.1); `progress[symbol]` se sigue escribiendo por compatibilidad. Resumen §3.5: `progress["_resumen"][symbol][tf] = {"saltados","escritos","rechazados","con_horas_fallidas"}` (+1 en con_horas_fallidas por tf escrito con hours_failed > 0); `_save_progress` tras cada chunk SALVO en dry-run (no escribe nada); al terminar, imprimir el resumen por símbolo/tf. Actualizar el docstring (6 líneas máx.).
6. §3.3 (reintentos) YA está cubierto por `download_hour` (retries=6, espera 2,4,8,16,30 s; `DukascopyError` solo tras agotar): NO añadir otra capa; cítalo con líneas en A05.md. §3.4 ya se cumple (checksum = sha256 de los bytes escritos): se preserva y se prueba en (c).
7. Test tests/test_dukascopy_backfill_idempotente.py (pytest, `tmp_path`, `capsys`; sin red; nunca toca `data/`). Constantes: `FIX_JSON`/`FIX_MANIFEST` = el par de ENTRADAS; símbolo `USA500IDXUSD`, tf `"1h"`, key `"2023-Q1"`, `c_start=datetime(2023,1,1)`, `c_end=datetime(2023,3,31)`; `out_dir=tmp_path/"normalized"`, `quarantine_dir=tmp_path/"quarantine"`. "Existente" = `shutil.copy2` del par a out_dir. "Descarga" = `_descarga_recortada(n_bars, hours_failed)` → callable con la firma de `ingest` (symbol, start, end, timeframes, verbose, concurrency, output_dir) que lee FIX_JSON, toma `bars[:n_bars]` (recorte, valores intactos), llama a `write_dataset_files(bars, SYMBOLS["USA500IDXUSD"], "1h", output_dir, hours_empty=0, hours_failed=hours_failed, has_volume=True)`, anota la llamada en `calls` y devuelve `{"status":"OK","hours_requested":n,"hours_empty":0,"hours_failed":hours_failed,"ticks_total":sum(tick_count),"datasets":{"1h":{"dataset_id":...,"bars":n}}}`. Comentario obligatorio: "sustituto del descargador etiquetado (W17 §2, infraestructura); ninguna barra inventada". Casos: (a) existente válido, recorte(5,0), sin force → `calls == []`, estado `saltado`, `"SKIP"` en stdout. (b) parametrizado (4,2),(5,1),(4,0) con `force=True` → sha256 del existente sigue siendo `cfdbfea2...7dc9`; estado `rechazado`; exactamente 1 `.json` de datos en `quarantine/backfill_rechazado_*/` y `MANIFEST_SHA256.txt` presente; `"RECHAZADO"` y `"peor que el existente"` en stdout; staging sin ficheros. (c) existente = recorte(4,1) escrito con `write_dataset_files` (nombre `..._1677628800000_1677639600000`, sha256 19010eb958894b7c8590d2d852dea711324fb6ea2468920438ef262d71100c50); `force=True`, recorte(5,0) → queda `ds_dukascopy_usa500idxusd_1h_1677628800000_1677643200000.json` con sha256 `cfdbfea2...7dc9` == `manifest["checksum_sha256"]`, `bar_count` 5; el par de 4 en `backfill_sustituido_*`; estado `escrito`. Variante: existente válido 5/0, `force=True`, recorte(5,0) → `escrito`, `len(calls)==1` (cubre §4.d), mismo sha256. (e) existente inválido: `write_dataset_files(bars[:4])` en out_dir y luego copiar FIX_JSON ENCIMA de su `.json` de datos (mismo nombre; el manifiesto ya no casa) → sin force, recorte(5,0): `len(calls)==1`, `"AVISO"` y `"manifiesto invalido"` en stdout, par viejo en `backfill_sustituido_*`, nuevo escrito. (f) `dry_run=True` con manifiesto copiado SIN el `.json` → `calls==[]`, estado `pendiente`, `"PENDIENTE"` y `"sin fichero de datos"` en stdout, no existe `out_dir/.staging_backfill`.
8. Ejecutar ACEPTACIÓN; pegar salidas crudas en orchestration/results/agy/A05.md; DONE; cierre.
## ACEPTACIÓN (comandos exactos + salida esperada; el orquestador los re-ejecuta él mismo desde la raíz del worktree)
```bash
export PYTHONPATH=. AGY_AGENT=A05; PY="C:/Users/yo/Pictures/Descargaspc/pro/UltrarentablePC/ultrarentable/.venv/Scripts/python.exe"
"$PY" -m pytest tests/test_dukascopy_backfill_idempotente.py -q -p no:cacheprovider        # >= 8 passed, 0 failed, 0 skipped
"$PY" -c "import inspect, services.data_ingestion.dukascopy_feed as f, services.data_ingestion.run_dukascopy_backfill as r; print(inspect.signature(f.ingest)); print(inspect.signature(f.write_dataset_files)); print(inspect.signature(r.process_chunk))"
# ingest(...) termina en output_dir=None; write_dataset_files y process_chunk con las firmas del paso 2/4
"$PY" -m services.data_ingestion.run_dukascopy_backfill --help | grep -c -E -- "--force|--dry-run"   # 2
"$PY" -m services.data_ingestion.run_dukascopy_backfill --start 2023-01-01 --end 2023-06-30 --symbols USATECHIDXUSD --dry-run | tee /tmp/a05_dry.txt
grep -c "^PENDIENTE USATECHIDXUSD 2023-Q[12] " /tmp/a05_dry.txt   # 10 en ESTE worktree (5 tf x 2 trimestres): "manifiesto sin fichero de datos"
grep -c "sin fichero de datos" /tmp/a05_dry.txt                   # 10 ; grep -c "^SKIP" -> 0 ; grep -c -E "^(DESCARGA|ESCRITO|RECHAZADO)" -> 0
# (En el checkout principal, con los .json presentes, el ORQ espera 10 lineas SKIP: Q1 1m 85039/0, 5m 17010/0, 15m 5671/0, 1h 1465/0, 4h 397/0; Q2 1m 85825/2, 5m 17168/2, 15m 5724/2, 1h 1478/2, 4h 401/2 barras/horas fallidas.)
git status --short data/normalized                                # vacio
ls data/normalized/.staging_backfill data/dukascopy_backfill_progress.json 2>&1 | grep -c "No such file"   # 2 (dry-run no crea nada)
sha256sum cuarentena/datasets_superseded/normalized/ds_dukascopy_usa500idxusd_1h_1677628800000_1677643200000.json   # cfdbfea259441b3200bc60e7253bb7fbf7cb1724b51ffcdd8fe8907f6aba7dc9 (fixture intacto)
git diff --name-only   # ⊆ TERRITORIO (más GO_A05.md si el ORQ añadió CORRECCION_n)
```
## RIESGO Y REGLAS ESPECÍFICAS
- ¿Toca semántica del motor (services/validation/engine/ o services/engine_version.py)? NO (ingesta de datos; regla #26 no aplica).
- ¿Ejecuta algo pesado (pytest completo, next build, verificacion_f02, campaña, backfill)? NO. Prohibido lanzar el backfill sin `--dry-run` y prohibido `pytest` completo; si crees necesitar algo pesado: `orca orchestration ask --question "A05 pide admisión para: <cmd>" --json` y esperar el OK.
- Bytes de salida idénticos a los de hoy: `write_dataset_files` no cambia formato ni campos; el kwarg `output_dir` va AL FINAL (compatibilidad posicional con `main()` de dukascopy_feed).
- `--force` fuerza la descarga pero sigue sujeto a "nunca empeorar" (§3.2): un `--force` no es licencia para degradar.
- Manifiesto inválido o sin datos = inexistente (§4.e): no se compara contra él; su par viejo va a `backfill_sustituido_<fecha>/`, nunca se borra.
- Sin dato ⇒ `no_data` / `NO DATA`; nunca valores por defecto, nunca barras fabricadas.
## PROHIBIDO (lista negra, sin excepciones)
git add/commit/push/reset/checkout/merge/stash · rm (se aparca en cuarentena/ con MANIFEST SHA-256) · datos sintéticos, mocks, random/seed, valores por defecto ante falta de dato (se escribe NO DATA) · relajar umbrales · escribir fuera del TERRITORIO · tocar services/engine_version.py salvo que este GO lo ordene · procesos largos sin admisión · inventar una salida que no se ejecutó · declarar subagentes que tu CLI no tiene. Además en este GO: `shutil.rmtree`/`unlink` sobre datos · tocar `download_hour`, `decode_ticks`, `aggregate_bars` o el bloque CSV · tocar `data/normalized/`, `scripts/mine.py`, `scripts/herramientas/consolidar_dukascopy.py` · `monkeypatch` de red (el test inyecta `ingest_fn`, no parchea módulos) · `npm install`.
## SALIDA
1. Working tree con los cambios (SIN commit).
2. orchestration/results/agy/A05.md: comandos ejecutados y salida CRUDA pegada literal (pytest, firmas, dry-run completo); cita de §3.1-§3.5 con la función/línea que cumple cada una y la de §3.3 en `download_hour`; lo que no se pudo; hallazgos fuera de alcance; veredicto propio.
3. orchestration/agy/DONE_A05.md (plantilla: orchestration/agy/PLANTILLA_DONE.md; si no existe en este worktree: ID, veredicto, ficheros tocados, comandos de aceptación con resultado, pendientes).
4. Cierre: orca orchestration send --type worker_done --subject "A05 <PASA|FALLA|PARCIAL>" --body "<3 frases>" --task-id <T> --dispatch-id <D> --outcome succeeded|failed --json
1. Working tree con los cambios (SIN commit).
2. orchestration/results/agy/A05.md: comandos ejecutados y salida CRUDA pegada literal (pytest, firmas, dry-run completo); cita de §3.1-§3.5 con la función/línea que cumple cada una y la de §3.3 en `download_hour`; lo que no se pudo; hallazgos fuera de alcance; veredicto propio.
3. orchestration/agy/DONE_A05.md (plantilla: orchestration/agy/PLANTILLA_DONE.md).
4. Cierre: orca orchestration send --type worker_done --subject "A05 <PASA|FALLA|PARCIAL>" --body "<3 frases>" --task-id <T> --dispatch-id <D> --outcome succeeded|failed --json
