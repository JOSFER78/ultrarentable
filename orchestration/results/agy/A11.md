# INFORME AGY A11 — Telemetría: D2 (espacio completo por defecto) + W2.8 (métricas IS/VAL de quien supera la etapa)

- **Agente**: A11
- **Ola**: A
- **Rama/Worktree**: `JOSFER78/agy-A11`
- **Fecha UTC**: 2026-09-02
- **Veredicto**: PASA (Aceptación completa 100% verde sin errores ni mocks)

---

## 1. Baseline

### 1.1 `git status --porcelain` inicial
Comando:
```bash
git status --porcelain
```
Salida cruda:
```text
(vacía - working tree limpio)
```

### 1.2 Tests existentes de telemetría (Baseline)
Comando:
```bash
"C:/Users/yo/Pictures/Descargaspc/pro/UltrarentablePC/ultrarentable/.venv/Scripts/python.exe" -m pytest tests/test_mine_telemetria_cobertura_familias.py tests/test_mine_telemetria_bruto_neto.py tests/test_mine_gates_passed_write.py -q -p no:cacheprovider
```
Salida cruda:
```text
.............                                                            [100%]
13 passed in 3.52s
```

---

## 2. Medición Real del Espacio de Búsqueda `arquetipos`

Comando ejecutado:
```bash
"C:/Users/yo/Pictures/Descargaspc/pro/UltrarentablePC/ultrarentable/.venv/Scripts/python.exe" -c "from scripts.mine import build_candidate_search_configs; cfgs = build_candidate_search_configs('fondeo', 'ES', '4h', 'arquetipos'); from collections import Counter; print('Total:', len(cfgs)); print('Por familia:', dict(Counter(c['archetype'] for c in cfgs)))"
```
Salida cruda:
```text
Total: 420
Por familia: {'REVERSION_ATR': 108, 'SQUEEZE_BREAKOUT': 96, 'SESSION_MOMENTUM': 72, 'STREAK_EDGE': 72, 'OPENING_RANGE_BREAKOUT': 36, 'VWAP_REVERSION': 36}
```

### Medición en diferentes temporalidades (5m, 15m, 1h, 4h)
Comando ejecutado:
```bash
"C:/Users/yo/Pictures/Descargaspc/pro/UltrarentablePC/ultrarentable/.venv/Scripts/python.exe" -c "from scripts.mine import build_candidate_search_configs; from collections import Counter; print('ES 5m:', len(build_candidate_search_configs('fondeo', 'ES', '5m', 'arquetipos'))); print('ES 15m:', len(build_candidate_search_configs('fondeo', 'ES', '15m', 'arquetipos'))); print('ES 1h:', len(build_candidate_search_configs('fondeo', 'ES', '1h', 'arquetipos')))"
```
Salida cruda:
```text
ES 5m: 420
ES 15m: 420
ES 1h: 420
```

**Confirmación**: El plan indica 420 configuraciones y 6 familias. La medición real en disco y código confirma exactamente **420 configuraciones** desglosadas en las 6 familias:
- `REVERSION_ATR`: 108
- `SQUEEZE_BREAKOUT`: 96
- `SESSION_MOMENTUM`: 72
- `STREAK_EDGE`: 72
- `OPENING_RANGE_BREAKOUT`: 36
- `VWAP_REVERSION`: 36

---

## 3. Modificaciones Realizadas

1. **`scripts/mine.py` (D2 & W2.8)**:
   - Firma `run_mining_pipeline`: `max_candidates: int = 0` (por defecto evalúa el espacio completo; `truncado=False`, `espacio_total == len(search_space)`).
   - `argparse`: `--max-candidates` default cambiado a `0` con ayuda explícita `(default: 0 = espacio completo / D2, nunca truncar por defecto)`.
   - Bucle de evaluación:
     - Tras superar IS: cuando un candidato muere en VAL, se adjunta `"is_trades"` e `"is_pf"` numéricos al registro de telemetría (sin `"val_pf"`).
     - Tras superar VAL: cuando un candidato muere en OOS o en GATES, se adjuntan `"is_trades"`, `"is_pf"`, `"val_trades"`, `"val_pf"` numéricos.
     - Registro de IS mantiene solo `"trades"` y `"pf"` (sin `"is_pf"` ni `"is_trades"`).

2. **`scripts/cola_mineria.py` (D2)**:
   - `_comando_mine`: `max_candidates: int = 0` por defecto.
   - `_lanzar`: `max_candidates: int = 0` por defecto.
   - `argparse`: subcomando `trabajar` define `--max-candidates` default `0`.

3. **`tests/test_mine_telemetria_d2_w28.py`**:
   - 8 tests unitarios y de integración cubriendo (a) espacio completo sin truncado por defecto en `run_mining_pipeline`, (b) default 0 en argparse y firma, (c) propagación en `_comando_mine`, (d) presencia de `is_*` en VAL y `is_*`/`val_*` en OOS/GATES, (e) ausencia de `is_pf` en IS, (f) persistencia en JSON, y (g) ejecución de pipeline con estructura correcta.

---

## 4. Ejecución de Aceptación (Comandos Exactos y Salidas Crudas)

### 4.1 Suite Completa de Tests de Telemetría (Nuevos + Existentes)
Comando:
```bash
"C:/Users/yo/Pictures/Descargaspc/pro/UltrarentablePC/ultrarentable/.venv/Scripts/python.exe" -m pytest tests/test_mine_telemetria_d2_w28.py tests/test_mine_telemetria_cobertura_familias.py tests/test_mine_telemetria_bruto_neto.py tests/test_mine_gates_passed_write.py -q -p no:cacheprovider
```
Salida cruda:
```text
.....................                                                    [100%]
21 passed in 10.16s
```

### 4.2 Verificación de Ayuda CLI de `mine.py`
Comando:
```bash
"C:/Users/yo/Pictures/Descargaspc/pro/UltrarentablePC/ultrarentable/.venv/Scripts/python.exe" scripts/mine.py --help | grep -A2 -- "--max-candidates"
```
Salida cruda:
```text
  --max-candidates MAX_CANDIDATES
                        Límite máximo de configuraciones a evaluar (default: 0
                        = espacio completo / D2, nunca truncar por defecto)
```

### 4.3 Verificación de Firma en `mine.py`
Comando:
```bash
grep -n "max_candidates: int = " scripts/mine.py
```
Salida cruda:
```text
910:    max_candidates: int = 0,
```

### 4.4 Verificación de Default en `cola_mineria.py`
Comando:
```bash
grep -n 'default=' scripts/cola_mineria.py | grep -i "max.candidates"
```
Salida cruda:
```text
318:    p_tra.add_argument("--max-candidates", type=int, default=0,
```

### 4.5 Verificación de Ocurrencias de `is_pf` en `mine.py`
Comando:
```bash
grep -c "is_pf" scripts/mine.py
```
Salida cruda:
```text
3
```

### 4.6 Verificación de Territorio en Git Diff
Comando:
```bash
git diff --name-only
```
Salida cruda:
```text
scripts/cola_mineria.py
scripts/mine.py
```

Comando:
```bash
git status --porcelain
```
Salida cruda:
```text
 M scripts/cola_mineria.py
 M scripts/mine.py
?? tests/test_mine_telemetria_d2_w28.py
```

---

## 5. Hallazgos

1. **Efecto de D2**: Al fijar `max_candidates = 0`, cualquier ejecución de minería explora el espacio de búsqueda completo por defecto (las 420 configuraciones del perfil `arquetipos` en lugar del prefijo truncado de 20 configuraciones de `REVERSION_ATR`).
2. **Efecto de W2.8**: Las métricas de etapas superadas (`is_trades`, `is_pf`, `val_trades`, `val_pf`) quedan adjuntas directamente a cada registro de candidato que alcanza etapas posteriores (VAL, OOS, GATES), posibilitando la trazabilidad y caracterización de near-misses sin inventar datos ni violar el esquema.

---

## 6. Veredicto

**PASA** — Todos los criterios del contrato `GO_A11.md` han sido cumplidos y verificados con pruebas reales deterministas.
