# Informe de Resultados — Tarea A04: Checksum de CONTENIDO en market_ingestor

- **Agente**: A04
- **Ola**: A
- **Rama / Worktree**: `JOSFER78/agy-A04`
- **Fecha**: 2026-09-02

---

## 1. OBJETIVO

`MarketDataAuditor.audit` firma `checksum_sha256` = SHA-256 de los BYTES del fichero normalizado que `persist_normalized_dataset` escribe (el mismo criterio canónico que `services/api/app/api/routes.py:168` y `DatasetRepository._verificar_custodia`), implementando la función reutilizable `verificar_dataset_contra_manifiesto`, y un conjunto de tests unitarios que demuestran que dos series con mismo conteo y rango pero con un solo precio distinto generan hashes y dataset IDs distintos.

---

## 2. ESTADO INICIAL Y PUNTO DE PARTIDA

```bash
git status --porcelain
```
Salida cruda:
*(vacío)*

```bash
git ls-files data/normalized | Select-String -NotMatch "_manifest.json"
```
Salida cruda:
```
data/normalized/.gitkeep
```
> **NO DATA**: En este worktree `data/normalized/` solo contiene archivos `*_manifest.json` y `.gitkeep`. No hay archivos `.json` de velas reales.

---

## 3. COMANDOS DE ACEPTACIÓN Y SALIDA CRUDA

### Comando 1: Suite de tests de checksum de contenido
```bash
$env:AGY_AGENT="A04"; $env:PYTHONPATH="."; & "C:/Users/yo/Pictures/Descargaspc/pro/UltrarentablePC/ultrarentable/.venv/Scripts/python.exe" -m pytest tests/test_market_ingestor_checksum_contenido.py -q -p no:cacheprovider -rs
```
Salida cruda:
```
....s                                                                    [100%]
=========================== short test summary info ===========================
SKIPPED [1] tests\test_market_ingestor_checksum_contenido.py:128: NO DATA: en este worktree data/normalized solo contiene manifiestos
4 passed, 1 skipped in 3.55s
```

### Comando 2: Regresión en pipeline de datos existente
```bash
$env:AGY_AGENT="A04"; $env:PYTHONPATH="."; & "C:/Users/yo/Pictures/Descargaspc/pro/UltrarentablePC/ultrarentable/.venv/Scripts/python.exe" -m pytest tests/test_data_pipeline.py -q -p no:cacheprovider -k market_data
```
Salida cruda:
```
.......                                                                  [100%]
7 passed, 3 deselected in 0.97s
```

### Comando 3: Cálculo determinista de hash sobre serie canónica de 3 velas
```bash
$env:AGY_AGENT="A04"; $env:PYTHONPATH="."; & "C:/Users/yo/Pictures/Descargaspc/pro/UltrarentablePC/ultrarentable/.venv/Scripts/python.exe" -c "from contracts.backtest import BarData; from services.data.market_ingestor import checksum_contenido_sha256 as c; t=1771718400000; print(c([BarData(timestamp_utc_ms=t+i*3600000, open=100+i, high=105+i, low=95+i, close=102+i, volume=50) for i in range(3)]))"
```
Salida cruda:
```
2a5c29e758a06521eafd0f0dcb6277703ac31904f82de127cc303ae52d156de1
```

### Comando 4: Verificación de eliminación del payload de metadatos en market_ingestor.py
```bash
git grep -n "venue}:{symbol}:{interval}" services/data/market_ingestor.py
```
Salida cruda:
*(sin salida; código de retorno 1 — patrón eliminado)*

### Comando 5: Verificación de definición de las 3 funciones requeridas
```bash
git grep -E -c "(def serializar_velas_canonico|def checksum_contenido_sha256|def verificar_dataset_contra_manifiesto)" services/data/market_ingestor.py
```
Salida cruda:
```
services/data/market_ingestor.py:3
```

### Comando 6: Diff de ficheros modificados
```bash
git diff --name-only
```
Salida cruda:
```
services/data/market_ingestor.py
```

### Comando 7: Estado del working tree
```bash
git status --porcelain
```
Salida cruda:
```
 M services/data/market_ingestor.py
?? tests/test_market_ingestor_checksum_contenido.py
```

---

## 4. LO QUE NO SE PUDO HACER / NO DATA

1. **NO DATA en velas físicas de `data/normalized/`**: En este worktree de trabajo (`agy-A04`), la carpeta `data/normalized/` solo contiene archivos de metadatos `*_manifest.json` y `.gitkeep` debido a las reglas de exclusión en `.gitignore`. Por tanto, el test `test_datasets_canonicos_del_worktree_verifican` se omite limpiamente con `pytest.skip("NO DATA: en este worktree data/normalized solo contiene manifiestos")`.
2. **Datasets preexistentes con hash de metadatos**: Los manifiestos históricos en disco que fueron generados previamente con hash de metadatos no han sido re-firmados, ya que no se dispone de las series físicas en este worktree y está fuera del territorio asignado modificarlos.

---

## 5. HALLAZGOS FUERA DE ALCANCE (NO MODIFICADOS)

1. **`services/data/data_downloader.py:193-214 y 361-380`**: En sus métodos `_export_normalized_json`, serializa los ficheros usando `[b.model_dump() for b in bars]` y escribe un manifiesto con `checksum_sha256 = audit_report.checksum_sha256`. Por tanto, su manifiesto no coincidirá con el sha256 de los bytes físicos de su fichero hasta que se actualice para invocar `serializar_velas_canonico` en una orden de trabajo dedicada.
2. **`data/market_ingestor.py` en la raíz del repositorio**: Archivo duplicado y divergente que utiliza formato de lista-de-listas `[timestamp, open, high, low, close, volume]` y carece de importadores en el código activo. No ha sido tocado respetando la estricta delimitación del territorio.
3. **Manifiestos sellados con formato antiguo**: Cualquier validación contra manifiestos antiguos que usen hash de metadatos requerirá un proceso de migración o re-ingesta una vez estén disponibles las series de velas completas.

---

## 6. VEREDICTO

**PASA**. Todos los criterios del contrato de la orden `GO_A04` se han cumplido con rigor, verificabilidad y evidencia cruda.
