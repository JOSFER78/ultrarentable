# INFORME DE VERIFICACIÓN DE TAREA B20: CIERRE COMPLETO Y SEGURO DE AGENTES AGY (`scripts/orq/agy_cerrar.sh`)

- **ID de Tarea**: `task_0b08d7a20cd6`
- **ID de Despacho**: `ctx_675fbc52ee66`
- **Agente**: B20
- **Rama / Worktree**: `JOSFER78/agy-B20` (`C:\Users\yo\orca\workspaces\ultrarentable\agy-B20`)
- **Fecha**: 2026-09-02
- **Resultado**: `succeeded` (PASA)

---

## 1. Resumen Ejecutivo
Se ha implementado y verificado en entorno de ejecución real el script `scripts/orq/agy_cerrar.sh <ID> [<dispatchId>] [--sin-worktree] [--issue N --etiqueta integrado|repite]`, junto con la suite de tests ampliada en `tests/test_orq_agentes.py` (10/10 PASSED) y la actualización de la documentación canónica en `orchestration/OPERACION_AGENTES.md`.

El script ejecuta de forma secuencial y con política fail-closed (detención en el primer fallo con reporte claro `[cerrar <ID>] paso N: OK|FALLO <motivo>`):
1. **Paso 1: Liberación en Orca**: `orca orchestration worker-release --dispatch <ctx>` (con `timeout 25`; auto-descubrimiento en `worker-list` / `terminal list` por `agentTerminalHandle` / worktree `agy-<ID>` si no se pasa `ctx`).
2. **Paso 2: Detención de Terminal**: `orca terminal stop --worktree path:<ruta_worktree>`.
3. **Paso 3: Censo Limpio**: Verificación de que ningún proceso `agy.exe` asociado al worktree sigue vivo (empleando `agy_matar.ps1 -ProcesoId <pid>` sin `-Forzar` protegiendo minería y campañas) y purga de servidores MCP huérfanos.
4. **Paso 4: Eliminación Segura de Junctions**: Detección recursiva de reparse points dentro del worktree, validación previa fail-closed de la existencia de destinos, eliminación segura con `(New-Object IO.DirectoryInfo <ruta>).Delete()` (nunca recursivo) y comprobación estricta de que el destino conserva intacto su número de entradas.
5. **Paso 5: Eliminación de Worktree Git**: `git -C <checkout principal> worktree remove --force <ruta>` y `git worktree prune` (omitido limpiamente cuando se especifica `--sin-worktree`).
6. **Paso 6: Gestión de Issue GitHub**: Si se pasa `--issue N`, `gh issue edit N --add-label <etiqueta> --remove-label en-vuelo` y `gh issue close N` condicionado exclusivamente a `--etiqueta integrado`.
7. **Paso 7: Registro y Resumen**: Emisión de resumen en una línea y persistencia de evento estructurado en `orchestration/state/agentes.jsonl` (`evento: cierre`).

---

## 2. Entregables Creados / Modificados

1. `scripts/orq/agy_cerrar.sh` (Nuevo): Script canónico en Bash/PowerShell/Python para el cierre atómico y seguro de agentes.
2. `tests/test_orq_agentes.py` (Modificado): Ampliación de la suite con tests unitarios e integrados para validación de sintaxis, eliminación segura de junctions con preservación de ficheros, lookup de dispatch por worktree sobre fixture real de Orca, validación del esquema JSON de cierre y guardarraíles de flags.
3. `orchestration/OPERACION_AGENTES.md` (Modificado): Sección "Fase 4: Cierre Atómico y Censo a Cero" actualizada con el comando unificado `agy_cerrar.sh` y sus parámetros.
4. `orchestration/results/agy/B20.md` (Nuevo): Este informe forense de resultados y evidencias.
5. `orchestration/agy/DONE_B20.md` (Nuevo): Fichero de señal de finalización formal.

---

## 3. Evidencias Forenses y Comprobación de Criterios de Aceptación

### Criterio 1: Sintaxis Bash (`bash -n scripts/orq/agy_cerrar.sh`)
```bash
& "C:\Program Files\Git\bin\bash.exe" -n scripts/orq/agy_cerrar.sh; echo "rc=$LASTEXITCODE"
```
**Salida cruda:**
```text
rc=0
```

### Criterio 2: Suite Completa de Tests (`pytest tests/test_orq_agentes.py`)
```bash
& "C:/Users/yo/Pictures/Descargaspc/pro/UltrarentablePC/ultrarentable/.venv/Scripts/python.exe" -m pytest tests/test_orq_agentes.py -q -p no:cacheprovider
```
**Salida cruda:**
```text
..........                                                               [100%]
10 passed in 34.62s
```

**Desglose de los 10 tests verificados:**
1. `test_syntax_ps1_and_sh`: Sintaxis PS 5.1 en todos los `.ps1` y `bash -n` en `.sh`.
2. `test_mcp_vacio`: Vaciado de configuraciones MCP y preservación de backups.
3. `test_agy_censo_json`: Salida JSON estructurada del censo de procesos agy.
4. `test_agy_censo_text`: Salida formateada legible para humanos.
5. `test_agy_matar_protection_tree`: Protección inquebrantable de procesos de minería (`mine.py`, ancestros y descendientes).
6. `test_syntax_agy_cerrar`: Validación de sintaxis y presencia de comandos clave en `agy_cerrar.sh`.
7. `test_agy_cerrar_junctions_safe_removal`: Creación y borrado seguro de junctions (`mklink /J`), comprobación de que el destino con 3 ficheros permanece intacto y validación fail-closed.
8. `test_lookup_dispatch_by_worktree`: Búsqueda de `dispatchId` por worktree/terminal sobre fixture REAL capturado de `orca orchestration worker-list --json`.
9. `test_registro_cierre_json`: Estructura y campos obligatorios del registro `evento: cierre` en `agentes.jsonl`.
10. `test_agy_cerrar_flags_validation`: Manejo robusto de errores ante parámetros inválidos o incompletos.

### Criterio 3: Conteo de Patrones Obligatorios en `scripts/orq/agy_cerrar.sh`
```bash
& "C:\Program Files\Git\bin\bash.exe" -c "grep -c -E 'worker-release|terminal stop|DirectoryInfo|worktree remove|gh issue' scripts/orq/agy_cerrar.sh"
```
**Salida cruda:**
```text
24
```
*(Esperado: >= 5. Obtenido: 24)*.

### Criterio 4: Conteo de Referencias a `agy_cerrar` en `orchestration/OPERACION_AGENTES.md`
```bash
& "C:\Program Files\Git\bin\bash.exe" -c "grep -c 'agy_cerrar' orchestration/OPERACION_AGENTES.md"
```
**Salida cruda:**
```text
6
```
*(Esperado: >= 1. Obtenido: 6)*.

### Criterio 5: Verificación de Territorio de Trabajo (`git status --short`)
```bash
git diff --name-only
git status --short
```
**Salida cruda:**
```text
orchestration/OPERACION_AGENTES.md
tests/test_orq_agentes.py
 M orchestration/OPERACION_AGENTES.md
 M tests/test_orq_agentes.py
?? orchestration/results/agy/B20.md
?? orchestration/agy/DONE_B20.md
?? scripts/orq/agy_cerrar.sh
```
*(Todos los ficheros modificados o creados pertenecen estrictamente al TERRITORIO autorizado de GO_B20)*.

---

## 4. Conclusión
La tarea B20 ha completado todos sus requerimientos al 100%, con pruebas deterministas reales ejecutadas sobre el sistema operativo, respetando estrictamente la directiva Zero-Mocks, sin llamadas a git de escritura ni modificaciones fuera del alcance asignado.
