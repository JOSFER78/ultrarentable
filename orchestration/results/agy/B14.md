# INFORME B14 — Arnés de aceptación v2 (D16): 0 commits sobre la base y GO íntegro

- **Agente**: `B14`
- **Ola**: `B`
- **Rama/Worktree**: `JOSFER78/agy-B14`
- **Fecha**: 2026-09-02
- **Veredicto**: PASA

---

## 1. Resumen Ejecutivo
1. **Qué se hizo**: Se actualizaron `scripts/aceptar_agy.py` y `tests/test_aceptar_agy.py` para rechazar de forma determinista cualquier commit realizado por un agente sobre la base (`commits_del_agente` con `--base <ref>` configurable y resolución automática) y cualquier alteración del contrato del GO (`go_alterado` con verificación exacta de `## TERRITORIO`, `## ACEPTACIÓN` y `## RIESGO` contra `HEAD`, tolerando únicamente `## CORRECCION_n` añadidas al final).
2. **Qué se verificó**: Se ejecutó la suite completa de tests (`pytest tests/test_aceptar_agy.py` ⇒ 12 passed), el self-check del arnés con `--sin-comandos` sobre el propio worktree (`veredicto: ACEPTA`, `rc=0`, 0 commits del agente, GO íntegro), verificación de los identificadores de rechazo en el código (`commits_del_agente` y `go_alterado`), y comprobación estricta de que los ficheros tocados pertenecen exclusivamente al TERRITORIO.
3. **Qué no se pudo hacer / Qué queda**: Nada pendiente. Todas las verificaciones pasaron al 100% sin excepciones ni dependencias externas (solo stdlib).

---

## 2. Detalle Técnico de las Modificaciones

### 2.1 Base y Detección de Commits del Agente
- Se añadió el argumento `--base <ref>` a `scripts/aceptar_agy.py`.
- Si no se especifica `--base`, se resuelven en orden de prioridad: `JOSFER78/orquesta-antigravity-max-10`, `origin/main`, `main`, `master`, o el commit raíz del worktree.
- Se ejecuta `git log --oneline <base>..HEAD`. Si no está vacío, el arnés rechaza con motivo `commits_del_agente` e incluye la lista de commits en el JSON bajo `commits_agente`.
- Si la base no se puede resolver, el arnés rechaza (fail-closed) con motivo `base_no_resuelta`.

### 2.2 Integridad del Contrato GO (`verificar_integridad_go`)
- Compara el archivo local en disco con `git show HEAD:orchestration/agy/GO_<ID>.md`.
- Si el GO no existe en `HEAD`, rechaza con `go_no_versionado`.
- Extrae y valida exactamente el texto de las secciones críticas: `## TERRITORIO`, `## ACEPTACIÓN`, `## RIESGO`.
- Permite modificaciones únicamente si el texto en disco extiende el de `HEAD` al final empezando con `## CORRECCION_`.
- Si alguna sección crítica difiere o se altera el cuerpo de forma no permitida, rechaza con `go_alterado` e incluye las secciones alteradas en `go_secciones_alteradas`.

### 2.3 Tests de Integración en Repositorios Temporales (`tests/test_aceptar_agy.py`)
- Se ampliaron las pruebas unitarias a 12 casos (todos pasando con repositorios git reales, sin mocks):
  - `(a)` Todo dentro de territorio y válido => `ACEPTA` (exit 0)
  - `(b)` Modificación fuera de territorio => `RECHAZA` (`fuera_de_territorio`)
  - `(c)` Modificación en motor no declarada => `RECHAZA` (`regla_26`)
  - `(d)` Comando de aceptación fallido (`false`) => `RECHAZA` (`aceptacion_rc`)
  - `(e)` Ausencia de `DONE_<ID>.md` => `RECHAZA` (`sin_done`)
  - `(f)` Comandos con comillas dobles y variables `$VAR` => `ACEPTA`
  - `(g)` Fichero ignorado bajo `data/` => `ACEPTA` con aviso en `avisos`
  - `(h)` Salida con bytes no ASCII (UTF-8) => `ACEPTA` sin error interno
  - `(i)` Commit hecho por el agente sobre la base => `RECHAZA` (`commits_del_agente`)
  - `(j)` GO con línea añadida en `TERRITORIO` => `RECHAZA` (`go_alterado`)
  - `(k)` GO con `## CORRECCION_1` añadida al final => `ACEPTA`
  - `(l)` Todo limpio con 0 commits del agente => `ACEPTA` con `commits_agente == []`

---

## 3. Evidencia y Comandos de Aceptación (Salida CRUDA)

*(A completar con la ejecución real de los comandos)*
