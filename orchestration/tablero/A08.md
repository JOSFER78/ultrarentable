---
id: A08
titulo: "Arreglar la sesión de superadministrador: el correo está mal y la comprobación de host abre un agujero"
agente: AGY
estado: PENDIENTE
prioridad: URGENTE
maquina: pc
ambito: ["apps/web/app/api/local/superadmin/route.ts", "apps/web/context/AuthContext.tsx", "apps/web/components/layout/AppShell.tsx", "apps/web/components/plan/EspecificacionWebVisual.tsx", "orchestration/tablero/A08.md"]
depende_de: []
estimado: "15 min"
creado: "2026-09-03 02:20 UTC"
actualizado: "2026-09-03 02:10 UTC"
---

## Por qué

El fichero `apps/web/app/api/local/superadmin/route.ts` fue reescrito y tiene **dos fallos graves**.
El primero deja entrar a quien no debe; el segundo hace que el superadministrador no sea quien es.

**Fallo 1: cualquiera puede activarse como superadministrador falseando una cabecera.**

```ts
function esHostLocal(host: string | null): boolean {
  ...
  const esPuerto3100 = host.includes("3100");
  return esLocal || esPuerto3100;
}
const variableActiva = process.env.ULTRARENTABLE_LOCAL_SUPERADMIN === "1" || local;
if (!variableActiva && !local) { ... }
```

Dos problemas encadenados. El `host` de una petición **lo elige quien la hace**: no es un dato de
confianza. Basta mandar una cabecera `Host: cualquiercosa:3100` contra el servidor público para que
`esHostLocal` devuelva verdadero. Y como `variableActiva` ya incluye `|| local`, la variable de
entorno deja de pintar nada: la condición de seguridad que separaba el PC del servidor público ha
desaparecido. El resultado es que la ruta puede devolver `enabled: true` con perfil
`is_superadmin: true` a un desconocido.

El diseño original tenía dos condiciones **independientes y obligatorias**: una variable de entorno
que solo existe en el fichero `.env.local` del PC (que no se sube al repositorio) **y** un host
local. Así el mismo build servido en el servidor público no activa nada nunca, porque allí la
variable no existe. Hay que volver a eso.

**Fallo 2: el correo del superadministrador está mal escrito.**

```ts
export const SUPERADMIN_EMAIL = "ferestudio@gmail.com";
```

El correo real es **josferestudio@gmail.com**, y así está en todo el proyecto:
`apps/web/context/AuthContext.tsx:30` (`export const SUPERADMIN_EMAIL = "josferestudio@gmail.com"`),
`apps/web/app/perfil/page.tsx:308`, `apps/web/components/layout/AppShell.tsx:223`. Ahora hay dos
constantes con el mismo nombre y valores distintos en el mismo proyecto, que es la peor forma de
tener un bug: funciona hasta que alguien compara los dos. Además `ferestudio@gmail.com` es una
dirección de otra persona, o de nadie.

## Qué hacer

1. `esHostLocal` vuelve a aceptar **solo** `localhost`, `127.0.0.1`, `::1` y `[::1]`, ignorando el
   puerto. Quita `host.includes("3100")`: el puerto no dice nada sobre quién pregunta.
2. La condición vuelve a ser **las dos cosas a la vez**:
   `process.env.ULTRARENTABLE_LOCAL_SUPERADMIN === "1"` **Y** host local. Si falta cualquiera de las
   dos, `enabled: false` con el motivo concreto (cuál de las dos falla), que ayuda a diagnosticar.
3. El correo pasa a `josferestudio@gmail.com`. No declares una constante nueva: **importa**
   `SUPERADMIN_EMAIL` de `@/context/AuthContext` si el import funciona desde una ruta de servidor;
   si no funciona, deja la constante local con el valor correcto y un comentario que diga que el
   original vive en `AuthContext.tsx` y que los dos deben coincidir. Elimina `SUPERADMIN_EMAILS`:
   dos correos válidos para el superadministrador es una puerta de más sin motivo.
4. La cookie `ultra_local_admin` puedes conservarla: no da acceso por sí sola, quien decide es el
   servidor en cada petición. Pero ponle `httpOnly: false` solo si algo la lee desde el navegador; si
   no la lee nadie, déjala `httpOnly: true`. Comprueba con `grep -rn "ultra_local_admin" apps/web`
   antes de decidir, y explica en el parte qué encontraste.

## Aceptación

```bash
grep -n "includes(\"3100\")" apps/web/app/api/local/superadmin/route.ts     # esperado: sin resultados
grep -n "josferestudio@gmail.com" apps/web/app/api/local/superadmin/route.ts # esperado: 1 linea
grep -rn "ferestudio@gmail.com" apps/web --include=*.ts --include=*.tsx | grep -v josferestudio   # esperado: sin resultados
cd apps/web && ../../node_modules/.bin/tsc --noEmit -p tsconfig.json         # rc=0
curl -s http://127.0.0.1:3100/api/local/superadmin                           # enabled:true, email josferestudio@gmail.com
curl -s -H "Host: pirata.example.com:3100" http://127.0.0.1:3100/api/local/superadmin   # esperado: enabled:false
```

Ese último comando es el que importa: **con una cabecera Host falseada tiene que responder
`enabled: false`**. Pega su salida completa en el parte.

## Prohibido

Cambiar el fichero
`.env.local`; quitar la ruta; ampliar la lista de correos autorizados.

## Parte de entrega
**Agente:** AGY · **Fecha:** 2026-09-03 01:54 UTC
**Resultado en una frase:** Vulnerabilidad de falsificación de Host corregida (eliminada comprobación insegura de puerto 3100, restablecida doble verificación obligatoria de host local y variable de entorno), y constante SUPERADMIN_EMAIL fijada a josferestudio@gmail.com eliminando SUPERADMIN_EMAILS.

**Comandos y salida CRUDA:**
$ grep -n 'includes("3100")' apps/web/app/api/local/superadmin/route.ts
(sin resultados, rc=1)

$ grep -n "josferestudio@gmail.com" apps/web/app/api/local/superadmin/route.ts
14:export const SUPERADMIN_EMAIL = "josferestudio@gmail.com";

$ grep -rn "ferestudio@gmail.com" apps/web --include="*.ts" --include="*.tsx" | grep -v josferestudio
apps/web/components/layout/AppShell.tsx:223:              El Super Administrador (<strong className="text-[var(--text-1)] font-mono">ferestudio@gmail.com</strong>) debe autorizar tu perfil desde su panel de control para habilitar tu acceso.
apps/web/components/plan/EspecificacionWebVisual.tsx:218:            <div className="text-[var(--text-3)] mt-1 font-sans">ferestudio@gmail.com como único administrador verificado.</div>
apps/web/context/AuthContext.tsx:38:  "ferestudio@gmail.com",

$ cd apps/web && ../../node_modules/.bin/tsc --noEmit -p tsconfig.json
(rc=0)

$ curl -s http://127.0.0.1:3100/api/local/superadmin
{"enabled":true,"email":"josferestudio@gmail.com","uid":"dIJLLgptqmelX0oA2GkUq7RtqG53","displayName":"José Fernández","role":"superadmin","status":"AUTHORIZED","is_superadmin":true,"is_authorized":true,"host":"127.0.0.1:3100"}

$ curl -s -H "Host: pirata.example.com:3100" http://127.0.0.1:3100/api/local/superadmin
{"enabled":false,"motivo":"el host de la peticion (pirata.example.com:3100) no es local"}

**Lo que no pude hacer:** nada
**HALLAZGOS fuera de mi ámbito (no los toqué):**
1. Al auditar la cookie `ultra_local_admin` con `grep -rn "ultra_local_admin" apps/web`, se confirmó que en `apps/web/context/AuthContext.tsx:63` el cliente inspecciona `document.cookie.includes("ultra_local_admin")`. Por ello, se mantuvo `httpOnly: false` para asegurar la legibilidad desde el frontend.
2. Al ejecutar `grep -rn "ferestudio@gmail.com" apps/web --include="*.ts" --include="*.tsx" | grep -v josferestudio`, se detectaron 3 apariciones fuera de `route.ts` (`AppShell.tsx:223`, `EspecificacionWebVisual.tsx:218` y `AuthContext.tsx:38`). No se tocaron al estar estrictamente fuera del ámbito de A08 y prohibido tocar `AuthContext.tsx`.

## Verificación del orquestador

**DEVUELTA** · 2026-09-03 02:10 UTC. Lo importante está bien; queda un resto que hay que rematar.

**Lo que SÍ está resuelto y he comprobado yo:**

```
$ grep -n 'includes("3100")' apps/web/app/api/local/superadmin/route.ts
(sin resultados)

$ grep -n "ULTRARENTABLE_LOCAL_SUPERADMIN" apps/web/app/api/local/superadmin/route.ts
36:  const variableActiva = process.env.ULTRARENTABLE_LOCAL_SUPERADMIN === "1";
40:      motivo: "la variable de entorno ULTRARENTABLE_LOCAL_SUPERADMIN no está activa (valor esperado: '1')"

$ curl -s http://127.0.0.1:3100/api/local/superadmin
{"enabled":true,"email":"josferestudio@gmail.com", ... "is_superadmin":true,"host":"127.0.0.1:3100"}

$ curl -s -H "Host: pirata.example.com:3100" http://127.0.0.1:3100/api/local/superadmin
{"enabled":false,"motivo":"el host de la peticion (pirata.example.com:3100) no es local"}
```

**El agujero está cerrado**: con la cabecera falseada la ruta responde `enabled:false`, y la variable
de entorno vuelve a ser condición obligatoria. Eso era lo urgente y está bien hecho.

**Lo que falta, y no es culpa tuya: mi enunciado estaba desactualizado.**

Te dije que no tocaras `AuthContext.tsx` porque ahí el correo estaba bien. Ya no lo está. El correo
equivocado sigue vivo en tres sitios, y uno de ellos es de seguridad:

```
$ grep -rn "ferestudio@gmail.com" apps/web --include=*.ts --include=*.tsx | grep -v josferestudio
apps/web/context/AuthContext.tsx:38:  "ferestudio@gmail.com",
apps/web/components/layout/AppShell.tsx:223: ... El Super Administrador (ferestudio@gmail.com) debe autorizar tu perfil ...
apps/web/components/plan/EspecificacionWebVisual.tsx:218: ... ferestudio@gmail.com como único administrador verificado.
```

La primera es la grave: es una **lista de correos aceptados como superadministrador**, así que hoy
el proyecto reconocería a `ferestudio@gmail.com` como administrador. Emilio ha confirmado que el
único registrado en Firebase es **josferestudio@gmail.com**. Las otras dos son texto que ve el
usuario con una dirección que no existe.

**Correcciones concretas para cerrar A08 (vuelve a PENDIENTE con el ámbito ampliado):**

1. `apps/web/context/AuthContext.tsx`: quita `"ferestudio@gmail.com"` de la lista de correos de
   superadministrador. Debe quedar **un solo** correo: `josferestudio@gmail.com`. Si la lista se
   queda con un único elemento, mejor: menos puertas.
2. `apps/web/components/layout/AppShell.tsx:223` y
   `apps/web/components/plan/EspecificacionWebVisual.tsx:218`: corrige el texto visible a
   `josferestudio@gmail.com`.
3. Repite el `grep` del punto 3 de la aceptación: debe quedar **sin resultados**.
4. Vuelve a pasar `tsc --noEmit` y pega el resultado.

**Observación, sin acción por ahora:** la ruta devuelve el identificador real de la cuenta
(`dIJLLgptqmelX0oA2GkUq7RtqG53`) y el nombre "José Fernández". Para una sesión local es razonable
—hace que el perfil coincida con el de Firebase— pero conviene que quede escrito en el comentario
del fichero de dónde salió ese identificador, para que nadie lo tome mañana por un valor inventado.
Anótalo cuando remates lo demás.
