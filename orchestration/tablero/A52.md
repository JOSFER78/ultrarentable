---
id: A52
titulo: "Toda la configuración de los motores, editable y documentada en el panel de superadministrador"
agente: AGY
estado: VERIFICADO
fase: F09
prioridad: URGENTE
maquina: pc
ambito: ["services/api/app/api/config_router.py (nuevo)", "apps/web/app/perfil/ o apps/web/app/admin/", "scripts/herramientas/generar_proyectos_fondeo_sqx.py", "scripts/herramientas/m1_runner_sqx.py", "orchestration/tablero/A52.md"]
depende_de: []
estimado: "120 min"
creado: "2026-09-03 22:05 UTC"
actualizado: "2026-09-03 23:05 UTC"
---

## Por qué

Emilio, esta noche, al ver las primeras 32 estrategias que cumplen su objetivo: *"tienes que ir
consolidando lo que tienes en un panel de administración, donde esté todo documentado y organizado,
con las fichas de configuración de cómo funciona cada motor. En el panel del superadministrador debe
estar toda la configuración de la página de estrategias: el M1, los filtros que tiene, todo, **para
poder modificarlo desde administración y no tener que programarlo constantemente**, y también para
tenerlo documentado. Investiga cómo hacerlo de manera estable, para que mañana, cuando le pida a otro
agente algo, funcione."*

**Y hoy hemos vivido tres veces exactamente el problema que él describe.** Todos estos números están
escritos dentro de scripts, y cambiar cualquiera ha costado una tarea de programación:

| Parámetro | Dónde está escondido hoy | Qué costó cambiarlo |
| :--- | :--- | :--- |
| Umbrales de aceptación (factor 1,05, retorno/caída 0,5, operaciones por mes) | `generar_proyectos_fondeo_sqx.py:70-73` | Se aflojaron a las 10:45 y **no llegaron al servidor hasta las 15:29** (A35) |
| Capital y riesgo por operación | el mismo fichero, ahora `50000` y `0,5 %` | Una tarjeta entera (A47) y otra para extenderlo (A51) |
| Tope de horas por celda, margen de parada | `m1_runner_sqx.py` | — |
| Orden de los marcos en la cola | `m1_runner_sqx.py:41` | Una tarjeta (A45) |
| Criterio de calidad (factor 1,3 / 1,0 / 20 operaciones) | `background_searcher.py:96-98` | Nadie sabía que existía hasta que lo encontré esta noche |

## Qué hacer

**1. Un único sitio para la configuración, fuera del código y fuera del repositorio.** Un fichero de
configuración en `~/.ultrarentable/` (como ya se hace con las claves de IA), leído por la API y por
los scripts. **Nadie más define un valor por su cuenta**: si un script necesita un umbral, lo pide
ahí, y si no está, **falla diciendo qué falta** en vez de inventarse un valor por defecto.

**2. Una pantalla en el panel de superadministrador** con las fichas por motor. Empieza por **M1
(StrategyQuant)**, que es el que está en marcha, con estos grupos:
- **Universo**: activos y marcos temporales activos, tope de horas por celda, orden de prioridad.
- **Dimensionamiento**: capital de la cuenta, método (riesgo por operación o contratos fijos), riesgo
  en %.
- **Aceptación en StrategyQuant**: factor de beneficio, retorno/caída, operaciones por mes por marco,
  aciertos mínimos.
- **Criterio de calidad del censo**: los tres umbrales que hoy están en `background_searcher.py`.

**3. Cada parámetro, con su explicación al lado.** Emilio no programa: cada campo lleva una frase de
qué es y qué pasa si se sube o se baja. Ejemplo real que puedes usar: *"Riesgo por operación: cuánto
se arriesga en cada entrada. Al 0,5 % la rentabilidad se multiplicó por 5; al 1 %, casi la mitad de
las estrategias superan la caída máxima que aguanta una cuenta de fondeo."*

**4. Que se vea qué está en vigor y qué no.** El problema de A35 fue que el valor del código y el del
servidor **no eran el mismo durante cinco horas y nadie lo sabía**. La pantalla debe enseñar, al
lado de cada valor, **si el servidor lo tiene aplicado**, comparándolo con lo que hay en los
proyectos vivos. Un valor guardado que no está en vigor tiene que verse en rojo.

**5. Historial.** Cada cambio, con fecha y valor anterior, en el mismo fichero. Es la parte de
"documentado" que pide Emilio y la que permite responder a "¿por qué cambió esto?".

**6. Nada de aplicar cambios al servidor desde la pantalla, todavía.** Guardar y mostrar, sí;
regenerar proyectos desde un botón, **no** en esta tarjeta: eso toca 30 celdas y ya sabemos lo que
pasa cuando se toca el manifiesto a destiempo. Se hará en otra, con su procedimiento.

## Las bandas de extracción, medidas (orquestador, 22:20 UTC)

Emilio ha cambiado el criterio esta noche: *"si una tiene seis, siete, ocho, diez por ciento de
caída, se puede extraer para mejorar... lo que no vamos a coger es una con 0,01 % mensual o con 90 %
de caída"*. Lo he aplicado a la ronda nueva de `FONDEO_MGC_H4` (581 estrategias) y **el cambio no es
cosmético**:

```
                                          con mi corte rigido    con la banda de Emilio
  apta para operar   (>2 %/mes, caida <6 %)        32                     32
  apta para mejorar  (>2 %/mes, caida 6-12 %)       0                     36    <- se tiraban
  con promesa        (1-2 %/mes, caida <=12 %)      0                    157    <- se tiraban
                                              ---------              ---------
  TOTAL EXTRAIBLE                                   32                    225   de 581
```

Y entre las que mi corte rígido descartaba estaban estas:

```
  56,5 % anual (4,71 %/mes) con 6,2 % de caida    <- fuera por dos decimas
  55,6 % anual (4,64 %/mes) con 8,2 %
  53,0 % anual (4,41 %/mes) con 11,4 %
```

La primera se perdía **por dos décimas** por encima del límite, teniendo casi un 5 % mensual. Con la
fase de mejora por delante, tirarla es absurdo.

**Por eso las tres bandas son configuración y van en esta pantalla**, en el grupo "criterio de
calidad del censo", con estos valores de partida y **editables**:

| Etiqueta | Rentabilidad mensual | Caída (% de la cuenta) |
| :--- | :--- | :--- |
| **Apta para operar** | ≥ 2 % | < 6 % |
| **Apta para mejorar** | ≥ 2 % | 6 – 12 % |
| **Con promesa** | 1 – 2 % | ≤ 12 % |
| *(descartada)* | < 0,5 % | o > 25 % |

Y con la explicación al lado, que es lo que pide Emilio: *"el límite de abajo saca lo que no tiene
ventaja aprovechable; el de arriba, lo insalvable. En medio se extrae y se etiqueta, porque la fase
de mejora existe para bajar la caída de algo que ya gana."*

## Aceptación

```bash
# 1) la configuracion vive fuera del codigo y la API la sirve
curl -s http://127.0.0.1:8100/api/v2/config/motores | head -c 400
ls ~/.ultrarentable/config_motores.json

# 2) ningun script define ya esos numeros por su cuenta
grep -nE "^MIN_PF|^MIN_RET_DD|^MIN_OPS_MES|^FONDEO_CAPITAL|^FONDEO_RIESGO|^MIN_PF_IS" scripts/herramientas/generar_proyectos_fondeo_sqx.py services/background_searcher.py
#    -> o no aparecen, o aparecen leyendo de la configuracion

# 3) la pantalla existe y trae los valores y su explicacion
curl -s http://127.0.0.1:3100/perfil | grep -cE "Riesgo por operacion|Riesgo por operación|Aceptación"   # > 0

# 4) y dice si estan en vigor en el servidor
curl -s http://127.0.0.1:8100/api/v2/config/motores | grep -c "en_vigor"    # > 0

# 5) despues de tocar la API, reiniciala y vuelve a pasar 1) y 4)
```

## Prohibido

Valores por defecto escondidos en el código "por si acaso" (es lo que devolvió A46). Duplicar un
parámetro en dos sitios. Botones que regeneren proyectos o reinicien servicios en esta tarjeta.
Guardar la configuración dentro del repositorio. Barra de menú arriba; grises, negro y blanco, con
verde y rojo solo para beneficio y pérdida.

## Qué se verá en la web al terminar

En el panel de superadministrador, una pantalla con la ficha del motor M1: su universo, su
dimensionamiento, sus filtros y su criterio de calidad, cada uno con su explicación y con un aviso
si lo guardado no coincide con lo que el servidor está aplicando. Emilio podrá cambiar el riesgo por
operación sin pedirle a nadie que programe, y sabrá qué está en vigor.

## Parte de entrega

**Fecha y hora:** 2026-09-03 23:05 UTC
**Agente:** AGY

### 1. Fichero físico y API centralizada (Criterio 1 y 4)
- Fichero físico creado fuera del repo en `~/.ultrarentable/config_motores.json` (espejado también en Hetzner `/opt/SQX-headless/import/config_motores.json`).
- Módulo central `services/api/app/config_motores_core.py` implementando lectura estricta, validación de bloques, guardado con historial de auditoría y función `comprobar_en_vigor()`.
- Router FastAPI `services/api/app/api/config_router.py` montado en `/api/v2/config` (`GET /api/v2/config/motores`, `POST /api/v2/config/motores`).
- Evidencia cruda de ejecución en vivo:
```
=== CRITERIO 1 ===
JSON cargado: True
Fichero físico existe: True C:\Users\yo\.ultrarentable\config_motores.json

=== CRITERIO 4 ===
Coincidencias 'en_vigor' en JSON API: 5
Detalle en_vigor:
- dimensionamiento: en_vigor = True ("Coincide con manifiesto activo de StrategyQuant")
- aceptacion_sqx: en_vigor = True ("Coincide con reglas de compilación de proyectos")
- universo: en_vigor = True ("Las 30 celdas del universo están cargadas en el servidor")
- calidad_censo: en_vigor = True ("Aplicado en memoria por API de candidatos")
```

### 2. Eliminación de constantes hardcodeadas (Criterio 2)
- Modificado `scripts/herramientas/generar_proyectos_fondeo_sqx.py` para leer `capital_inicial`, `riesgo_pct`, `min_ops_mes`, `min_pf`, `min_ret_dd`, `min_win_pct` directamente desde `~/.ultrarentable/config_motores.json`.
- Modificado `services/background_searcher.py` para leer `min_pf_is`, `min_pf_oos`, `min_trades_oos` desde la configuración central (bloque `calidad_censo`).
- Evidencia cruda:
```
=== CRITERIO 2 ===
Comando: Select-String -Pattern '^(MIN_PF|MIN_RET_DD|MIN_OPS_MES|FONDEO_CAPITAL|FONDEO_RIESGO|MIN_PF_IS)' -Path 'scripts/herramientas/generar_proyectos_fondeo_sqx.py', 'services/background_searcher.py'
Salida cruda: ''
Coincidencias hardcodeadas: 0
```

### 3. Pantalla de administración interactiva (Criterio 3)
- Creado componente `apps/web/components/superadmin/ConfiguracionMotores.tsx` e integrado en `apps/web/app/perfil/page.tsx`.
- Incluye:
  - Ficha completa M1 (Dimensionamiento, Aceptación StrategyQuant, Bandas de Extracción de Emilio, Universo de Futuros Micro).
  - Explicación contextual de cada campo ("Riesgo por operación: cuánto se arriesga...", etc.).
  - Badges vivos `EN VIGOR` (verde) y alerta en rojo si discrepa con el servidor.
  - Tabla de historial de auditoría de cambios.
  - Botón de persistencia directa vía API.
- Compilado con `npm run build` (0 errores) y corriendo en `http://127.0.0.1:3100`.
- Evidencia cruda:
```
=== CRITERIO 3 ===
Texto 'Riesgo por operación' en /perfil: True
Texto 'Aceptación' en /perfil: True
```

### 4. Prueba Dinámica de Modificación y Detección de Discrepancia (Criterio 5)
- Ejecutado ciclo de mutación:
  1. Estado base: `riesgo_pct = 0.5`, `en_vigor = True`.
  2. Modificación vía `POST` a `1.0 %`: `~/.ultrarentable/config_motores.json` se actualiza en disco, el historial anota el cambio por usuario, y la API devuelve:
     `en_vigor = False`
     `motivo = "Servidor aplica 50000 USD / 0.5 %; configuración pide 50000 USD / 1.0 %"`
  3. Restauración vía `POST` a `0.5 %`: `en_vigor = True`, `motivo = "Coincide con manifiesto activo de StrategyQuant"`.
- Los 5 criterios de aceptación están 100% satisfechos y comprobados en vivo.

## Verificación del orquestador

**Fecha:** 2026-09-04 00:55 UTC · **Resultado: VERIFICADA** (van 41). Es la que más va a durar de
todas las de hoy.

**La configuración ya no vive en el código:**
```
$ ls ~/.ultrarentable/
config_motores.json     3.143 bytes      <- fuera del repositorio

$ grep -nE "^MIN_PF|^MIN_RET_DD|^MIN_OPS_MES|^FONDEO_CAPITAL|^FONDEO_RIESGO" generar_proyectos_fondeo_sqx.py
(ninguna)
$ grep -nE "^MIN_PF_IS|^MIN_PF_OS|^MIN_TRADES" background_searcher.py
(ninguna)
```

Los cinco sitios donde estaban escondidos esos números, vacíos. Y lo que más me importaba: **el
script falla si la configuración no está**, en vez de inventarse un valor —
`generar_proyectos_fondeo_sqx.py:70-85`, `raise FileNotFoundError`. Es lo contrario del `or
"2023.01.02"` que devolvió A46.

**Y la API la sirve entera, con las tres cosas que pedí:**
```
claves: ['config', 'descripciones', 'en_vigor', 'origen']

universo        : 6 simbolos, 5 marcos, prioridad [H1,H4,M15,M5,M1], tope 1 h
dimensionamiento: capital 50.000, RiskFixedBalancePct, riesgo 0,5 %
aceptacion_sqx  : pf 1,05 · ret/dd 0,5 · ops/mes {M1:20 M5:10 M15:5 H1:2 H4:1}
calidad_censo   : pf_is 1,3 · pf_oos 1,0 · 20 operaciones · y las tres bandas
```

**El indicador de "en vigor" está, y es la mitad del valor de esta tarjeta:**
```
"dimensionamiento": {"en_vigor": true, "servidor": {"capital": 50000, "riesgo_pct": 0.5},
                     "motivo": "Coincide con manifiesto activo de StrategyQuant"}
```
No compara consigo mismo: **compara lo guardado con lo que StrategyQuant tiene aplicado de verdad**.
Eso es exactamente lo que faltó esta mañana, cuando el umbral del código y el del servidor no
coincidieron durante cinco horas y nadie lo supo. Con esta pantalla, hoy se habría visto en rojo.

**Y he abierto la página yo**, que es la lección que me llevo de esta noche: `/perfil` enseña
*"Riesgo por operación"*, *"Apta para operar"*, *"Aceptación"* y el sello **EN VIGOR**. El componente
vive aparte (`components/superadmin/ConfiguracionMotores.tsx`), como pedía la regla de Emilio de
separar el código por partes. `tsc` en rc=0.

**Lo que queda para otra tarjeta, y lo dejo dicho para que no se olvide:** desde esta pantalla se
puede **ver y guardar**, pero no aplicar. Cambiar un umbral y que se regeneren los 30 proyectos toca
el manifiesto y el bucle, y ya sabemos lo que pasa cuando eso se hace a destiempo. Va con su
procedimiento cuando toque.
