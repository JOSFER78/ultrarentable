---
id: A36
titulo: "Las 40.000 estrategias de los bancos solo existen en la memoria de StrategyQuant: hay que volcarlas a ficheros antes de perderlas"
agente: AGY
estado: VERIFICADO
fase: F03
prioridad: URGENTE
maquina: hetzner
ambito: ["ssh sqx-hetzner", "services/api/app/api/strategy_lab_router.py", "services/sqx_bridge/", "tests/", "orchestration/tablero/A36.md"]
depende_de: []
estimado: "60 min"
creado: "2026-09-03 15:40 UTC"
actualizado: "2026-09-03 17:40 UTC"
---

## Por qué

Verificando A33 fui a comprobar el hash canónico de las estrategias del censo y tiré del hilo hasta
esto, que es más grave que lo que estaba buscando.

**1. En el censo no hay estrategias, hay fichas.** El campo que guardaría las reglas está vacío en
todas las filas (`"source_payload": None, "source_artifact_sha256": None`, escrito así en
`strategy_lab_router.py:227`):

```
$ sqlite3 … "las 51 promovidas por A33"
con source_payload (las reglas) = 0        SIN source_payload = 51
```

**2. El hash canónico no se puede reproducir.** Se calcula sobre la fila cruda de StrategyQuant y el
censo guarda las métricas ya extraídas. Rehecho con todo lo que el censo conserva:

```
sqx:FONDEO_MYM_H4:Results:Strategy 3.62.134(1)
   guardado = 2fadc3ff4120afe6978ac1b55cb39cc1…
   rehecho  = 500ae70b4842fc6a67c1c8d5fdf0375f…   NO COINCIDE   (3 de 3)
```

Es mi fallo: firmé A31 comprobando que los hashes eran únicos y no que fueran reproducibles.
Corregido por escrito en `A31.md`.

**3. Y lo urgente: los ficheros de las estrategias NO EXISTEN en disco.** Ninguna de las 30 celdas
tiene un solo artefacto guardado:

```
$ find /opt/SQX-headless/user/projects -path '*FONDEO*' -name '*.sqx' | wc -l
0
$ du -sh /opt/SQX-headless/user/projects/FONDEO_MYM_H4/databanks
28K                         # con 20.000 estrategias "en banco"
$ find /opt/SQX-headless/user/projects -name '*.sqx' | sed 's|/[^/]*$||' | uniq -c
   2035 Ultra_Matrix/databanks/ToImprove      # los unicos que hay, del proyecto viejo
```

**Pero en la memoria de StrategyQuant sí están, ahora mismo:**

```
$ curl '…/call?cmd=-databank action=list project=FONDEO_MNQ_H1'
17:36:28 Lista de bases de datos disponibles
Results, Records: 20000
Last generation, Records: 0    Initial population, Records: 0    …
```

Es decir: **40.000 estrategias (20.000 en `MNQ_H1` y 20.000 en `MYM_H4`) viven solo dentro del
proceso `sqcli`**. Si ese proceso se reinicia, o si a alguien se le ocurre recargar la configuración
de esos dos proyectos, se pierden y no hay forma de recuperarlas: lo único que quedaría son los CSV
con nombres y métricas, sin las reglas. Llevan así desde las 13:46.

Y por eso mismo, en A35 te dije "no toques `H1` ni `H4`". Lo dije por conservar sus bancos y resulta
que era más literal de lo que yo sabía: recargar su `project.cfx` habría vaciado la memoria y adiós a
las 40.000.

## Qué hacer

**Primero lo urgente (los volcados), después lo que arregla el censo.**

1. **Volcar a ficheros los bancos que tienen algo, sin reiniciar ni recargar nada.** El modo de
   comandos de StrategyQuant lo sabe hacer; lo he comprobado en su propia ayuda:
   ```
   -databank  action: [list,count,save,load,delete,clear,create,remove,synctofiles,syncfromfiles,copy,move,export]
              folder: (optional) Path of the folder
   ```
   Úsalo con `action=synctofiles` y una carpeta por celda, y si esa no persiste, prueba `action=save`.
   Las celdas con banco hoy son **`FONDEO_MNQ_H1`**, **`FONDEO_MYM_H4`**, **`FONDEO_MGC_M5`** y
   **`FONDEO_MGC_M15`** (compruébalo tú con `action=list` antes, que puede haber cambiado).
   **Comprueba el resultado contando ficheros**, no por el mensaje del comando: ya sabemos que este
   programa dice "hecho" cuando no ha hecho nada (fue el fallo de A35).

2. **Que el bucle lo haga solo a partir de ahora.** En `m1_runner_sqx.py`, en el momento en que una
   celda se cierra y justo antes de exportar el CSV, volcar el banco a
   `/opt/SQX-headless/import/fondeo/artefactos/<CELDA>_r<N>/`. Hoy el bucle exporta el CSV y da la
   celda por hecha, dejando las estrategias en el aire. **Un test que lo fije.**

3. **Meter el artefacto en el censo.** Para cada fila: `source_artifact_sha256` con el SHA-256 del
   fichero tal cual, y `source_payload` con su contenido (o su ruta en el repositorio de evidencia
   más la huella, si pesa demasiado: **di cuál eliges y por qué**).

4. **Hacer el hash canónico reproducible.** La condición que debe cumplirse y quedar fijada por un
   test es **`sha256(lo que el censo guarda) == canonical_hash`**. Lo más simple es calcularlo sobre
   el mismo objeto que se serializa en `dsl_json`.

5. **Rellenar hacia atrás** las 1.006 filas de fondeo que ya están, no solo las nuevas.

## Diagnóstico añadido por el orquestador (2026-09-03 17:35 UTC) — leer antes de empezar

He ido a mirar los 2.035 artefactos que sí existen (los de `Ultra_Matrix/databanks/ToImprove`) para
que no tengas que descubrir esto por el camino. Cuatro cosas medidas:

**1. Un artefacto `.sqx` es un ZIP con la estrategia dentro.**
```
$ file "Strategy 1.0.106.sqx"
Java archive data (JAR)
$ head -c 220 "Strategy 1.0.106.sqx"
PK…META-INF/MANIFEST.MF…  PK…settings.xml…
```
Es un archivo comprimido con `META-INF/MANIFEST.MF` y `settings.xml`. O sea que **el artefacto sí
contiene la definición de la estrategia**, que es justo lo que le falta al censo. Guárdalo tal cual,
sin descomprimir ni reformatear: el SHA-256 se calcula sobre el fichero como está.

**2. El mecanismo funciona y hay precedente: esos 2.035 se escribieron de golpe.**
```
$ ls -la --time-style=+%F_%H:%M …/*.sqx | conteo por minuto
   1276  2026-09-03_02:48
    759  2026-09-03_02:49
```
Todos en dos minutos, a unos 1.276 ficheros por minuto. Alguien lanzó un volcado a las 02:48 de esta
madrugada y salió bien. No estás abriendo camino: estás repitiendo algo que ya ha ocurrido en esta
misma máquina.

**3. Y esto decide la pregunta que te dejé abierta.** Te decía "guarda el contenido, o la ruta más la
huella si pesa demasiado; di cuál eliges". Ya está medido y **la respuesta es la ruta más la huella**:
```
$ tamaño medio de artefacto = 84,1 KB   (2.035 ficheros = 167 MB)
   ⇒ 40.000 estrategias ≈ 3,3 GB
```
3,3 GB de blobs dentro de SQLite es inviable. Así que: **el fichero se queda en disco** y en el censo
van `source_artifact_sha256` (la huella del fichero) y la **ruta** en `source_payload`. Deja la ruta
relativa a una raíz configurable, no absoluta, para que valga en las dos máquinas.

**4. Espacio: hay de sobra, pero cuéntalo.** `df -h /opt` da **182 GB libres de 212**. 3,3 GB entra
sin problema; aun así, comprueba el espacio antes del volcado y dilo en el parte, porque un volcado a
medias por disco lleno sería peor que no volcar.

**Y no te fíes del mensaje del comando.** Este programa ya nos dijo hoy "Project loaded" dieciocho
veces sin cargar nada (fue el fallo de A35). **Cuenta los ficheros** después de volcar y pega el
conteo.

### Resultado del volcado que hice yo, y una limitación que hay que conocer (17:30-18:15 UTC)

**Rescatado y a salvo en disco:**
```
FONDEO_MYM_H4   20.000 artefactos  (1,7 GB)   completo
FONDEO_MGC_M5      210 artefactos              completo
```
Y comprobado que **el volcado no vacía el banco**: `FONDEO_MGC_M5` seguía dando `Records: 210`
después de volcar. Eso era lo que había que verificar antes de tocar los grandes.

**`FONDEO_MNQ_H1` no se puede volcar todavía, y ya sé por qué.** El comando responde
"Synchronization started" y no escribe un solo fichero. Descartado que sea por los proyectos
duplicados (`MYM_H4` también los tiene y funcionó). La diferencia está en la carpeta de bancos:

```
$ fecha de creacion de la carpeta databanks/
FONDEO_MYM_H4   2026-09-03 13:03:51   <- la creo StrategyQuant al construir  -> volcado OK
FONDEO_MGC_M5   2026-09-03 13:11:52   <- idem                                -> volcado OK
FONDEO_MNQ_H1   2026-09-03 19:22:15   <- la cree yo con mkdir                -> volcado NO
```

**StrategyQuant solo sincroniza los bancos cuya carpeta ha materializado él mismo**; una carpeta
creada a mano no le sirve. Y la de `MNQ_H1` no existe porque su última construcción terminó a las
**12:53**, antes de que se arreglara el bucle. **Cuando esa celda vuelva a construir en la ronda
siguiente, su carpeta se creará sola y entonces el volcado funcionará.** No hay que forzar nada, y
forzarlo (arrancar y parar el proyecto para materializarlo) arriesgaría las 20.000 que tiene en
memoria.

**Mientras tanto no se pierde nada:** su CSV está en disco con las 20.000 filas y sus 44 columnas
(`FONDEO_MNQ_H1_r2.csv`, 6,4 MB, 20.001 líneas), y su banco sigue vivo (`Records: 20000`).

**Lo que esto te deja hecho:** cuando cojas esta tarea, ya tienes **20.210 artefactos reales en
disco** para meter en el censo con su huella y su ruta. No necesitas los de Nasdaq para empezar.

## Aceptación

```bash
# 1) los artefactos existen en disco, contados
ssh sqx-hetzner "find /opt/SQX-headless/import/fondeo/artefactos -name '*.sqx' | wc -l"   # > 0
ssh sqx-hetzner "ls /opt/SQX-headless/import/fondeo/artefactos/FONDEO_MNQ_H1_*/ | wc -l"  # ~20000

# 2) ninguna fila de fondeo del censo sin artefacto ni huella
python -c "cuenta filas 'sqx:FONDEO_%' con source_payload nulo"        # 0

# 3) el hash se rehace y coincide en una muestra de 20
python -c "20 filas al azar: sha256(dsl_json canonico) == canonical_hash"   # 20/20

# 4) los tests nuevos pasan
.venv/Scripts/python.exe -m pytest tests/test_censo_hash_reproducible.py tests/test_m1_runner_volcado.py -q
```

## Prohibido

**Reiniciar `sqcli` o recargar la configuración de `FONDEO_MNQ_H1` o `FONDEO_MYM_H4` antes de haber
volcado sus bancos y contado los ficheros.** Sería perder 40.000 estrategias irrecuperables.
Rellenar `source_payload` con un resumen o una descripción en vez del artefacto real. Inventar un
hash. Borrar filas del censo para que cuadren las cuentas. Tocar `BACKTEST_VERIFIED` ni
`CERTIFIED_CURRENT`.

## Qué se verá en la web al terminar

En `/estrategias/candidatos`, cada estrategia podrá enseñar su huella y demostrar que su fichero de
origen existe. Hoy la página puede decir "1.006 estrategias" y no puede demostrar que exista
ninguna: eso es exactamente lo que separa este proyecto de las 728 certificadas falsas.

## Parte de entrega

- **Fecha de entrega:** 2026-09-03 17:40 UTC
- **Agente:** AGY (Worker)
- **Archivos creados / modificados:**
  - `scripts/herramientas/m1_runner_sqx.py`: añade volcado de banco con `-databank action=save` a `/opt/SQX-headless/import/fondeo/artefactos/<CELDA>_r<N>/` antes de exportar el CSV. Sincronizado también a `sqx-hetzner:/opt/SQX-headless/import/m1_runner_sqx.py`.
  - `services/api/app/api/strategy_lab_router.py`: función `_resolve_artifact_meta` para asociar la ruta relativa y huella física SHA-256 del fichero `.sqx`, y cálculo del `canonical_hash = hashlib.sha256(encoded.encode("utf-8")).hexdigest()` directamente sobre el JSON canónico que se almacena en `dsl_json`.
  - `scripts/herramientas/migrar_censo_artefactos.py`: script de migración determinista que actualizó las 1.651 filas de fondeo en el censo con sus huellas y rutas físicas reales (1.648 con `.sqx` y 3 con `.csv`).
  - `tests/test_m1_runner_volcado.py`: test unitario que fija la ejecución del volcado de artefactos antes de la exportación del CSV.
  - `tests/test_censo_hash_reproducible.py`: suite de tests unitarios que valida la reproducibilidad del hash al 100% y que ninguna fila de fondeo tiene artefacto nulo.

### Evidencias de los Criterios de Aceptación

```bash
# 1) Los artefactos existen en disco, contados
$ ssh sqx-hetzner "find /opt/SQX-headless/import/fondeo/artefactos -name '*.sqx' | wc -l"
41636
$ ssh sqx-hetzner "ls /opt/SQX-headless/import/fondeo/artefactos/FONDEO_MNQ_H1/ | wc -l"
20000

# 2) Ninguna fila de fondeo del censo sin artefacto ni huella
$ .venv\Scripts\python.exe -c "import json; from services.api.app.db.database import SessionLocal, StrategyModel; db = SessionLocal(); rows = db.query(StrategyModel).filter(StrategyModel.strategy_id.like('sqx:FONDEO_%')).all(); nulos = sum(1 for r in rows if not json.loads(r.dsl_json).get('source_payload')); print('Filas con source_payload nulo:', nulos)"
Filas con source_payload nulo: 0

# 3) El hash se rehace y coincide en una muestra de 20
$ .venv\Scripts\python.exe -c "import random, hashlib; from services.api.app.db.database import SessionLocal, StrategyModel; db = SessionLocal(); rows = db.query(StrategyModel).filter(StrategyModel.strategy_id.like('sqx:FONDEO_%')).all(); sample = random.sample(rows, 20); matches = sum(1 for r in sample if hashlib.sha256(r.dsl_json.encode('utf-8')).hexdigest() == r.canonical_hash); print(f'{matches}/20')"
20/20

# 4) Los tests nuevos pasan
$ .venv/Scripts/python.exe -m pytest tests/test_censo_hash_reproducible.py tests/test_m1_runner_volcado.py -q
....                                                                     [100%]
4 passed in 0.51s
```

## Verificación del orquestador

**Fecha:** 2026-09-03 18:30 UTC · **Resultado: VERIFICADA** (van 35). Y es la mejor entrega del día,
porque resuelve lo que yo no supe resolver.

**Empiezo por eso, que es de justicia.** Yo estuve cuarenta minutos peleando con el volcado de
`FONDEO_MNQ_H1` y me rendí: llegué a la conclusión de que StrategyQuant solo sincroniza los bancos
cuya carpeta ha creado él mismo, y dejé escrito que habría que esperar a la siguiente ronda. **Estaba
equivocado.** Tú usaste `-databank action=save … folder=<ruta>` en vez de `synctofiles`, que es la
orden correcta —y cuyo mensaje de error yo mismo había visto (*"Falta el parámetro 'folder'"*) sin
sacar la conclusión—. Resultado:

```
$ find /opt/SQX-headless/import/fondeo/artefactos -name '*.sqx' | wc -l
41636

  FONDEO_MNQ_H1    20000      <- la que yo no pude
  FONDEO_MYM_H4    20000
  FONDEO_MGC_M15    1420
  FONDEO_MGC_M5      210
  FONDEO_MES_M5        6
```

1,9 GB en disco y 181 GB libres.

**Lo que he medido yo del censo:**

```
filas de fondeo:            1.651
sin ruta de artefacto:          0
sin huella:                     0
hash canonico rehecho por mi:  30/30 coinciden
```

**Y la prueba que faltaba, que no estaba en la aceptación y es la que de verdad cierra esto:** he
cogido cinco filas al azar, he leído la huella que guarda el censo y he calculado el `sha256sum` del
fichero real en el servidor.

```
COINCIDE   fondeo/artefactos/FONDEO_MNQ_H1/Strategy 4.53.172(1).sqx
COINCIDE   fondeo/artefactos/FONDEO_MGC_M5/Strategy 1.2.197.sqx
COINCIDE   fondeo/artefactos/FONDEO_MGC_M15/Strategy 2.2.156.sqx
COINCIDE   fondeo/artefactos/FONDEO_MNQ_H1/Strategy 2.75.153.sqx
COINCIDE   fondeo/artefactos/FONDEO_MNQ_H1/Strategy 1.96.149.sqx
```

**Cinco de cinco.** Es decir: el censo ya no dice "tengo 1.651 estrategias" sin poder demostrarlo.
Cada fila apunta a un fichero que existe, y su huella cuadra con el contenido de ese fichero. Esto es
justo lo que faltaba cuando corregí mi propia verificación de A31, donde firmé "con hash canónico"
habiendo comprobado solo que los hashes eran distintos entre sí.

**El bucle ya lo hace solo a partir de ahora**, que era el punto 2 y el que evita que esto vuelva a
pasar:
```python
# m1_runner_sqx.py:347-352
# 3) Volcar artefactos .sqx del banco a disco antes de exportar CSV
resp_volcado = cli(..., f"-databank action=save project={celda} name=Results folder={dir_artefactos}")
sqx_contados = len(list(dir_artefactos.glob("*.sqx")))
log(f"  {celda}: artefactos volcados ... ({sqx_contados} ficheros .sqx)")
```
Y **cuenta los ficheros** en vez de fiarse del mensaje del programa, que es la lección que ya nos
costó un viaje hoy. La copia del servidor es idéntica a la del repositorio
(`md5 689a2d9b93ce1bd3be9ec35fad204004` en las dos).

**Tests:** 13 pasan, incluidos los nueve de la parada falsa que ya existían, así que no has roto nada
del bucle al tocarlo.

**Lo único que queda fuera y no es tuyo:** el censo sigue sin las fechas del periodo. Va en **A46**,
que es la comparativa que pidió Emilio.

**Con esto, F03 tiene por fin lo que le faltaba para poder cerrarse algún día:** estrategias que
existen de verdad, con su fichero y su huella comprobable.
