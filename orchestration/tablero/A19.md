---
id: A19
titulo: "/plan abre por el plan completo: es lo primero que se ve, antes que las fases"
agente: AGY
estado: VERIFICADO
fase: F09
prioridad: ALTA
maquina: pc
ambito: ["apps/web/app/plan/page.tsx", "apps/web/app/api/plan/doc/route.ts", "orchestration/tablero/A19.md"]
depende_de: []
estimado: "20 min"
creado: "2026-09-03 04:15 UTC"
actualizado: "2026-09-03 07:15 UTC"
---

## Por qué

Emilio lo ha pedido dos veces: *"tiene que haber un plan completísimo"* y *"deja clarísimo y
completo el plan completo en la página plan"*. El orquestador está escribiendo ese documento ahora
mismo (será la nueva versión de `orchestration/state/plan_maestro.md`, la v5) y, cuando exista, tiene
que ser **lo primero que se ve al entrar en `/plan`**: no una pestaña más entre siete, ni un enlace
en un pie.

Hoy `/plan` abre por la lista de fases, y el plan maestro solo se alcanza desde un enlace dentro de
la pestaña de fases o desde "Bitácora". Al revés de lo que hace falta: primero el plan entero (qué se
persigue, qué hay que hacer y cómo), y después las fases con su estado.

**El documento ya existe** (`orchestration/state/plan_maestro.md`, v5, 03-09 07:20 UTC): léelo antes de
pintarlo.

## Qué hacer

1. Nueva pestaña **"Plan completo"**, la **primera** de la barra y la que se abre por defecto al
   entrar en `/plan`. Renderiza `orchestration/state/plan_maestro.md` con el visor de Markdown que
   ya existe (`DocViewer`), a pantalla completa, sin el botón de cerrar (es la página, no un
   documento abierto encima).
2. Encima del documento, una franja de una sola línea, en gris, con la fecha de última modificación
   del fichero (la devuelve ya `/api/plan/doc`) y el texto *"Se actualiza solo con el fichero del
   repositorio"*.
3. La pestaña de fases pasa a segunda y se renombra a **"Fases y estado"**. Nada más cambia.
4. **El documento no se copia al código.** Se lee del fichero, siempre. Si el fichero no existe o no
   se puede leer, la pestaña lo dice en gris, sin inventar un plan.

## Aceptación

```bash
curl -s "http://127.0.0.1:3100/api/plan/doc?name=plan_maestro" | head -c 200
grep -n "Plan completo" apps/web/app/plan/page.tsx
cd apps/web && ../../node_modules/.bin/tsc --noEmit -p tsconfig.json    # rc=0
```

Y a ojo: al entrar en `http://localhost:3100/plan` lo primero que se ve es el plan completo,
renderizado y legible; la segunda pestaña son las fases.

## Prohibido

Copiar texto del plan dentro del código; tocar otras pestañas más allá del cambio de orden y nombre;
instalar dependencias.

## Parte de entrega
**Agente:** AGY · **Fecha:** 2026-09-03 07:15 UTC
**Resultado en una frase:** La página `/plan` abre ahora por defecto en la pestaña "Plan completo", renderizando el `plan_maestro.md` (v5) completo mediante `DocViewer` sin botón de cerrar, con franja superior de actualización automática y metadatos reales leídos de disco, pasando "Fases y estado" a segunda pestaña.

**Detalles de implementación:**
1. `apps/web/app/api/plan/doc/route.ts`: Actualizado título de `plan_maestro` a "Plan Maestro v5".
2. `apps/web/app/plan/page.tsx`:
   - Configurado `activeTab = "plan_completo"` como valor inicial por defecto.
   - Pestaña 1: `Plan completo` con icono `FileText`.
   - Pestaña 2: `Fases y estado ({bloques.length})` con icono `Layers`.
   - Añadida franja gris de una sola línea con `Última modificación:` y `Se actualiza solo con el fichero del repositorio`.
   - Renderizado dinámico de `plan_maestro.md` usando `DocViewer` a pantalla completa (sin prop `onClose`, por lo que no muestra botón 'X' de cierre modal).
   - En caso de error o ausencia física, se indica claramente el estado sin inventar contenido.
   - El documento **nunca está hardcodeado** en el código fuente; se consume exclusivamente vía `/api/plan/doc?name=plan_maestro` que lee el archivo en disco.

**Salidas Crudas de Aceptación:**
```bash
$ curl -s "http://127.0.0.1:3100/api/plan/doc?name=plan_maestro" | head -c 200
{"title":"Plan Maestro v5","filename":"plan_maestro.md","lastModified":"2026-09-03T06:56:27.167Z","sizeBytes":24061,"content":"# PLAN MAESTRO v5 — ULTRARENTABLE: qué tiene que hacer la aplicación y cómo,

$ grep -n "Plan completo" apps/web/app/plan/page.tsx
64:  // Plan completo (orchestration/state/plan_maestro.md)
203:          <span>Plan completo</span>

$ cd apps/web && npx tsc --noEmit -p tsconfig.json
(rc=0, compilación TypeScript limpia sin errores)
```

**Verificación en Navegador (http://localhost:3100/plan):**
- Al acceder a `/plan`, la pestaña activa por defecto es **"Plan completo"**.
- Muestra la franja gris: *"Última modificación: 2026-09-03 06:56:27 UTC"* | *"Se actualiza solo con el fichero del repositorio"*.
- El visor renderiza *"PLAN MAESTRO v5 — ULTRARENTABLE: qué tiene que hacer la aplicación y cómo, en principio, se consigue"*.
- DocViewer tiene botones de *"Copiar"* y *"Descargar"*, pero **ningún botón 'X' de cerrar**.
- La segunda pestaña es *"Fases y estado (11)"*.
- Captura de pantalla de evidencia: `plan_page_verification_1788418889026.png`.

**Lo que no pude hacer:** nada
**HALLAZGOS fuera de mi ámbito (no los toqué):** ninguno

## Verificación del orquestador

**VERIFICADA** · 2026-09-03 07:40 UTC. Re-ejecutada la aceptación:

```
$ curl -s "http://127.0.0.1:3100/api/plan/doc?name=plan_maestro"
Plan Maestro v5 | 23576 chars | lastModified 2026-09-03T06:56

$ grep -n "Plan completo|useState<TabId>(|TabId = " apps/web/app/plan/page.tsx
26: type TabId = "plan_completo" | "fases" | "agy" | ...      (la pestaña existe y va la primera)
39: const [activeTab, setActiveTab] = useState<TabId>("plan_completo");   (se abre por defecto)
203: <span>Plan completo</span>

$ grep -c "StrategyQuant nunca ha estado enchufado" page.tsx components/plan/*.tsx
(ninguna copia: el texto del plan NO está en el código, se lee del fichero)

$ grep -n "Fases y estado" page.tsx
215: <span>Fases y estado ({bloques.length})</span>

$ cd apps/web && tsc --noEmit -p tsconfig.json
rc=0
```

Cumple las cuatro condiciones que importaban: es la primera pestaña y la que se abre por defecto; se
renderiza con el visor existente y sin botón de cerrar; lleva la franja con la fecha real del fichero;
y **el documento no está copiado en el código**, se lee del repositorio, así que cuando el plan cambie
la página cambia sola. Buen detalle actualizar el título del mapa de documentos a "v5".

Con esto queda cumplida la petición de Emilio tal como la hizo: *"deja clarísimo y completo el plan
completo en la página plan"*. Está, es lo primero que se ve, y se actualiza sola.
