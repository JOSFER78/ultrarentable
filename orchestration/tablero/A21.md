---
id: A21
titulo: "La pestaña 3 de Mejora presenta como «mecanismos» un mejorador que viola la doctrina y va a cuarentena"
agente: AGY
estado: VERIFICADO
fase: F09
prioridad: ALTA
maquina: pc
ambito: ["apps/web/app/estrategias/mejora/page.tsx", "orchestration/tablero/A21.md"]
depende_de: ["A18"]
estimado: "15 min"
creado: "2026-09-03 07:30 UTC"
actualizado: "2026-09-03 07:25 UTC"
---

## Por qué

A18 quedó muy bien: fuera la cifra inventada, fuera el comité, y una lista completa de las ocho frases
en presente con su decisión. Al verificarla me quedé con un resto que tú mismo dejaste apuntado en el
punto 3 de tu lista y que merece su propia tarea, porque no es un matiz de redacción sino de fondo.

La pestaña **"3. Mecanismos de Dopaje Algorítmico"** describe ahora *"tratamientos cuantitativos de
reprogramación (`services/optimization/expert_refinement_loop.py`)"* como lo que el motor aplica a
una candidata que falla por poco. Dos problemas:

1. **Ese fichero es el mejorador antiguo que viola la doctrina del proyecto.** La auditoría del 03-09
   (`plan/bloques/F04_mejora_inteligente.md`, actualización) lo verificó línea a línea: usa el tramo
   ciego **dentro** del bucle de iteraciones (línea 242), que es justo lo que la fase de mejora
   prohíbe, y aplica multiplicadores fijos (×0,88, ×1,20) en vez de buscar el valor. Está decidido que
   va a **cuarentena**. Presentarlo en la web como "los mecanismos" de M2 es enseñar como bueno lo que
   se va a retirar: la misma clase de error que A18 acaba de corregir, un escalón más abajo.
2. **"Dopaje"** no es una palabra que Emilio deba encontrarse en su panel: sugiere trampa, y encima
   describe algo que hoy **no se ejecuta** en ningún flujo de producción (nadie llama a ese fichero
   fuera de los tests; verificado en la misma auditoría).

Lo que sí es cierto y sí se puede contar: existe el **diseño** de cómo se mejora una candidata que
falla por poco (hipótesis → experimento → nueva evaluación con el tramo ciego intocable,
`ARQUITECTURA_MODULAR_ESTRATEGIAS.md`), existe un **esqueleto limpio** que lo respeta
(`services/improvement/`), y **todavía no está conectado**.

## Qué hacer

Solo en `apps/web/app/estrategias/mejora/page.tsx`, pestaña 3:

1. Renombrarla a algo honesto y en llano, por ejemplo **"3. Qué se hará con las que fallan por
   poco"**. Sin "dopaje", sin "mecanismos" en presente.
2. El contenido pasa a describir el **diseño** (las tres ideas: hipótesis escrita, experimento solo en
   los tramos de entrenamiento y validación, el tramo ciego nunca se toca) y a decir claramente que
   **está especificado y no construido**. Cita `ARQUITECTURA_MODULAR_ESTRATEGIAS.md` como fuente.
3. Quita la referencia a `expert_refinement_loop.py` y cualquier lista de "tratamientos" con
   multiplicadores: no son el diseño, son el fichero que se retira.
4. Si la pestaña queda demasiado vacía para justificar su existencia, **retírala** y dilo en el parte:
   igual que en A18, mejor una pestaña menos que una que promete.

## Aceptación

```bash
grep -n -i "dopaje\|expert_refinement_loop" apps/web/app/estrategias/mejora/page.tsx   # esperado: sin resultados
grep -n -i "especificad\|no construid\|todavía no" apps/web/app/estrategias/mejora/page.tsx   # aparece la aclaración (o la pestaña se retiró)
cd apps/web && ../../node_modules/.bin/tsc --noEmit -p tsconfig.json                     # rc=0
```

En el parte, di qué opción elegiste (reescribir o retirar) y por qué.

## Prohibido

Tocar las pestañas 1 y 2, que son telemetría real; conectar nada; inventar un estado; tocar otros
ficheros.

## Parte de entrega
**Agente:** AGY · **Fecha:** 2026-09-03 07:25 UTC
**Resultado en una frase:** Pestaña 3 renombrada a *"3. Qué se hará con las que fallan por poco"*, eliminando cualquier rastro de "dopaje" y del fichero en cuarentena (`expert_refinement_loop.py`), y reescrita como ficha de diseño honesto con los 3 principios modulares y el aviso explícito de que está especificado pero no construido y no conectado.

**Opción elegida y por qué:**
Se eligió **reescribir la pestaña como diseño pendiente** (en lugar de retirarla). Razón: a diferencia del comité inventado de A18 (que simulaba agentes y bases de datos fantasma), M2 sí posee una arquitectura técnica formal ya decidida y aprobada en `docs/arquitectura/ARQUITECTURA_MODULAR_ESTRATEGIAS.md` y un esqueleto limpio en `services/improvement/`. Presentar los 3 principios invariantes (hipótesis previa, ajuste limitado a IS/Val, tramo ciego OOS intocable) con el badge gris `DISEÑO PENDIENTE · NO CONECTADO` instruye con total honestidad a Emilio sobre cómo funcionará la mejora sin simular una ejecución falsa.

**Cambios concretos:**
1. Tipo de pestaña actualizado a `diseno_mejora`.
2. Botón renombrado a: `3. Qué se hará con las que fallan por poco`.
3. Eliminadas todas las menciones a multiplicadores fijos arbitrarios (×0,88, ×1,20) y al script antiguo `expert_refinement_loop.py`.
4. Añadido banner con badge `DISEÑO PENDIENTE · NO CONECTADO` y texto: *"Este módulo está **especificado pero no construido** (fuente: `docs/arquitectura/ARQUITECTURA_MODULAR_ESTRATEGIAS.md` y el esqueleto limpio en `services/improvement/`). Todavía no está conectado en producción."*
5. Expuestas las 3 tarjetas del diseño modular real.

**Salidas Crudas de Aceptación:**
```bash
$ node -e "const fs = require('fs'); const content = fs.readFileSync('apps/web/app/estrategias/mejora/page.tsx', 'utf8'); console.log('dopaje/expert_refinement_loop matches:', (content.match(/dopaje|expert_refinement_loop/gi) || []).length); const lines = content.split('\n'); lines.forEach((l, i) => { if (/especificad|no construid|todav[ií]a no/i.test(l)) console.log((i+1) + ': ' + l.trim()); });"
dopaje/expert_refinement_loop matches: 0
376: Este módulo está <strong>especificado pero no construido</strong> (fuente: <code className="text-[var(--text-2)]">docs/arquitectura/ARQUITECTURA_MODULAR_ESTRATEGIAS.md</code> y el esqueleto limpio en <code className="text-[var(--text-2)]">services/improvement/</code>). Todavía no está conectado en producción.

$ cd apps/web && npx tsc --noEmit -p tsconfig.json
(rc=0, compilación TypeScript limpia sin errores)
```

**Verificación en Navegador (http://localhost:3100/estrategias/mejora):**
- Al hacer clic en `3. Qué se hará con las que fallan por poco`, se despliega la sección con badge `DISEÑO PENDIENTE · NO CONECTADO`.
- Se visualiza el descargo: *"Este módulo está especificado pero no construido... Todavía no está conectado en producción."*
- Las 3 tarjetas de principios arquitectónicos se muestran legibles y estilizadas.
- Evidencia gráfica guardada en: `tab3_verification_1788419039614.png`.

**Lo que no pude hacer:** nada
**HALLAZGOS fuera de mi ámbito (no los toqué):** ninguno

## Verificación del orquestador

**VERIFICADA** · 2026-09-03 07:50 UTC. Re-ejecutada la aceptación:

```
$ grep -n -i "dopaje|expert_refinement_loop" apps/web/app/estrategias/mejora/page.tsx
(0 resultados)

$ grep -n -i "especificad|no construid|todavía no" apps/web/app/estrategias/mejora/page.tsx
376: Este módulo está <strong>especificado pero no construido</strong> (fuente: docs/arquitectura/ARQUITECTURA_MODULAR_ESTRATEGIAS.md ...)

$ grep -n "0,88|0.88|1,20|×" apps/web/app/estrategias/mejora/page.tsx
(0 resultados: los multiplicadores fijos han desaparecido)

$ git diff HEAD -- apps/web/app/estrategias/mejora/page.tsx | grep "^-"
(todas las líneas quitadas pertenecen a la pestaña 3: el tipo "dopaje_reprogramacion", el título
"Mecanismos de Dopaje Algorítmico" y los cinco "tratamientos". Nada de las pestañas 1 y 2.)

$ cd apps/web && tsc --noEmit -p tsconfig.json
rc=0
```

Bien elegido **reescribir en vez de retirar**, y bien argumentado: aquí sí hay un diseño real y
aprobado, así que contarlo como diseño no es prometer. Las tres tarjetas dicen exactamente lo que
dice la arquitectura (hipótesis previa; ajuste solo en entrenamiento y validación; el tramo ciego no
se toca) y la etiqueta "DISEÑO PENDIENTE · NO CONECTADO" deja el estado sin ambigüedad.

Un matiz para la próxima, sin devolver nada: "A diferencia de scripts antiguos en cuarentena" es
verdad, pero al usuario no le aporta y lo obliga a preguntarse qué scripts. Cuando algo se retira,
mejor no mencionarlo en el panel: se cuenta lo que hay, no lo que hubo.
