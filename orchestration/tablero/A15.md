---
id: A15
titulo: "M1: lanzar la descarga de los datos que faltan, desacoplada y sin esperar a nada"
agente: AGY
estado: PENDIENTE
prioridad: ALTA
maquina: oracle
ambito: ["ssh oracle-vps: lanzar el backfill de Dukascopy en segundo plano", "orchestration/results/agy/A15_backfill.md", "orchestration/tablero/A15.md"]
depende_de: []
estimado: "10 min, sin esperas"
creado: "2026-09-03 03:35 UTC"
actualizado: "2026-09-03 03:35 UTC"
---

## Por qué

Emilio ha decidido **completar los datos antes de generar en serio** (decisión D3 de
`orchestration/state/ESPECIFICACION_WEB.md`). Y ya sabemos exactamente qué falta, porque lo mediste
tú en A11 y lo verifiqué yo:

| Activo | Estado real en disco |
| :--- | :--- |
| NQ (Nasdaq) | **Completo** en los cinco marcos temporales |
| YM (Dow) | **Completo** en los cinco |
| ES (S&P) | A medias: 2-3 trozos por marco, más los consolidados de 5m y 15m |
| GC (oro) | Un solo trozo por marco: prácticamente nada |
| CL (petróleo) | **Cero ficheros.** No hay nada |

La descarga es lenta (horas) y trabaja con el disco, no con el procesador, así que **no molesta a
nadie** si se lanza bien. Lo que no puede pasar es que te quedes tú esperando a que termine: se lanza
desacoplada, se comprueba que ha arrancado, y se entrega.

## Qué hacer

**1. Mira qué hay ya en marcha** (no vaya a haber una descarga corriendo y lances otra encima):

```bash
ssh oracle-vps 'ps -eo pid,etime,cmd | grep -i "[d]ukascopy" | cut -c1-140; echo "---"; tail -3 /home/ubuntu/logs/dukascopy*.log 2>/dev/null'
```

Si ya hay una corriendo, **no lances nada**: anota su PID y desde cuándo lleva, y entrega. Eso ya es
la respuesta.

**2. Si no hay ninguna, lánzala desacoplada**, con prioridad baja para no estorbar:

```bash
ssh oracle-vps 'cd "/home/ubuntu/workspace/pro/trading/01 Ultrarentable" && setsid nohup nice -n 19 ionice -c 3 .venv/bin/python -m services.data_ingestion.run_dukascopy_backfill --start 2023-01-01 --end 2026-08-30 --concurrency 3 > /home/ubuntu/logs/dukascopy_backfill_$(date -u +%Y%m%d_%H%M).log 2>&1 < /dev/null & echo "lanzado"; sleep 2; ps -eo pid,etime,cmd | grep -i "[d]ukascopy" | cut -c1-120'
```

Antes de lanzarlo, **comprueba que el módulo se llama así**:

```bash
ssh oracle-vps 'ls "/home/ubuntu/workspace/pro/trading/01 Ultrarentable/services/data_ingestion/" | head -20'
```

Si no existe `run_dukascopy_backfill`, **para y dilo**: no inventes el nombre del módulo ni pruebes
variantes a ciegas. Pega lo que sí existe y déjalo en `BLOQUEADO`.

**3. Comprueba que arrancó y entrega.** Un vistazo al log recién creado y a que el proceso existe. Y
ya está: **no esperes a que termine**. De vigilar el avance me encargo yo.

## Aceptación

```bash
ssh oracle-vps 'ps -eo pid,etime,cmd | grep -i "[d]ukascopy" | cut -c1-140'
ssh oracle-vps 'ls -la /home/ubuntu/logs/dukascopy_backfill_*.log | tail -2; tail -5 $(ls -t /home/ubuntu/logs/dukascopy_backfill_*.log | head -1)'
```

Esperado: un proceso vivo y un log con líneas de verdad (o, si ya había una descarga en marcha, la
prueba de que está corriendo desde antes). Pega las dos salidas.

## Prohibido

Esperar a que termine la descarga; lanzar dos descargas a la vez; subir la concurrencia por encima de
3 (el servidor lo comparten la API y la web); borrar ficheros de `data/`; inventar el nombre del
módulo o de sus parámetros.

## Parte de entrega

(lo rellena AGY)

## Verificación del orquestador

(lo rellena el orquestador)
