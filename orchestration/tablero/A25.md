---
id: A25
titulo: "M1 en la web: la rejilla de 40 celdas, leída del servidor, en lugar de la tabla escrita a mano"
agente: AGY
estado: VERIFICADO
fase: F03
prioridad: CRITICA
maquina: pc
ambito: ["apps/web/app/estrategias/generacion/page.tsx", "apps/web/lib/api.ts", "orchestration/tablero/A25.md"]
depende_de: []
estimado: "1 h"
creado: "2026-09-03 10:50 UTC"
actualizado: "2026-09-03 10:50 UTC"
---

## Por qué

La página `/estrategias/generacion` es la cara de M1, y hoy **enseña una tabla escrita a mano dentro
del código** (`page.tsx`, líneas 60-215): activos, temporalidades, comisiones y "cobertura histórica
2008-2026" puestos a dedo. Nada de eso se mide: el bloque de "Preconfiguración del Motor de Minería"
dice "18 años" de historia cuando lo que StrategyQuant tiene cargado son **3,7 años**. Y el recuadro
de estado dice `DESCONECTADO` aunque el servidor esté trabajando.

Mientras tanto, en el servidor hay 40 celdas de verdad, con datos de verdad, y una construcción
corriendo ahora mismo. Eso es lo que tiene que ver Emilio.

**Ya existe el endpoint y no hay que inventar nada:** `GET /api/v2/m1/rejilla`. Lo calcula el
supervisor del servidor cada minuto y la API lo sirve. Esta es su respuesta real, comprobada:

```json
{"disponible": true,
 "bucle": {"activo": true, "celda_en_curso": "FONDEO_MNQ_H1", "ronda": 1, "horas_por_celda": 1},
 "resumen": {"celdas": 25, "con_datos": 25, "con_proyecto": 25,
             "con_al_menos_una_ronda": 0, "estrategias_en_bancos": 17508},
 "celdas": [{"celda": "MNQ_H1", "simbolo": "MNQ", "contrato_grande": "NQ",
             "nombre": "Micro E-mini Nasdaq 100", "tf": "H1", "tf_etiqueta": "1h",
             "proyecto": "FONDEO_MNQ_H1", "proyecto_en_sqx": true,
             "datos": {"desde": "2023.01.02", "hasta": "2026.08.30", "dias": 1337,
                       "velas_base": 1248322, "tf_base": "M1"},
             "estado": "EN_CURSO", "rondas_hechas": 0, "generadas": 92214, "en_banco": 17508,
             "por_hora": "246356", "aceptado_pct": "16.21", "tiempo": "22 min. 15 s.",
             "csv_filas": null, "csv_sha256": null,
             "costes": {"spread": 1, "slippage": 2, "comision": 2.0}}]}
```

Hoy vienen 25 celdas (5 activos x 5 temporalidades). Emilio quiere **40 (8 activos x 5)**: faltan
Russell 2000, el bono a 30 años y el euro, cuyos datos estoy cargando yo en el servidor. **La página
no debe dar por hecho ni 25 ni 40: pinta las que vengan** y dice cuántas hay.

## Qué hacer

Solo en `apps/web/app/estrategias/generacion/page.tsx` (y el cliente en `lib/api.ts`):

1. **Fuera la constante escrita a mano** con los activos y sus datos. Todo sale de `/api/v2/m1/rejilla`.
2. **Arriba, cuatro cifras que se leen de un vistazo**, todas del `resumen` y del `bucle`:
   celdas con datos, celdas con proyecto creado, estrategias acumuladas en los bancos, y qué celda se
   está construyendo ahora mismo (con su caudal por hora y el tiempo que lleva).
3. **La rejilla**, que es lo importante: una fila por activo y una columna por temporalidad
   (1m, 5m, 15m, 1h, 4h), y en cada cruce el estado de esa celda (`SIN_EMPEZAR`, `PENDIENTE`,
   `EN_CURSO`, `HECHA`) y cuántas estrategias lleva. La que esté `EN_CURSO`, marcada. En grises;
   verde o rojo solo para bien/mal.
4. **Debajo, el detalle por activo**: nombre llano, contrato grande del que es micro, desde/hasta y
   número de velas de 1 minuto que hay cargadas, y los costes (diferencial, deslizamiento, comisión).
   Los costes vienen marcados como supuestos en el manifiesto: dilo con esa palabra, "supuesto".
5. **Si `disponible` es `false`**, se enseña `motivo_no_disponible` en gris y nada más. Ni tabla
   fantasma ni ceros que parezcan datos.
6. Refresco automático cada 30 s, como ya hace `/plan`.

## Aceptación

```bash
curl -s http://127.0.0.1:8100/api/v2/m1/rejilla | head -c 200
grep -c "USA500IDXUSD\|18 años\|2008 – 2026\|RTY\|ZB (UB)\|6E (M6E)" apps/web/app/estrategias/generacion/page.tsx   # 0
grep -c "api/v2/m1/rejilla" apps/web/lib/api.ts                                                                        # >= 1
cd apps/web && ../../node_modules/.bin/tsc --noEmit -p tsconfig.json                                                   # rc=0
```

Y a ojo en `http://localhost:3100/estrategias/generacion`: se ve la rejilla con las celdas reales, la
que está corriendo marcada, y ningún activo que no venga del servidor.

## Prohibido

Escribir en el código ningún activo, fecha, número de velas o comisión: **todo del endpoint**.
Barras o pestañas arriba (un solo menú, el lateral). Colores fuera de la escala de grises salvo
verde/rojo semánticos. Tocar otras páginas.

## Parte de entrega

- **Cero datos inventados**:
  - Se eliminan por completo las constantes fijas con tablas mockeadas (`ACTIVOS_FONDEO_CME` y `ACTIVOS_ULTRA_CRIPTO`) en `apps/web/app/estrategias/generacion/page.tsx`.
  - La totalidad de los activos, temporalidades, coberturas, velas base y estados se consumen en vivo desde `/api/v2/m1/rejilla` vía la función `getM1Rejilla()` de `@/lib/api`.
- **4 Cifras Clave en cabecera**:
  - Celdas con datos: 30 / 40 (indicando que 10 están sin datos todavía).
  - Proyectos creados: 30 / 40.
  - Estrategias acumuladas en bancos: 20.000.
  - Celda en construcción ahora mismo: `FONDEO_MES_M5`, con caudal medido y tiempo transcurrido en vivo.
- **Rejilla dinámica (Matriz Activo x Temporalidad)**:
  - Fila por cada activo real presente en el manifiesto (`MES`, `MNQ`, `MYM`, `M2K`, `MGC`, `MCL`, `UB`, `M6E`).
  - Columnas por temporalidad (`1m`, `5m`, `15m`, `1h`, `4h`).
  - Cada celda refleja su estado (`EN_CURSO`, `PENDIENTE`, `HECHA`) y recuento de estrategias en banco. Las celdas sin datos cargados (`M2K`, `UB`) se marcan explícitamente como "sin datos todavía", sin ocultarse ni inventar nada.
  - La celda activa (`MES_M5`) se resalta visualmente con badge `EN CURSO`.
- **Detalle por activo con palabra "supuesto"**:
  - Se listan los rangos de fechas medidos (`2023.01.02 → 2026.08.30`), días, velas base de 1 minuto y los costes de fricción explícitamente catalogados como "Fricción supuesta" en el manifiesto.
- **Gestión de no disponibilidad y refresco**:
  - Si `disponible: false`, se muestra `motivo_no_disponible` en gris sobrio sin tablas fantasma.
  - Refresco automático cada 30 segundos mediante `setInterval`.
- **Aceptación**:
  ```
  $ curl -s http://127.0.0.1:8100/api/v2/m1/rejilla | head -c 200
  {"schema":"ultrarentable.m1.rejilla.v1","medido":"2026-09-03T09:18:47.734577+00:00","disponible":true,"bucle":{"activo":true,"celda_en_curso":"FONDEO_MES_M5","ronda":1,"horas_por_celda":1},"resumen":{

  $ git grep -c "USA500IDXUSD\|18 años\|2008 – 2026\|RTY\|ZB (UB)\|6E (M6E)" apps/web/app/estrategias/generacion/page.tsx
  (0 coincidencias)

  $ git grep -c "api/v2/m1/rejilla" apps/web/lib/api.ts
  apps/web/lib/api.ts:1

  $ cd apps/web && ../../node_modules/.bin/tsc.cmd --noEmit -p tsconfig.json
  (exit code 0)
  ```

## Verificación del orquestador

**VERIFICADA** · 2026-09-03 12:35 UTC. Y es la tarea que más falta hacía: es la pantalla que Emilio
abre y la que hacía días que le mentía.

Re-ejecutada la aceptación, con dos comprobaciones más que añadí yo:

```
$ curl -o /dev/null -w "%{http_code}" http://127.0.0.1:8100/api/v2/m1/rejilla        -> 200
$ grep -c "USA500IDXUSD|18 años|2008 – 2026|SHA-256 OK" generacion/page.tsx          -> 0
$ grep -c "api/v2/m1/rejilla" apps/web/lib/api.ts                                    -> 1
$ grep -c "rejilla" apps/web/app/estrategias/generacion/page.tsx                     -> 39
$ tsc --noEmit -p tsconfig.json                                                      -> rc=0

  (añadidas por mí)
$ grep -cE "(text|bg|border|ring|shadow)-(rose|amber|blue|purple|cyan|...)-[0-9]"    -> 0
$ grep -c "Anterior|Siguiente"  (barra de navegación arriba, prohibida)              -> 0
$ peticiones reales a /api/v2/m1/rejilla en el log de la API                         -> 14, todas 200
```

**Ha desaparecido lo que había que quitar.** Las dos constantes con la tabla a mano ya no existen, y
con ellas el "2008 – 2026 (18 años)", el "SHA-256 OK" de activos que ni siquiera tienen datos, y las
temporalidades distintas por activo. Todo eso era falso y llevaba días en pantalla.

**Y has acertado en las tres decisiones delicadas:**

1. Las 10 celdas sin datos (Russell y bono) se marcan "sin datos todavía" en vez de ocultarse o
   rellenarse. Es lo honesto y es lo que pedía la tarea.
2. Si `disponible` viene en `false`, sale el motivo en gris y nada más. Sin tabla fantasma.
3. Los costes van con la palabra **supuesto**, que es lo que son hasta que la lista de operaciones
   los confirme.

Y esta vez el color está limpio a la primera. La comprobación que te puse funciona.

**Lo único que no he podido comprobar yo**, y lo digo para que conste: la página pide los datos desde
el navegador, así que al descargar el HTML desde la consola llega la plantilla vacía. He verificado
que el endpoint devuelve los datos correctos, que el código los consume campo a campo, y que hay 14
peticiones reales con respuesta 200 en el registro de la API. La comprobación a ojo la hace Emilio al
abrir la página.

**Contexto que quizá te interesa:** mientras hacías esto, la primera celda cerrada (Nasdaq en 1 hora)
dejó 20.000 estrategias en su banco, y de esas **una** cumpliría el criterio sellado del proyecto
(factor de beneficio fuera de muestra por encima de 1,25 con más de 200 operaciones). Una de veinte
mil, en la primera celda de cuarenta. Eso es exactamente el caudal que M1 tenía que empezar a
demostrar, y ahora se ve en tu pantalla.
