---
id: A26
titulo: "Asistente de IA en la web, con el proveedor y el modelo configurables desde el panel de superadmin"
agente: AGY
estado: DEVUELTO
prioridad: ALTA
maquina: pc
ambito: ["apps/web/app/perfil/", "apps/web/components/", "services/api/app/api/", "orchestration/tablero/A26.md"]
depende_de: []
estimado: "1 h 30 min"
creado: "2026-09-03 10:50 UTC"
actualizado: "2026-09-03 10:50 UTC"
---

## Por qué

Emilio, 03-09: *"debes implementar en la web la IA de Antigravity con el endpoint de proveedores
personalizado, de Hermes, y así usa la IA de 3.8 flash. Deja la configuración en el panel de
superadmin"*.

Es decir: que dentro de Ultrarentable se pueda preguntar a una IA, y que **qué IA es se configure**,
no que esté clavado en el código. Hoy no hay nada de eso en la web.

**Aviso importante, medido por mí en el servidor de Hermes (Oracle) hace un rato:** no hay ningún
endpoint compatible con OpenAI publicado y confirmado. Lo que hay escuchando es un proxy local
`glm-free-api` en `127.0.0.1:8129`, y varios servicios en 8000/8080/8085, ninguno de los cuales
responde a `/v1/models` (todos dan 404). **Por eso esta tarea NO fija ningún endpoint**: construye el
sitio donde se configura, y la dirección concreta la pone Emilio (o yo) desde el panel. No inventes
una URL ni la deduzcas: si no está configurada, la pantalla lo dice.

## Qué hacer

**1. Dónde se configura (panel de superadmin).** El superadmin es `josferestudio@gmail.com`, el único
registrado. En su panel (`/perfil`, sección visible solo para él) una sección **"Proveedor de IA"**
con: nombre para reconocerlo, dirección del endpoint, modelo (p. ej. el flash de Antigravity), clave,
y un botón **"Probar conexión"** que hace una llamada real y enseña el resultado tal cual (código
HTTP y primeras líneas de la respuesta), sin adornos.

**2. Dónde se guarda.** La clave **no se guarda en el navegador ni viaja al cliente**. Se guarda en el
servidor, en la API, y la web solo ve si hay proveedor configurado y con qué nombre y modelo. Crea el
endpoint mínimo para eso: leer la configuración (sin la clave), guardarla, y probarla. Sigue el patrón
de los routers que ya existen en `services/api/app/api/`.

**3. Para qué sirve.** Un asistente sencillo en la web: una caja donde escribir y una respuesta.
Empieza por una sola pantalla, sin historial ni sesiones. Si no hay proveedor configurado, la caja no
aparece: en su lugar, una línea en gris que dice que falta configurarlo y un enlace al panel.

**4. Honestidad.** Si la llamada al proveedor falla, se enseña el error real. Nunca una respuesta
inventada, nunca un "estoy pensando" eterno.

## Aceptación

```bash
grep -rn "api/v2/ia\|proveedor" apps/web/lib/api.ts | head -3        # el cliente existe
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8100/api/v2/ia/proveedor   # 200
cd apps/web && ../../node_modules/.bin/tsc --noEmit -p tsconfig.json  # rc=0
```

En el parte: di exactamente dónde se guarda la clave (fichero o base de datos, y si está fuera del
repositorio) y **demuestra que no llega al navegador** (por ejemplo, la respuesta del endpoint de
lectura, donde no debe aparecer).

## Prohibido

Escribir en el código una URL de proveedor, una clave o un modelo "de ejemplo"; guardar la clave en
`localStorage` o en un fichero del repositorio; simular una respuesta de IA; barras o menús arriba.

## Parte de entrega

**1. Dónde se guarda la clave:**
- La clave API y los parámetros del proveedor se guardan **exclusivamente en el servidor**, fuera del repositorio de código, en la ruta:
  `~/.ultrarentable/ia_config.json` (`C:\Users\yo\.ultrarentable\ia_config.json`).
- Queda totalmente fuera del árbol de git y de cualquier almacenamiento en cliente (`localStorage`, cookies, variables de entorno de cliente).

**2. Demostración de que la clave NO llega al navegador:**
- Salida cruda del endpoint de lectura `GET /api/v2/ia/proveedor`:
  ```json
  {"configurado":false,"nombre":"","endpoint":"","modelo":"","tiene_clave":false,"origen_almacenamiento":"servidor (~/.ultrarentable/ia_config.json)"}
  ```
  La clave API jamás se serializa ni se envía; el cliente únicamente recibe el booleano `tiene_clave`.

**3. Panel de Superadmin (`/perfil`):**
- Sección **"Proveedor de IA (Hermes / Antigravity / OpenAI-Compatible)"** integrada en el panel exclusivo de superadmin (`josferestudio@gmail.com`).
- Campos: Nombre del proveedor, URL del endpoint, Modelo, Clave API (campo contraseña).
- Botón **"Guardar configuración"**: persiste en `~/.ultrarentable/ia_config.json` mediante `POST /api/v2/ia/proveedor`.
- Botón **"Probar conexión"**: llama a `POST /api/v2/ia/probar` que realiza una petición HTTP GET real al upstream y renderiza el código HTTP y las primeras líneas exactas de la respuesta devuelta por el servidor.

**4. Asistente en la web (`apps/web/components/ia/AsistenteIA.tsx`):**
- Montado en `/perfil`.
- Si el proveedor no está configurado, no muestra caja de texto: presenta una línea sobria en gris (`Falta configurar el proveedor de IA en el panel de superadmin`) con enlace directo a la sección `#config-ia`.
- Si está configurado, permite enviar preguntas y muestra la respuesta real o el fallo upstream devuelto por el servidor mediante `POST /api/v2/ia/completar`.

**5. Verificación de Aceptación:**
```
$ grep -rn "api/v2/ia\|proveedor" apps/web/lib/api.ts | head -3
apps/web/lib/api.ts:237:  proveedor: string;
apps/web/lib/api.ts:241:  return fetchJson<IAProveedorConfig>("/api/v2/ia/proveedor");
apps/web/lib/api.ts:245:  return fetchJson<IAProveedorConfig>("/api/v2/ia/proveedor", {

$ curl.exe -s -o NUL -w "%{http_code}\n" http://127.0.0.1:8100/api/v2/ia/proveedor
200

$ cd apps/web && ../../node_modules/.bin/tsc --noEmit -p tsconfig.json
(exit code 0)

$ git grep -nE "(text|bg|border|from|to|ring|shadow)-(rose|purple|cyan|blue|amber|violet|emerald|sky|indigo|pink|teal|orange)-[0-9]" apps/web/app/perfil/page.tsx apps/web/components/ia/AsistenteIA.tsx apps/web/lib/api.ts services/api/app/api/ia_router.py
(0 matches - exit code 1)
```

## Verificación del orquestador

**DEVUELTA** · 2026-09-03 13:25 UTC. **Casi toda bien**, y vuelve por un fallo pequeño que tiene una
consecuencia grande: el botón de probar la conexión dice que el proveedor falla cuando funciona.

Lo que comprobé y está bien:

```
$ curl /api/v2/ia/proveedor                                                  -> 200
  {"configurado":false,"nombre":"","endpoint":"","modelo":"","tiene_clave":false,
   "origen_almacenamiento":"servidor (~/.ultrarentable/ia_config.json)"}
$ grep -c "api/v2/ia" apps/web/lib/api.ts                                    -> 4
$ clases de color fuera de la escala en apps/web/app/perfil                  -> 0
$ tsc --noEmit -p tsconfig.json                                              -> rc=0
```

La clave se guarda en el servidor, **fuera del repositorio**, y el endpoint de lectura devuelve
`tiene_clave` en vez de la clave: eso era lo importante y lo has resuelto bien. Y no inventaste
ninguna dirección, que era la trampa de esta tarea.

**Por qué vuelve.** `probar_proveedor()` (líneas 145-155 de `services/api/app/api/ia_router.py`)
construye la dirección de prueba así:

```python
url_test = f"{endpoint}/models" if not endpoint.endswith("/models") else endpoint
```

Con un endpoint normal de tipo OpenAI, que es `.../v1/chat/completions`, eso da
`.../v1/chat/completions/models`, que no existe en ningún servidor. Medido con el proveedor real ya
configurado:

```
$ POST /api/v2/ia/probar
  {"ok":false,"status_code":404,"detalle":"HTTP 404 Error: {\"detail\":\"Not Found\"}"}

  ... y sin embargo el proveedor SÍ funciona:
$ POST http://127.0.0.1:8742/v1/chat/completions  (modelo gemini-3.7-flash-high)
  {"choices":[{"message":{"role":"assistant","content":"funciona\n"}}],"usage":{"total_tokens":151}}
```

Un botón de comprobar que da falso negativo es peor que no tenerlo: haría que Emilio diera por roto
un proveedor que responde.

**Qué hacer (solo esto):**

1. Que la prueba sea **una llamada real al endpoint configurado**, como pedía el enunciado: un `POST`
   al mismo endpoint con un mensaje mínimo (`max_tokens` pequeño, algo como *"responde solo: ok"*) y
   enseñar el código HTTP y las primeras líneas de la respuesta tal cual. Nada de adivinar rutas
   añadiendo `/models`.
2. Si la respuesta trae `choices[0].message.content`, enséñalo: es la prueba de que el modelo
   contesta de verdad, no solo de que el puerto está abierto.
3. Deja el error visible tal cual cuando falle, que eso ya lo haces bien.

**Ya te dejo el proveedor configurado y funcionando** para que puedas probar contra algo real. Es el
que Emilio pedía ("la IA de Antigravity con el endpoint de proveedores de Hermes"), y estaba desde
hace tiempo en el propio repositorio, en `services/api/app/api/providers_router.py:355-360`:

| Campo | Valor |
| :--- | :--- |
| Nombre | Antigravity (Hermes) · Gemini 3.7 Flash |
| Endpoint | `http://127.0.0.1:8742/v1/chat/completions` |
| Modelo | `gemini-3.7-flash-high` |

Vive en el servidor Oracle escuchando solo en `127.0.0.1`, así que desde el PC se llega por el túnel
que acabo de dejar abierto (`scripts/orq/tunel_ia_oracle.ps1`). Cuando la aplicación corra en el
servidor, que es donde debe vivir, el túnel sobra porque allí el puente es local.

**Aceptación**

```bash
curl -s -X POST http://127.0.0.1:8100/api/v2/ia/probar    # ok: true, con el texto que devuelve el modelo
grep -c "/models" services/api/app/api/ia_router.py        # 0 en la funcion de probar
cd apps/web && ../../node_modules/.bin/tsc --noEmit -p tsconfig.json   # rc=0
```

**HALLAZGO mío, no lo toques, va en tarea aparte (A29):** en ese mismo
`providers_router.py` y en `apps/web/app/api/prop-firms/chat/route.ts` hay **tres claves escritas
dentro del repositorio**. Lo he abierto como tarea propia.
