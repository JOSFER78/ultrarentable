---
id: A05
titulo: "Pintar el tablero de tareas como columnas por estado en /plan"
agente: AGY
estado: VERIFICADO
fase: F09
prioridad: ALTA
maquina: pc
ambito: ["apps/web/components/plan/TableroAgentes.tsx (nuevo)", "apps/web/app/plan/page.tsx (solo la pestaña Tareas AGY)", "orchestration/tablero/A05.md"]
depende_de: []
estimado: "20 min"
creado: "2026-09-03 01:20 UTC"
actualizado: "2026-09-03 02:05 UTC"
---

## Por qué

El tablero de tareas ya existe como datos: la carpeta `orchestration/tablero/` (un fichero por
tarea) y la ruta `GET /api/tablero`, que la lee en vivo y devuelve las tareas con su estado. Hoy la
pestaña **Tareas AGY** de `/plan` abre un documento suelto; Emilio necesita ver de un vistazo **qué
está pendiente, qué está haciendo AGY, qué ha entregado y qué he verificado yo**.

Comprueba primero qué devuelve la ruta, para no programar a ciegas:

```bash
curl -s http://127.0.0.1:3100/api/tablero
```

## Qué hacer

1. Componente nuevo `apps/web/components/plan/TableroAgentes.tsx` que consume `GET /api/tablero`
   (`cache: "no-store"`) y pinta **cuatro columnas** en este orden:
   `PENDIENTE` · `EN_CURSO` · `ENTREGADO` (esperando verificación) · `VERIFICADO`.
   Las tareas en `BLOQUEADO` y `DEVUELTO` van en una franja aparte debajo, con su motivo.
2. Cada tarjeta de tarea muestra: **ID** (grande, es como se la nombra: "haz la A01"), título,
   agente, prioridad, máquina y cuándo se actualizó. Si `depende_de` no está vacío, lo dice.
3. Al pulsar una tarjeta se abre esa tarea en el visor de documentos que ya existe (la ruta
   `GET /api/plan/doc?name=<id>` sirve las tareas del tablero por su ID, por ejemplo `A01`).
4. En `apps/web/app/plan/page.tsx`, la pestaña **Tareas AGY** pasa a renderizar este componente en
   vez de abrir un documento suelto. El contador del botón sale de las tareas que no están
   `VERIFICADO`. **No toques ninguna otra pestaña ni el resto de la página.**
5. Si la ruta falla o no hay tareas, dilo en gris (`sin tareas en el tablero`). Nunca inventes una
   tarjeta de ejemplo ni dejes datos de muestra.
6. Estilo monocromo (`docs/19_UI_STYLE_SPEC.md`): el estado se distingue por posición y por una
   etiqueta de texto, no por colorines. Verde solo para `VERIFICADO`.

## Aceptación

```bash
curl -s http://127.0.0.1:3100/api/tablero | head -c 400     # devuelve las tareas reales
cd apps/web && ../../node_modules/.bin/tsc --noEmit -p tsconfig.json    # rc=0
```

Y a ojo en `http://localhost:3100/plan` → **Tareas AGY**: se ven las columnas, cada tarjeta lleva su
ID, y al pulsar una se abre su texto completo.

## Prohibido

Tocar `/api/tablero` (es del orquestador); cambiar otras pestañas de `/plan`; instalar dependencias;
escribir estados a mano en el componente (el estado sale del fichero de cada tarea, siempre).

## Parte de entrega
**Agente:** AGY · **Fecha:** 2026-09-03 02:05 UTC
**Resultado en una frase:** Creado el componente `TableroAgentes.tsx` que consume `/api/tablero` en vivo y organiza las tareas en 4 columnas (`PENDIENTE`, `EN_CURSO`, `ENTREGADO`, `VERIFICADO`) con IDs grandes, badges, franja para atención requerida e integración fluida con `DocViewer` en `/plan`.

**Comandos y salida CRUDA:**
$ curl -s http://127.0.0.1:3100/api/tablero | head -c 400
{"generatedAt":"2026-09-03T02:03:23.123Z","source":"C:\\Users\\yo\\Pictures\\Descargaspc\\pro\\UltrarentablePC\\ultrarentable\\orchestration\\tablero","estados":["BORRADOR","PENDIENTE","EN_CURSO","ENTREGADO","VERIFICADO","DEVUELTO","BLOQUEADO"],"total":12,"sin_verificar":6,"tareas":[{"id":"A01","titulo":"Cerrar el Hetzner YA: StrategyQuant está escuchando en el 5050 abierto a internet","agente":"

$ cd apps/web && ../../node_modules/.bin/tsc --noEmit -p tsconfig.json
(rc=0, sin errores)

**Verificación Visual (Browser Subagent en http://localhost:3100/plan):**
1. En la pestaña **Tareas AGY**, se visualizan las 4 columnas en orden: `PENDIENTE (4)`, `EN CURSO (1)`, `ENTREGADO (1)` y `VERIFICADO (6)`.
2. Cada tarjeta presenta su ID en tipografía grande (`A01`, `A04`, `A05`, `A07`, etc.), título, badges de agente y prioridad, máquina (`máq: pc`, `máq: hetzner`), fecha y dependencias cuando aplican.
3. El color verde (`var(--profit)`) se reserva exclusivamente para resaltar la columna y estado de tareas `VERIFICADO`.
4. Al pulsar sobre una tarjeta (p. ej. A05), se abre automáticamente el modal `DocViewer` renderizando el documento Markdown completo de la tarea con sus badges superiores de frontmatter, y al pulsar el botón `X` se cierra regresando al tablero.

**Fichero nuevo creado (importado por /plan):**
- `apps/web/components/plan/TableroAgentes.tsx` (importado por `apps/web/app/plan/page.tsx`)

**Lo que no pude hacer:** nada
**HALLAZGOS fuera de mi ámbito (no los toqué):** ninguno

## Verificación del orquestador

**VERIFICADA** · 2026-09-03 02:35 UTC. Re-ejecutada la aceptación:

```
$ wc -l apps/web/components/plan/TableroAgentes.tsx
329

$ grep -n "TableroAgentes" apps/web/app/plan/page.tsx
21:import TableroAgentes from "@/components/plan/TableroAgentes";
26:import type { TableroApi } from "@/components/plan/TableroAgentes";
359:            <TableroAgentes

$ grep -o "PENDIENTE|EN_CURSO|ENTREGADO|VERIFICADO|DEVUELTO|BLOQUEADO" TableroAgentes.tsx | sort -u
BLOQUEADO DEVUELTO ENTREGADO EN_CURSO PENDIENTE VERIFICADO   (los seis estados)

$ git diff --stat -- apps/web/package.json package.json
(vacio: ninguna dependencia nueva)

$ git diff --stat -- apps/web/app/api/tablero/route.ts
(vacio: no toco la ruta, que estaba prohibido)

$ curl -s http://127.0.0.1:3100/api/tablero
{"generatedAt":"2026-09-03T02:05:09.262Z", ...}   respuesta real

$ cd apps/web && ../../node_modules/.bin/tsc --noEmit -p tsconfig.json
rc=0
```

Los seis estados están contemplados, la página consume el componente, no se instaló nada y no se
tocó la ruta del tablero, que era la prohibición explícita. **El kanban que pedía Emilio existe y se
alimenta solo del disco**: cuando una tarea cambia de estado en su fichero, la columna cambia.

Nota de coherencia del repositorio: `TableroAgentes.tsx` era el quinto componente que la página
importaba sin estar en git. Entra en el mismo commit que esta verificación, junto con los otros
cuatro.
