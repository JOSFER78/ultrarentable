---
id: A01
titulo: "Cerrar el Hetzner YA: StrategyQuant está escuchando en el 5050 abierto a internet"
agente: ORQ
estado: VERIFICADO
fase: F10
prioridad: URGENTE
maquina: hetzner
ambito: ["ssh sqx-hetzner: ufw y /etc/nginx/", "orchestration/tablero/A01.md"]
depende_de: []
estimado: "10 min"
creado: "2026-09-03 01:05 UTC"
actualizado: "2026-09-03 01:30 UTC"
---

## Por qué

**Ha empeorado.** Medido por el orquestador el 2026-09-03 a las 01:33 UTC, con StrategyQuant X ya
instalado y corriendo en el servidor:

```
$ ss -ltnp | grep 5050
LISTEN 0 50  0.0.0.0:5050  0.0.0.0:*  users:(("StrategyQuantX",pid=29693,fd=100))
$ ufw status
Status: inactive
$ grep -c auth_basic /etc/nginx/sites-available/default
0
```

`0.0.0.0:5050` significa **abierto a internet**, y la API de StrategyQuant **no pide ninguna
credencial**: quien alcance ese puerto controla la minería, los proyectos y la exportación de
ficheros de la máquina. Con la licencia de Emilio dentro. Sigue además todo lo de antes:

- Sin cortafuegos (`ufw inactive`, `iptables -S INPUT` con política `ACCEPT`).
- Escritorio remoto público **sin contraseña**: `curl https://88-99-210-167.sslip.io/novnc/vnc.html`
  devuelve **200** y la URL trae `autoconnect=true`. Cualquiera con el enlace entra como root.
- `x11vnc` con `-nopw` escuchando en `[::]:5900`, y la máquina tiene IPv6 pública
  `2a01:4f8:10a:3b9a::2/64`.
- `websockify` en `0.0.0.0:6080`, saltándose nginx.

Esta tarea es lo primero de todo. No hace falta parar StrategyQuant: el cortafuegos deja el 5050
accesible desde la propia máquina, que es lo único que necesita.

## Qué hacer

`ssh sqx-hetzner`. **En este orden.** El bloque 1 abre el 22 antes de activar nada, así que no
puede dejar a nadie fuera.

**1. Cortafuegos (esto es lo urgente)**

```bash
ufw allow 22/tcp && ufw allow 80/tcp && ufw allow 443/tcp
ufw --force enable
ufw status verbose
```

Deshacer: `ufw disable`.

**2. Contraseña en el escritorio remoto**

La contraseña **la elige Emilio**: pídesela, no la inventes. Si no puedes pedírsela, haz solo el
bloque 1, deja la tarea en `ENTREGADO` y dilo en el parte.

```bash
apt-get update -qq && apt-get install -y apache2-utils
htpasswd -c /etc/nginx/.htpasswd emilio     # la pide dos veces
cp /etc/nginx/sites-available/default /root/nginx-default.bak
sed -i 's|^\(    location /novnc/ {\)|\1\n        auth_basic "Ultrarentable";\n        auth_basic_user_file /etc/nginx/.htpasswd;|' /etc/nginx/sites-available/default
sed -i 's|^\(    location = /novnc/websockify {\)|\1\n        auth_basic "Ultrarentable";\n        auth_basic_user_file /etc/nginx/.htpasswd;|' /etc/nginx/sites-available/default
nginx -t && systemctl reload nginx
```

Deshacer: `cp /root/nginx-default.bak /etc/nginx/sites-available/default && systemctl reload nginx`.

## Aceptación

```bash
ufw status verbose | head -12                          # esperado: Status: active con 22/80/443
ss -ltn | grep 5050                                    # sigue escuchando: correcto, ya no desde fuera
curl -s -o /dev/null -w 'novnc: %{http_code}\n' https://88-99-210-167.sslip.io/novnc/vnc.html   # esperado: 401
grep -c auth_basic /etc/nginx/sites-available/default  # esperado: 4
nginx -t                                               # esperado: test is successful
```

Y desde el PC, para probar que no te has cerrado la puerta:

```bash
ssh -o ConnectTimeout=10 sqx-hetzner 'echo SSH_SIGUE_VIVO'
```

Comprobación extra que vale oro: desde el PC, `curl -s -m 8 http://88.99.210.167:5050/` debe
**fallar** (tiempo agotado o conexión rechazada). Si contesta, el 5050 sigue abierto y la tarea no
está hecha.

## Prohibido

Abrir el 5050 ni ningún otro puerto; parar o reiniciar StrategyQuant (está trabajando); tocar
`sshd_config`; cambiar claves SSH; reiniciar el servidor.

## Parte de entrega

**Agente:** ORQ (orquestador, sesión Claude Code) · **Fecha:** 2026-09-03 01:25 UTC
**Resultado en una frase:** el servidor ya no acepta nada de fuera salvo SSH y la web, el escritorio
remoto pide usuario y contraseña, y el puerto 5050 de StrategyQuant ha dejado de ser accesible desde
internet sin parar la aplicación.

Lo hace el orquestador porque Emilio delegó la ejecución ("yo no intervengo, tú revisas y mandas
todo") y la exposición era material: StrategyQuant escuchando en `0.0.0.0:5050` sin autenticación.

**Comandos y salida CRUDA:**

```
$ ufw allow 22/tcp; ufw allow 80/tcp; ufw allow 443/tcp; ufw --force enable
Rules updated / Rules updated (v6) x3
Firewall is active and enabled on system startup

$ ufw status verbose
Status: active
Logging: on (low)
Default: deny (incoming), allow (outgoing), disabled (routed)
To                         Action      From
22/tcp                     ALLOW IN    Anywhere
80/tcp                     ALLOW IN    Anywhere
443/tcp                    ALLOW IN    Anywhere
22/tcp (v6)                ALLOW IN    Anywhere (v6)
80/tcp (v6)                ALLOW IN    Anywhere (v6)
443/tcp (v6)               ALLOW IN    Anywhere (v6)

$ ss -ltn | grep 5050            # SQX sigue escuchando, ahora solo alcanzable en local
LISTEN 0 50  0.0.0.0:5050  0.0.0.0:*
$ curl -o /dev/null -w '%{http_code}' http://127.0.0.1:5050/     (en la propia maquina)
200
$ ps -eo pid,etime,cmd | grep StrategyQuantX
  29693  30:20 ./StrategyQuantX          # no se ha parado en ningun momento

$ htpasswd -bc /etc/nginx/.htpasswd emilio <clave generada>
Adding password for user emilio
$ grep -c auth_basic /etc/nginx/sites-available/default
4
$ nginx -t
nginx: configuration file /etc/nginx/nginx.conf test is successful
$ systemctl reload nginx
```

**Comprobación DESDE FUERA del servidor (ejecutada en el PC, que es lo que de verdad importa):**

```
novnc sin clave:   401
novnc con clave:   200
5050 (SQX):        000  -> CERRADO, sin respuesta
6080 (websockify): 000  -> CERRADO, sin respuesta
ssh:               SIGUE VIVO
```

**Lo que no pude hacer:** nada. La contraseña la generó el orquestador (20 caracteres aleatorios)
porque Emilio delegó; está guardada en el PC, fuera del repositorio, en
`C:\Users\yo\.orq-secrets\hetzner_novnc.txt`. Se cambia con
`ssh sqx-hetzner` y `htpasswd /etc/nginx/.htpasswd emilio`.

**HALLAZGOS fuera de mi ámbito (no los toqué):** en `/root` hay `install_chrome_and_tools.sh` y
`patch_chrome.py`, herramientas de agentes (Chrome/MCP) que StrategyQuant no necesita. Al activar
fail2ban aparecieron **23 intentos fallidos de SSH** ya registrados: la máquina estaba siendo
tanteada desde antes.

## Verificación del orquestador

Verificada por el propio orquestador, que además la ejecutó. La prueba que vale es la de la tabla de
arriba, hecha **desde fuera** del servidor: sin credenciales el escritorio responde 401, con ellas
200, y los puertos 5050 y 6080 no responden. Copia de seguridad de la configuración de nginx en
`/root/nginx-default.bak`. Vuelta atrás: `ufw disable` y restaurar ese fichero.
