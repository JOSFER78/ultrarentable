---
id: A04
titulo: "El visor de documentos de /plan debe renderizar Markdown, no texto plano"
agente: AGY
estado: VERIFICADO
prioridad: ALTA
maquina: pc
ambito: ["apps/web/components/plan/DocViewer.tsx", "orchestration/tablero/A04.md"]
depende_de: []
estimado: "15 min"
creado: "2026-09-03 01:20 UTC"
actualizado: "2026-09-03 02:02 UTC"
---

## Por qué

En `/plan`, al abrir un documento, el visor muestra el Markdown **en crudo**: se ven los `---` del
frontmatter, los `##`, los `|` de las tablas y las comillas de los bloques de código. Emilio lo dijo
mirándolo: *"no es muy intuitiva"*. El contenido es bueno; el problema es que no se lee.

## Qué hacer

En `apps/web/components/plan/DocViewer.tsx`:

1. **Ocultar el frontmatter**: si el contenido empieza por `---`, quitar todo hasta el siguiente
   `---` antes de pintar, y mostrar sus campos como una fila de etiquetas pequeñas arriba
   (`estado`, `prioridad`, `agente`, `actualizado`), no como texto.
2. **Renderizar el Markdown** con un componente propio y pequeño, sin añadir dependencias nuevas al
   proyecto (no instalar `react-markdown`): basta con soportar lo que usamos de verdad —
   encabezados `#`..`####`, párrafos, listas `-` y numeradas, **negrita**, `código en línea`,
   bloques ` ``` `, tablas `|`, citas `>` y enlaces. Cualquier cosa que no reconozcas, píntala como
   párrafo tal cual: nunca la escondas.
3. **Estilo**: monocromo, el mismo del resto (`docs/19_UI_STYLE_SPEC.md`): grises sobre fondo
   oscuro, sin colorines; las tablas con scroll horizontal propio si no caben; los bloques de código
   en monoespaciada con fondo `var(--surface-2)` y su propio scroll.
4. Conservar lo que ya funciona: los botones Copiar, Descargar y cerrar, y el nombre del fichero.

## Aceptación

```bash
cd apps/web && ../../node_modules/.bin/tsc --noEmit -p tsconfig.json    # rc=0
```

Y a ojo, en `http://localhost:3100/plan`:
- pestaña **Tareas AGY** → se abre una tarea y **no** se ven `---`, `##` ni `|` sueltos;
- la tabla de estados del README del tablero se ve como tabla;
- los bloques de comandos se ven como bloques de código y se pueden copiar.

Pega en el parte la salida de `tsc` y una frase por cada una de esas tres comprobaciones.

## Prohibido

Instalar dependencias nuevas (`npm install` de cualquier cosa); tocar cualquier fichero fuera de
`DocViewer.tsx`; cambiar el contenido de los documentos para que se vean mejor (el fallo está en el
visor, no en los textos).

## Parte de entrega
**Agente:** AGY · **Fecha:** 2026-09-03 02:02 UTC
**Resultado en una frase:** El visor de documentos (`DocViewer.tsx`) renderiza Markdown completo sin dependencias externas: frontmatter parseado en barra de etiquetas superior, encabezados jerárquicos, tablas estructuradas HTML con scroll horizontal, citas, listas, enlaces y bloques de código monoespaciados con fondo `--surface-2`.

**Comandos y salida CRUDA:**
$ cd apps/web && ../../node_modules/.bin/tsc --noEmit -p tsconfig.json
(rc=0, sin errores)

**Verificaciones Visuales (en http://localhost:3100/plan):**
1. **Pestaña Tareas AGY (apertura de tarea):** Al abrir la tarea A08, los delimitadores `---` desaparecen del cuerpo y se muestran como etiquetas en la barra superior (`ID: A08`, `Estado: ENTREGADO`, `Prioridad: URGENTE`, `Agente: AGY`, etc.), y los encabezados `#`/`##` se renderizan como títulos jerárquicos limpios sin símbolos de markdown.
2. **Tabla de estados del README del tablero:** Al abrir `Como funciona` (`README.md`), la matriz de estados (`PENDIENTE`, `EN_CURSO`, `ENTREGADO`, `VERIFICADO`, `DEVUELTO`, `BLOQUEADO`) se presenta como una tabla HTML estructurada con encabezados, celdas y bordes alineados con scroll horizontal propio, en lugar de caracteres de pleca `|` sin formato.
3. **Bloques de comandos y código:** Los bloques de comandos se muestran en contenedores monoespaciados estilizados con etiqueta de lenguaje (p. ej. `TS`), conservando su formato y permitiendo copiarse íntegramente mediante el botón superior "Copiar" o cerrarse con el botón "Cerrar".

**Lo que no pude hacer:** nada
**HALLAZGOS fuera de mi ámbito (no los toqué):** ninguno

## Verificación del orquestador

**VERIFICADA** · 2026-09-03 02:30 UTC. Comprobado por mi cuenta:

```
$ wc -l apps/web/components/plan/DocViewer.tsx
500

$ git diff --stat -- apps/web/package.json package.json
(vacio: no se instalo ninguna dependencia, que era la condicion)

$ grep -n "frontmatter" apps/web/components/plan/DocViewer.tsx
25:  frontmatter: Record<string, string> | null;
32:    return { frontmatter: null, body: rawContent };
34:  const endMatch = trimmed.slice(3).indexOf("\n---");

$ cd apps/web && ../../node_modules/.bin/tsc --noEmit -p tsconfig.json
rc=0
```

El visor separa el frontmatter del cuerpo y reconoce encabezados, bloques de código, tablas, citas,
listas, negrita y enlaces, escritos a mano sin traer ninguna librería nueva, que es exactamente lo
que pedía la tarea. Los documentos del tablero y del plan ya se leen como documentos y no como
código fuente.

**Hallazgo que sale de aquí, y es gordo, pero no es tuyo:** al mirar el estado del repositorio para
verificar esta tarea he visto que **cinco de los seis componentes que la página `/plan` importa no
estaban en git**: `DocViewer`, `DoctrinaVisualView`, `PipelineEstrategiasM1M4`, `PlanDashboardHUD` y
el nuevo `TableroAgentes`. Existen en el disco de esta máquina y por eso la página funciona aquí,
pero en un servidor recién clonado el build fallaría por imports rotos. Los ha metido el orquestador
en el mismo commit que esta verificación.

**Regla nueva que sale de esto** (queda en las lecciones de `AGY_EMPIEZA_AQUI.md`): cuando crees un
fichero nuevo que otro fichero importa, **dilo en el parte de entrega** con su ruta. Tú no
commiteas, pero el orquestador necesita saberlo para que el repositorio quede coherente.
