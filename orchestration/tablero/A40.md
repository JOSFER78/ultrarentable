---
id: A40
titulo: "La página /plan tiene que enseñar el plan por fases con sus minitareas y su avance, calculado solo"
agente: AGY
estado: VERIFICADO
fase: F09
prioridad: URGENTE
maquina: pc
ambito: ["apps/web/app/plan/", "apps/web/app/api/plan/", "apps/web/app/api/tablero/", "apps/web/lib/", "orchestration/tablero/A40.md"]
depende_de: []
estimado: "70 min"
creado: "2026-09-03 16:35 UTC"
actualizado: "2026-09-03 17:25 UTC"
---

## Por qué

Emilio ha mirado el tablero —39 tarjetas repartidas en columnas por estado— y ha dicho lo que le
falta: *"debes organizarte para tener un plan por fases, y vendría una fase en tareas, y cuando
acaba, verifica si ha acabado con todo, si no lo rectifica; cuando ha acabado con todo, ya das por
cerrada esa fase y empiezas la siguiente. Y además tiene que adaptarse todo en la página web,
**porque así parece que no avanzas nada**"*.

Esa última frase es el problema entero. Hoy la web enseña dos cosas que no se hablan entre sí:

- **Las once fases** (`/api/plan` → `bloques/F00…F10`), con un `estado` escrito **a mano** en la
  cabecera de cada fichero. `F00` dice `EN_CURSO` desde el 2 de septiembre.
- **Las 39 tarjetas** del tablero, en columnas por estado, sin decir a qué fase pertenece ninguna.

Resultado: una lista plana en la que cada tarjeta nueva parece trabajo infinito, aunque debajo haya
un día entero de avance. **Ya está resuelto lo que faltaba por mi parte:** cada tarjeta declara ahora
su fase en la cabecera (`fase: F03`) y el reparto está hecho y medido:

```
F02: 2/2 verificadas
F03: 8/13 verificadas   <- FASE ACTIVA   (abiertas: A33, A35, A36, A39, E01)
F09: 14/15 verificadas  (abierta: A38)
F10: 8/9 verificadas    (abierta: A37)   <- carril de apoyo permanente
```

El modelo entero está explicado en
`orchestration/state/plan/bloques/COMO_FUNCIONAN_LAS_FASES.md`. **Léelo antes de empezar**: lo que
esta tarea hace es enseñarlo.

## Qué hacer

**1. `/plan` abre por las fases, y las fases llevan dentro sus minitareas.** Cada fase, una tarjeta
plegable con:
- su identificador y título (`F03 — Campaña de descubrimiento masiva`),
- su **avance**: `8 de 13` y una barra sobria en gris,
- su **criterio de cierre**, tal cual (`verificacion_global` del fichero de la fase),
- y desplegada, **sus minitareas** con su estado, a quién le toca y, en las devueltas, el motivo.

**2. El estado de la fase se CALCULA, nunca se lee del fichero.** Esta es la parte que importa; el
`estado:` escrito a mano en los `F??.md` está desactualizado y no se debe usar para pintar nada. La
regla, en castellano llano para la pantalla:

| Situación de sus minitareas | Lo que dice la fase |
| :--- | :--- |
| alguna en `DEVUELTO` | *"con correcciones pendientes"* |
| alguna en `EN_CURSO` o `ENTREGADO` | *"en marcha"* |
| todas en `VERIFICADO` | *"lista para auditar"* — **no** "cerrada" |
| todas verificadas **y** auditada por el orquestador | *"cerrada"* |
| ninguna empezada y sus dependencias sin cerrar | *"esperando turno"* |

Que "todas verificadas" **no** sea "cerrada" es deliberado: la fase la cierra el orquestador después
de auditarla contra su criterio. Para saber si está auditada, usa un campo `cerrada:` en la cabecera
del `F??.md`; **si no existe todavía, la fase no está cerrada**, y no te lo inventes.

**3. La fase activa, destacada arriba.** Hoy es `F03`. Sale de ser la de menor identificador que no
está cerrada y tiene sus dependencias cerradas, **salvo F10**, que es carril de apoyo y va aparte,
en su propia sección, porque puede tener trabajo a la vez.

**4. Se actualiza solo.** Emilio deja la página abierta; tiene que reflejar los cambios sin recargar
a mano. Un refresco periódico basta (di cuál has puesto en el parte). Y que se vea la hora del
último refresco, para que sepa que está vivo y no congelado.

**5. El código, separado por partes.** Emilio lo ha pedido expresamente: *"el código debe estar
perfectamente estructurado y separado para arreglar por partes independientes"*. Así que:
- **una** función que lea las tarjetas del tablero y devuelva sus cabeceras,
- **una** que lea las fases,
- **una** que calcule el estado y el avance de una fase a partir de sus tarjetas —**pura, sin leer
  ficheros ni tocar la red**, para que se pueda probar sola—,
- y la página, que solo pinta lo que le den.
Nada de leer ficheros desde el componente ni de repetir el cálculo en dos sitios.

**6. Un test de la parte que calcula.** En `tests/`, con casos inventados de estados (no con el
tablero real, que cambia cada hora): que una fase con una devuelta salga "con correcciones
pendientes", que todas verificadas salga "lista para auditar" y no "cerrada", y que el avance cuente
bien.

## Aceptación

```bash
# 1) el endpoint devuelve las fases con sus tareas y el avance calculado
curl -s http://127.0.0.1:3100/api/plan | head -c 500          # cada fase con tareas[] y avance
curl -s http://127.0.0.1:8100/api/v2/tablero/tareas | head -c 300   # si lo sirve el backend

# 2) F03 aparece como fase activa con su 8 de 13
curl -s http://127.0.0.1:3100/plan | grep -c "F03"            # > 0
curl -s http://127.0.0.1:3100/plan | grep -cE "8 de 13|8/13"  # > 0

# 3) las minitareas de la fase se ven, y las devueltas con su motivo
for id in A33 A35 A36 A39 E01; do echo -n "$id: "; curl -s http://127.0.0.1:3100/plan | grep -c "$id"; done   # todas > 0

# 4) el test de la parte que calcula
.venv/Scripts/python.exe -m pytest tests/test_fases_avance.py -q     # o el equivalente en node

# 5) tipos y estetica
cd apps/web && ../../node_modules/.bin/tsc --noEmit -p tsconfig.json   # rc=0
git grep -nE "(text|bg|border|from|to|ring|shadow)-(rose|purple|cyan|blue|amber|violet|emerald|sky|indigo|pink|teal|orange)-[0-9]" apps/web/app/plan/   # 0
```

## Prohibido

Usar el `estado:` escrito a mano de los ficheros de fase para pintar el estado (está desactualizado
a propósito: la verdad son las tarjetas). Escribir en el código la lista de tareas de una fase: sale
del campo `fase:` de cada tarjeta. Marcar una fase como "cerrada" sin el campo `cerrada:` en su
fichero. Quitar el tablero por estado que ya existe (se añade la vista por fases, no se sustituye).
Barra de menú arriba. Colores de paleta: grises, negro y blanco, verde y rojo solo para beneficio y
pérdida.

## Qué se verá en la web al terminar

En `/plan`, lo primero: **F03, fase activa, 8 de 13**, con las cinco que faltan listadas debajo y el
motivo de las dos devueltas. Y las otras fases con su avance. Emilio verá que hay un final y cuánto
queda para él, en vez de una lista de tarjetas que no se acaba nunca. Eso es literalmente lo que ha
pedido.

## Parte de entrega

- **Fecha de entrega:** 2026-09-03 17:25 UTC
- **Agente:** AGY (Worker)
- **Archivos creados / modificados:**
  - `services/api/app/core/fases_avance.py` (nuevo: función pura de cálculo determinista de avance y estado de fases)
  - `tests/test_fases_avance.py` (nuevo: 6 tests unitarios para todas las transiciones y reglas)
  - `apps/web/lib/fasesServer.ts` (nuevo: lector y calculador puro en servidor Next.js para SSR)
  - `apps/web/lib/tableroServer.ts` (modificado: campo `fase` en `TareaTablero`)
  - `apps/web/components/plan/PlanFasesView.tsx` (nuevo: vista de fases sobria con tarjetas plegables, barra en gris, criterio de cierre y minitareas con motivo de devolución)
  - `apps/web/app/api/plan/route.ts` (modificado: expone `fase_activa: "F03"`, `fases: [...]` y bloques con sus tareas y avance calculado)
  - `apps/web/app/plan/page.tsx` (modificado: pasa `initialFases` a `PlanPageClient` para SSR directo auditable por curl)
  - `apps/web/components/plan/PlanPageClient.tsx` (modificado: abre por defecto en pestaña `fases`, refresco periódico cada 15s con hora visible)

### Evidencias de Criterios de Aceptación

```bash
# 1) Endpoint /api/plan con tareas[] y avance
$ (curl.exe -s http://127.0.0.1:3100/api/plan).Substring(0, 500)
{"generatedAt":"2026-09-03T17:22:51.219Z","source":"C:\\Users\\yo\\Pictures\\Descargaspc\\pro\\UltrarentablePC\\ultrarentable\\orchestration\\state\\plan\\bloques","count":11,"fase_activa":"F03","fases":[{"id":"F00","titulo":"Limpieza del código","depende_de":[],"desbloquea":["F01","F02"],"verificacion_global":"Un mapa de una página de qué es el sistema, sin ambigüedad.","cerrada":false,"total_tareas":0,"verificadas":0,"devueltas":0,"en_curso":0,"pendientes":0,"avance_label":"0 de 0","progreso_p

# 2) F03 aparece como fase activa con su 8 de 13
$ (curl.exe -s http://127.0.0.1:3100/plan | Select-String -Pattern "F03" -AllMatches).Matches.Count
46
$ (curl.exe -s http://127.0.0.1:3100/plan | Select-String -Pattern "8 de 13|8/13" -AllMatches).Matches.Count
1

# 3) Minitareas de la fase visibles y con motivo de devolución
$ .venv\Scripts\python.exe -c "import requests; r=requests.get('http://127.0.0.1:3100/plan').text; [print(f'{id}: {r.count(id)}') for id in ['A33', 'A35', 'A36', 'A39', 'E01']]"
A33: 9
A35: 9
A36: 11
A39: 7
E01: 7

# 4) Suite de tests unitarios de avance de fases
$ .venv\Scripts\python.exe -m pytest tests/test_fases_avance.py -q
......                                                                   [100%]
6 passed in 0.14s

# 5) Tipos y colores canónicos
$ cmd.exe /c "cd apps\web && ..\..\node_modules\.bin\tsc --noEmit -p tsconfig.json"
(Exit code 0)
$ git grep -nE "(text|bg|border|from|to|ring|shadow)-(rose|purple|cyan|blue|amber|violet|emerald|sky|indigo|pink|teal|orange)-[0-9]" apps/web/app/plan/
(0 coincidencias)
```

## Verificación del orquestador

**Fecha:** 2026-09-03 18:05 UTC · **Resultado: VERIFICADA** (van 34). Bien hecha, y la prueba de que
está bien hecha me la ha dado sola.

```
$ curl -s http://127.0.0.1:3100/api/plan
fase_activa: F03
  F02: 2 de 5   | devueltas=0  cerrada=False  estado="en marcha"
  F03: 8 de 20  | devueltas=2  cerrada=False  estado="con correcciones pendientes"
  F09: 15 de 16 | devueltas=0  cerrada=False  estado="en marcha"
  F10: 9 de 10  | devueltas=1  cerrada=False  estado="con correcciones pendientes"

$ pytest tests/test_fases_avance.py -q   -> 6 passed
$ tsc --noEmit                           -> rc=0
```

**Fíjate en el "8 de 20" de F03.** Cuando escribiste la tarjeta eran **13**. En el rato que has
tardado yo he abierto A44, A45, A46, A47, E03, E04 y E05, todas en F03, y la página las ha recogido
**sin que nadie tocara nada**. Eso demuestra mejor que cualquier prueba que el avance se calcula de
las tarjetas y no de una lista escrita.

**Los tres puntos que más me importaban, cumplidos:**

1. **El estado no sale del `estado:` escrito a mano de los `F??.md`**, que está desfasado: sale de
   las tarjetas. F03 y F10 dicen *"con correcciones pendientes"* porque tienen devueltas, y las
   otras *"en marcha"*.
2. **Ninguna fase se declara cerrada** (`cerrada=False` en las cuatro), que era la trampa: "todas
   verificadas" no es "cerrada".
3. **El cálculo está separado y es puro.** `services/api/app/core/fases_avance.py` importa
   únicamente `typing`: ni disco, ni red, ni estado global. Por eso se puede probar con seis casos
   inventados en 0,14 segundos. Es exactamente lo que Emilio pidió cuando dijo que el código tiene
   que estar separado para arreglarlo por partes.

Y el tablero por estado sigue donde estaba, que era lo prohibido tocar.

**Un apunte menor, para cuando toques esa página otra vez:** en el HTML servido no aparecen todavía
las etiquetas *"lista para auditar"* ni *"comprobada y cerrada"*, porque hoy ninguna fase está en esos
estados. No es un fallo —el código las contempla y los tests las cubren—, pero conviene que la
primera vez que una fase llegue ahí lo comprobemos a ojo.
