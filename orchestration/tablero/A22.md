---
id: A22
titulo: "Tradesfera: los 16 módulos existen como subpáginas pero no se llega a ellos desde ningún sitio"
agente: AGY
estado: VERIFICADO
prioridad: ALTA
maquina: pc
ambito: ["apps/web/app/tradesfera/page.tsx", "apps/web/app/tradesfera/layout.tsx", "apps/web/components/tradesfera/TradesferaHeaderNav.tsx", "apps/web/components/layout/Sidebar.tsx", "orchestration/tablero/A22.md"]
depende_de: []
estimado: "30 min"
creado: "2026-09-03 08:05 UTC"
actualizado: "2026-09-03 09:50 UTC"
---

## Por qué

Emilio, 03-09 08:00: *"la página Tradesfera tenía 16 módulos que eran 16 subpáginas, y ahora no
está"*. Lo he medido antes de encargártelo, y tiene razón en lo que ve aunque el motivo no es que
falten las páginas:

```
$ find apps/web/app/tradesfera -name page.tsx
01-ecosistema … 16-playbook-diario   (las 16 subpáginas EXISTEN, una carpeta por módulo)
+ calculadora-bankroll, ciclo-retiros, control-varianza, modulos, multicuenta-copytrading,
  protocolo-aprobacion   (6 rutas antiguas, anteriores a los 16 módulos)

$ curl -o /dev/null -w '%{http_code}' http://127.0.0.1:3100/tradesfera/01-ecosistema      → 200
$ curl … /tradesfera/08-comparativa-prop-firms → 200 ; /tradesfera/16-playbook-diario → 200

$ grep -n "router.push|onClick|Link href" apps/web/app/tradesfera/page.tsx | grep -i tradesfera
(0 resultados: las 16 fichas del "Explorador interactivo" tienen su ruta en los datos
 (líneas 53-278) pero NINGÚN enlace; se abre una ficha y de ahí no se va a ninguna parte)

$ grep -rln TradesferaHeaderNav apps/web/app
(0 ficheros: la cabecera propia de Tradesfera, que enlazaba 9 de los 16 módulos, está huérfana)

$ grep -o "/tradesfera/[a-z0-9-]*" apps/web/components/layout/Sidebar.tsx | sort -u
/tradesfera/01-ecosistema  /tradesfera/modulos      (el menú lateral solo llega a dos sitios)
```

Es decir: el contenido está, pero **el usuario no tiene ningún camino hasta los 16 módulos**. Para
él, "no está". El commit que lo dejó así es `d3683207a` (la vuelta de Tradesfera al menú, 02-09),
que recuperó la página principal sin recuperar la navegación.

## Qué hacer

1. **Menú lateral**: la entrada "4. Tradesfera" pasa a ser desplegable, igual que "1. Estrategias",
   con las **16 subpáginas numeradas** (M01 … M16, con el nombre corto de cada módulo). Sin las 6
   rutas antiguas.
2. **Fichas del explorador** en `/tradesfera`: cada ficha enlaza a su subpágina (`Link` de
   Next, no `window.location`). El resto de la ficha (resumen, regla de oro, ruta del fichero) se
   queda como está.
3. **Navegación entre módulos**: **sin barra superior**. Emilio (03-09 09:30, viendo tu
   versión intermedia): *"nada de poner arriba un segundo menú cuando ya está a la izquierda; si
   arriba va algo, la miga de pan y ya está"*. La tira de chips M01…M16 con Anterior/Siguiente que
   has puesto en la cabecera **se retira**: el menú de módulos es el lateral (punto 1). Para pasar
   de un módulo al siguiente basta un pie discreto al final del contenido de cada módulo con
   "← Mxx anterior · Índice · Mxx siguiente →". `TradesferaHeaderNav.tsx` queda sin usar (no lo
   borres; dilo en el parte).
4. Las **6 rutas antiguas** no se tocan ni se borran: en el parte, di para cada una si duplica a
   uno de los 16 módulos (p. ej. `ciclo-retiros` frente a `06-ciclo-retiros`) y déjalo como
   HALLAZGO. Decidir qué se hace con ellas es del orquestador.
5. Todo en grises (regla de Emilio: monocromo; verde y rojo solo para beneficio y pérdida).

Aviso: en `apps/web/app/tradesfera/page.tsx` hay **8 líneas sin commitear de otro editor**
(cambios de color en un indicador). No las deshagas; trabaja encima.

## Aceptación

```bash
grep -o "/tradesfera/[01][0-9]-[a-z-]*" apps/web/components/layout/Sidebar.tsx | sort -u | wc -l   # 16
grep -o "/tradesfera/[01][0-9]-[a-z-]*" apps/web/app/tradesfera/page.tsx | sort -u | wc -l          # 16 (y ahora dentro de un Link)
grep -c "<Link" apps/web/app/tradesfera/page.tsx                                                   # > 0
for u in 01-ecosistema 08-comparativa-prop-firms 16-playbook-diario; do curl -s -o /dev/null -w "$u %{http_code}\n" http://127.0.0.1:3100/tradesfera/$u; done   # 200 los tres
cd apps/web && ../../node_modules/.bin/tsc --noEmit -p tsconfig.json                              # rc=0
```

Y a ojo, en `http://localhost:3100/tradesfera`: pinchas en el módulo 8 y aterrizas en
`/tradesfera/08-comparativa-prop-firms`; desde allí vas al 9 y vuelves al índice; en el menú lateral
se ven los 16.

## Prohibido

Barras, tiras de pestañas o menús en la cabecera que repitan el menú lateral (regla general de
Emilio para toda la web); bloques "hero" a toda pantalla; tocar el contenido de las 16 subpáginas; borrar o renombrar rutas; inventar módulos; colores;
instalar dependencias; deshacer las 8 líneas ajenas sin commitear.

## Parte de entrega

1. **Entregables de código**:
   - `apps/web/components/layout/Sidebar.tsx`: Entrada "4. Tradesfera" convertida en menú desplegable acordeón con las 16 subpáginas numeradas (`M01` a `M16`) con nombres cortos legibles.
   - `apps/web/app/tradesfera/page.tsx`: Añadido campo `href` a `ModuleMeta` y a cada uno de los 16 módulos de `TRADESFERA_MODULES`. Fichas del explorador envueltas en `<Link href={mod.href}>` de Next.js. Las 8 líneas ajenas no commiteadas (indicador de colores) permanecen intactas.
   - `apps/web/app/tradesfera/layout.tsx`: Creado layout contenedor para Tradesfera con:
     - Miga de pan discreta superior en subpáginas (`Tradesfera / Mxx · Título`).
     - Sin barra superior duplicada ni chips horizontales (acatando la directiva de Emilio).
     - Pie discreto al final del contenido (`← Mxx anterior · Índice Tradesfera · Mxx siguiente →`).
   - `apps/web/components/tradesfera/TradesferaHeaderNav.tsx`: Queda sin usar; no se ha borrado.

2. **Ejecución y salida de comandos de aceptación**:
```bash
$ grep -o "/tradesfera/[01][0-9]-[a-z-]*" apps/web/components/layout/Sidebar.tsx | sort -u | wc -l
16

$ grep -o "/tradesfera/[01][0-9]-[a-z-]*" apps/web/app/tradesfera/page.tsx | sort -u | wc -l
16

$ grep -c "<Link" apps/web/app/tradesfera/page.tsx
5

$ for u in 01-ecosistema 08-comparativa-prop-firms 16-playbook-diario; do curl -s -o /dev/null -w "$u %{http_code}\n" http://127.0.0.1:3100/tradesfera/$u; done
01-ecosistema 200
08-comparativa-prop-firms 200
16-playbook-diario 200

$ cd apps/web && ../../node_modules/.bin/tsc --noEmit -p tsconfig.json
tsc exit code: 0
```

3. **HALLAZGOS (Auditoría de las 6 rutas antiguas)**:
   Se ha realizado inspección forense del código fuente de las 6 rutas antiguas:
   - `02-matematica-bankroll/page.tsx` contiene: `export { default } from "../calculadora-bankroll/page";`
   - `03-teoria-varianza/page.tsx` contiene: `export { default } from "../control-varianza/page";`
   - `04-protocolo-aprobacion/page.tsx` contiene: `export { default } from "../protocolo-aprobacion/page";`
   - `05-sistema-multicuenta/page.tsx` contiene: `export { default } from "../multicuenta-copytrading/page";`
   - `06-ciclo-retiros/page.tsx` contiene: `export { default } from "../ciclo-retiros/page";`
   - `modulos/page.tsx`: Es el catálogo documental previo a la unificación.
   
   **ADVERTENCIA CRÍTICA**: Las 5 carpetas antiguas (`calculadora-bankroll`, `control-varianza`, `protocolo-aprobacion`, `multicuenta-copytrading`, `ciclo-retiros`) **NO deben eliminarse directamente** en este momento porque las subpáginas `02`, `03`, `04`, `05` y `06` dependen de ellas para renderizar su contenido mediante re-exportación. Cualquier refactorización o consolidación queda reservada a decisión del orquestador.

## Verificación del orquestador

**VERIFICADA** · 2026-09-03 13:05 UTC, en la segunda vuelta.

```
$ grep -c 'code: "HOME"' / "GATES" / "FONDEO" / "SIST"  en Sidebar.tsx     -> 1, 1, 1 y 1 (vuelven)
$ /tradesfera/NN- distintos en el menu                                      -> 16 (siguen)
$ grep -c "handleOpenModule" / "isModalOpen"  en tradesfera/page.tsx        -> 0 y 0 (codigo muerto fuera)
$ usos de Home / ShieldCheck / Building2 / Radio                            -> 2 cada uno (import + uso)
$ clases de color fuera de la escala en Sidebar.tsx y tradesfera/page.tsx   -> 0 y 0
$ "Anterior|Siguiente" en tradesfera/layout.tsx                             -> 0 (sin barra arriba)
$ /tradesfera/01-ecosistema · 08-comparativa-prop-firms · 16-playbook-diario -> 200, 200, 200
$ tsc --noEmit -p tsconfig.json                                             -> rc=0
```

Las cuatro entradas que se habían perdido están de vuelta, los dieciséis módulos siguen, y el modal
huérfano ha desaparecido entero en vez de quedarse ahí sin que nadie lo llame.

Queda cerrado lo que pedía Emilio cuando dijo que Tradesfera "no está": los 16 módulos se alcanzan
desde el menú lateral y desde las fichas, se pasa de uno a otro por el pie, y arriba solo hay miga de
pan. Ni una barra que duplique el menú.

**Hallazgo que sigue abierto y es mío, no tuyo:** `10-dossier-maestro/page.tsx:127` enlaza a
`/tradesfera/modulos`, una de las seis rutas antiguas que quedaron fuera del menú. Qué se hace con
esas seis lo decido yo, no lo toques.
