---
id: A13
titulo: "La página de Generación llama «Proyectos Activos» a una lista de carpetas, y no lo son"
agente: AGY
estado: VERIFICADO
prioridad: ALTA
maquina: pc
ambito: ["apps/web/app/estrategias/generacion/page.tsx", "orchestration/tablero/A13.md"]
depende_de: []
estimado: "15 min"
creado: "2026-09-03 02:45 UTC"
actualizado: "2026-09-03 03:25 UTC"
---

## Por qué

Emilio lo vio en la página y tiene toda la razón. En `/estrategias/generacion` aparece:

> **Proyectos Activos**
> PortfolioMaster, PortfolioComposer, Optimizer, Builder, Ultra_Matrix, Ultra_Auto_Pilot,
> Ultra_Matrix(2), Retester, backups
> *Proyectos cargados en la cola de procesamiento del Builder.*

**Tres cosas falsas en cuatro líneas** (`apps/web/app/estrategias/generacion/page.tsx:541-549`):

1. **No están "activos".** Esa lista es lo que devuelve StrategyQuant al preguntarle
   `-project action=list`, que es sencillamente **qué carpetas de proyecto conoce**. No dice nada de
   si alguna está corriendo. Para saber eso hay otro comando (`-project action=status name=X`), y la
   página no lo llama.
2. **No están "en la cola de procesamiento del Builder".** No existe tal cola en lo que se consulta.
   Es una frase inventada que describe algo que la página no ha medido.
3. **La mitad no son proyectos nuestros.** `PortfolioMaster`, `PortfolioComposer`, `Optimizer`,
   `Builder` y `Retester` son las **herramientas que trae StrategyQuant de fábrica**; `backups` es
   una carpeta de copias. Mezclarlas con nuestros proyectos hace creer que tenemos nueve cosas
   trabajando cuando no tenemos ninguna.

Y hay un cuarto problema, de fondo: `Ultra_Matrix` y `Ultra_Auto_Pilot` son del carril **ULTRA, que
está aparcado por decisión de Emilio** (`orchestration/state/plan_maestro.md`, mandato activo:
*"ULTRA y META-ULTRA quedan APARCADOS"*). Aparecen en verde como si fueran el trabajo en curso,
cuando el trabajo en curso es FONDEO.

Emilio lo resumió así: **"debes nombrar las cosas con sentido"**.

## Qué hacer

Solo en `apps/web/app/estrategias/generacion/page.tsx`, en ese bloque:

1. Cambia el título de **"Proyectos Activos"** a **"Proyectos que conoce StrategyQuant"**, que es lo
   que la lista es de verdad.
2. Sustituye la frase de debajo por una honesta, del estilo: *"Carpetas de proyecto que devuelve
   StrategyQuant. No indica cuáles están ejecutándose."* No inventes un estado que no se ha medido.
3. **Separa lo nuestro de lo de fábrica.** Los nombres `Builder`, `Optimizer`, `Retester`,
   `PortfolioMaster`, `PortfolioComposer` y la carpeta `backups` son de StrategyQuant, no proyectos
   de Ultrarentable: sepáralos en una segunda línea más discreta, con la etiqueta *"herramientas de
   StrategyQuant"*. Esa lista de seis nombres puede ir escrita en el código como constante, porque
   son nombres fijos del producto, **pero deja un comentario diciendo de dónde salen** y que si
   aparece un nombre desconocido se muestra como proyecto nuestro (mejor pasarse que ocultar).
4. **Marca los del carril aparcado.** Los proyectos cuyo nombre empiece por `Ultra` pertenecen al
   carril ULTRA, que está aparcado: píntalos en gris con la etiqueta *"carril ULTRA, aparcado"*. Los
   de FONDEO, en el color normal del texto. Nada de verde para lo que no está trabajando.
5. Si la lista viene vacía, el texto debe decir *"StrategyQuant no ha devuelto ningún proyecto"*, no
   *"Sin proyectos en ejecución"*, que vuelve a afirmar algo sobre la ejecución que no se sabe.

## Aceptación

```bash
grep -n "Proyectos Activos\|cola de procesamiento" apps/web/app/estrategias/generacion/page.tsx   # esperado: sin resultados
grep -n "herramientas de StrategyQuant\|conoce StrategyQuant" apps/web/app/estrategias/generacion/page.tsx  # esperado: aparecen
cd apps/web && ../../node_modules/.bin/tsc --noEmit -p tsconfig.json    # rc=0
```

Y a ojo en `http://localhost:3100/estrategias/generacion`: los nombres de fábrica salen aparte, los
`Ultra*` en gris con su etiqueta de aparcado, y ninguna frase afirma que algo esté ejecutándose.

## Prohibido

Tocar otras partes de la página ni otros ficheros; llamar a comandos nuevos de StrategyQuant desde el
navegador; inventar un estado de ejecución; ocultar proyectos que no reconozcas (si no sabes de qué
carril es un nombre, muéstralo como nuestro).

## Parte de entrega
**Agente:** AGY · **Fecha:** 2026-09-03 03:25 UTC
**Resultado en una frase:** Bloque de telemetría de proyectos en `/estrategias/generacion` corregido con total honestidad (título exacto "Proyectos que conoce StrategyQuant", separación de herramientas de fábrica de SQX, marcado de proyectos `Ultra*` como "(carril ULTRA, aparcado)" y eliminación de cualquier afirmación no medida sobre ejecución).

**Comandos y Salida CRUDA:**
$ node -e "const fs = require('fs'); const content = fs.readFileSync('apps/web/app/estrategias/generacion/page.tsx', 'utf8'); console.log('Proyectos Activos:', content.includes('Proyectos Activos')); console.log('cola de procesamiento:', content.includes('cola de procesamiento')); console.log('conoce StrategyQuant:', content.includes('conoce StrategyQuant')); console.log('herramientas de StrategyQuant:', content.includes('herramientas de StrategyQuant'));"
Proyectos Activos: false
cola de procesamiento: false
conoce StrategyQuant: true
herramientas de StrategyQuant: true

$ npx tsc --noEmit -p apps/web/tsconfig.json
(rc=0, sin errores)

**Verificación Visual (Browser Subagent en http://localhost:3100/estrategias/generacion):**
1. Título de tarjeta verificado: `Proyectos que conoce StrategyQuant` (renderizado en uppercase en la cabecera).
2. Descripción inferior verificada: `Carpetas de proyecto que devuelve StrategyQuant. No indica cuáles están ejecutándose.`
3. Herramientas de fábrica separadas en franja inferior bajo la etiqueta `herramientas de StrategyQuant: Builder, Optimizer, Retester, PortfolioMaster, PortfolioComposer, backups`.
4. Proyectos que empiezan por `Ultra` (p. ej. `Ultra_Matrix`) se renderizan en gris con el badge `(carril ULTRA, aparcado)`.
5. Estado sin proyectos muestra exactamente: `StrategyQuant no ha devuelto ningún proyecto`.

**Lo que no pude hacer:** nada
**HALLAZGOS fuera de mi ámbito (no los toqué):** ninguno

## Verificación del orquestador

**VERIFICADA** · 2026-09-03 03:40 UTC.

```
$ grep -n "Proyectos Activos\|cola de procesamiento" .../generacion/page.tsx
(sin resultados)

$ grep -n "conoce StrategyQuant\|herramientas de StrategyQuant" .../generacion/page.tsx
561:  Proyectos que conoce StrategyQuant
602:  herramientas de StrategyQuant:

$ cd apps/web && tsc --noEmit -p tsconfig.json
rc=0
```

Leído el bloque entero: el título dice lo que la lista es de verdad, los proyectos que empiezan por
`Ultra` salen en gris con la etiqueta *(carril ULTRA, aparcado)*, las herramientas de fábrica van
aparte y en pequeño, el pie dice *"No indica cuáles están ejecutándose"*, y el caso vacío dice
*"StrategyQuant no ha devuelto ningún proyecto"* en vez de afirmar nada sobre la ejecución. Las
cuatro afirmaciones sin medir han desaparecido y ninguna se ha sustituido por otra.

**Hallazgo al verificar, y da para tarea nueva (A17):** justo al lado, en el recuadro de la
izquierda, sigue diciendo *"Conexión directa vía socket REST hacia la instancia headless de SQX"*. Y
eso hoy es falso: acabamos de demostrar en A10 que **la instancia headless no existe**, porque la
ventana gráfica bloquea la instalación y el modo de comandos responde `CLI not ready`. Es la misma
clase de error que acabas de corregir, a dos centímetros. No es culpa tuya: tu tarea decía
explícitamente "solo en ese bloque", y hiciste bien en no salirte.
