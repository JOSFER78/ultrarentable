import React from "react";
import GateDetailClient from "@/app/gates/[slug]/GateDetailClient";

// Subpágina canónica de cada una de las 11 puertas dentro de la carpeta M3: Valoración
// (/estrategias/valoracion/[slug])

export default function M3GateDetailPage() {
  return <GateDetailClient />;
}
