# ORDER AG2-P02-006 — PHASE 02 BEHAVIORAL RUNTIME PROOF & EXECUTION-BOUNDARY VERIFICATION

## STATUS
`ISSUED`

## OBJECTIVE
Cerrar los defectos R01-R09 de la revisión P02-005 demostrando comportamiento real y trazabilidad hasta el execution boundary. Esta orden no acepta como evidencia la mera existencia de campos, compiladores o tests estructurales.

## STRICT SCOPE
SOLO PHASE 02 / REWORK.
NO Phase 03.
NO Discovery Factory.
NO Strategy Genome.
NO Meta-Strategy.
NO ULTRA implementation.
NO FONDEO implementation.

## MANDATORY EXECUTION PLAN

### STEP 0 — CONTROL-IDENTITY CHECKPOINT
Subagentes: RECON + RELIABILITY.
Antes de modificar código:
- comprobar `00_DISPATCH`, `01_CONTROL_STATE`, `02_CURRENT_ORDER`;
- registrar el `dispatch_id` exacto recibido;
- verificar que cualquier handoff final usa exactamente el mismo `dispatch_id`;
- documentar SHA inicial de `origin/main`.
Si hay incoherencia: corregir el control/evidence y no declarar entrega.

### STEP 1 — BEHAVIORAL TEST DESIGN
Subagentes: QUANT + TEST/INTEGRATION.
Construir casos físicos controlados y reproducibles para demostrar comportamiento, no sólo representación:
- LONG con movimiento favorable/desfavorable;
- SHORT con movimiento favorable/desfavorable;
- BOTH produciendo al menos una entrada LONG y una entrada SHORT en casos físicos separados;
- AND/OR;
- SL/TP por cada tipo realmente soportado;
- trailing;
- time stop;
- sesión válida / inválida / cambio de día / close_at_eod;
- sizing y cantidad efectiva;
- max_open_positions.

### STEP 2 — REMOVE CAPITAL DEFAULT
Subagentes: RUNTIME + RED-TEAM.
Eliminar cualquier default cuantitativo como `account_equity_usd=100000.0`.
El capital debe ser un input obligatorio y trazable a un account/track/policy contract.
Ausencia = FAIL CLOSED.

### STEP 3 — TRUE BOTH SEMANTICS
Subagentes: RUNTIME + QUANT + TEST.
No basta con conservar `BOTH` en el instruction.
Demostrar comportamiento real LONG y SHORT con resultados separados y hashes/fixtures de datos físicos verificables.
Si el motor universal no soporta una semántica concreta, `UNSUPPORTED_FAIL_CLOSED`.

### STEP 4 — TRUE POSITION MODEL
Subagentes: RUNTIME + QUANT/RISK + LEDGER.
Resolver `max_open_positions` de forma real.
Si el engine canónico sólo admite una posición, el contrato debe rechazar valores >1 de forma explícita.
No leer el campo sin aplicarlo.
Demostrar quantity/position count en el resultado.

### STEP 5 — TRUE INSTRUMENT SIZING
Subagentes: QUANT/RISK + DATA/PROVENANCE.
Demostrar de dónde provienen:
- tick/point value;
- contract multiplier cuando aplique;
- moneda/valor monetario;
- riesgo monetario;
- conversión a contratos/unidades.
No asumir que `USD / price` es un sizing válido universal.
Si el contrato de instrumento no existe, FAIL CLOSED.

### STEP 6 — SESSION / EOD BEHAVIOR
Subagentes: RUNTIME + DATA + TEST.
Demostrar con timestamps físicos:
- start/end;
- allowed_days;
- ventana cruzando medianoche;
- entrada prohibida fuera de sesión;
- `close_at_eod` cerrando una posición de forma reproducible.

### STEP 7 — FILL/INTRABAR POLICY
Subagentes: REAL ENGINE TRACE + QUANT + RED-TEAM.
Encontrar la política real de fills del engine universal.
- Si existe: reutilizarla y demostrar binding.
- Si no existe: registrar `BLOCKED` y NO inventar una política.
La prioridad SL>TP no puede considerarse verdad universal sólo porque un adaptador la haya programado.

### STEP 8 — REAL EXECUTION BOUNDARY
Subagentes: REAL ENGINE TRACE + LEDGER/LINEAGE + API/UI PROVENANCE.
Demostrar call-sites reales desde:
`CanonicalStrategy -> snapshot/serialization -> compile -> adapter -> universal engine -> execution/ledger input`.
La evidencia debe mostrar código de producción real, no un nuevo backtester paralelo utilizado únicamente por tests.

### STEP 9 — BEHAVIORAL TEST MATRIX
Subagentes: TEST + RED-TEAM + RELIABILITY.
Crear/actualizar tests donde cada claim importante se valide por resultado de ejecución.
Los tests estructurales pueden complementar, pero nunca sustituir los behavioral tests.
Registrar exactamente qué test prueba qué requisito.

### STEP 10 — CROSS-AGENT INDEPENDENCE
Subagentes independientes:
1. RECON
2. RUNTIME
3. QUANT
4. DATA
5. TEST
6. RED-TEAM
7. LINEAGE
8. RELIABILITY

Cada uno debe aportar:
- agent_id;
- tarea concreta;
- archivos revisados;
- comandos;
- exit code;
- evidencia única;
- hallazgo;
- conclusión.

Al menos dos agentes deben revisar de forma independiente los puntos críticos `BOTH`, `sizing` y `fill policy`.

### STEP 11 — RECONCILIATION
El lead no puede convertir automáticamente todos los resultados en PROVEN.
Debe clasificar cada claim como:
`PROVEN / UNPROVEN / FAILED / BLOCKED / DEFERRED`.
Cualquier `UNPROVEN` crítico impide READY_FOR_REVIEW.

### STEP 12 — DELIVERY GATE
Sólo después de todos los pasos:
- focused tests;
- bounded regression;
- remote jobs terminados;
- exit codes reales;
- commit;
- push `origin/main`;
- verificar SHA exacto;
- handoff.

El handoff debe contener exactamente el mismo `dispatch_id` que `00_DISPATCH.md`.
Después: STOP.

## SUBAGENT EXECUTION PROOF
El ledger debe existir antes del handoff final. Si un subagente obligatorio no pudo ejecutarse, el estado es `BLOCKED`, nunca READY_FOR_REVIEW.

## SSH / LONG JOBS
Usar ejecución asíncrona/detached. Registrar `remote_job_id`, comando exacto, target SHA, log, estado y exit code. No bloquear el orquestador esperando suites largas.

## ZERO ABSOLUTE
ZERO-MOCK.
ZERO-SIMULATION.
ZERO-FORCING.
ZERO-LOOKAHEAD.
REAL-ONLY.
EVIDENCE-GATED.

No fabricar capital, sizing, fills, métricas, datasets, hashes, trades ni evidencia.

## COMPLETION RULE
Tests verdes no equivalen a cierre. La orden termina sólo cuando el comportamiento real, la trazabilidad y la evidencia multi-agente están demostrados en `origin/main`.
