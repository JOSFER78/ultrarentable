# ORDER AG2-P00-002 — REALITY LOCK P0 REMEDIATION

## Status

`LOCKED`

This order becomes executable only when `02_CURRENT_ORDER.md` is replaced by this order with `status: ISSUED` and `01_CONTROL_STATE.md` points to the new order while remaining in `CURRENT_PHASE: 00` with `PHASE_STATUS: REWORK` or `READY`.

## Why this order exists

The forensic baseline AG2-P00-001 successfully mapped the real repository and exposed critical P0 defects. We do not enter the downstream data/strategy phases while foundational certification integrity is still compromised.

This is an adaptive remediation order created from evidence in `03_HANDOFF_AG2-P00-001.md`.

## P0 defects that must be addressed

### P0-01 — Synthetic / precomputed portfolio curves

Audit and remove any operational path in `services/api/app/factory/ultra_portfolio_engine.py` or related services that can return fabricated/static equity curves, fixed metrics or precomputed growth instead of results generated by the canonical execution/ledger pipeline.

Requirements:
- no hardcoded trading results;
- no fabricated curves;
- no fallback curve when evidence is missing;
- `NO_EVIDENCE` / error when real computation is unavailable;
- all portfolio metrics must trace to canonical component ledgers and current evidence.

### P0-02 — Broken FastAPI lifespan imports

Resolve the broken/missing `services.optimization.*` import paths identified in `services/api/app/main.py`.

Requirements:
- trace actual runtime startup;
- remove dead/ghost imports or restore the canonical modules if they are truly required;
- no broad catch-and-ignore fallback that masks broken startup;
- prove a real application startup path.

### P0-03 — Missing version-lineage modules

Resolve the missing `services/version_control_manager.py` and `services/engine_version.py` dependencies identified by lineage/version routers.

Requirements:
- establish one canonical SSOT for strategy/engine/contract version identity;
- no duplicate version stores;
- deterministic version resolution;
- preserve legacy/current distinction;
- material engine/contract/policy changes must trigger affected evidence invalidation/revalidation;
- integrate `trial_id` where required by certification lineage.

## Required secondary remediation

Address, where necessary to make the P0 fixes real and non-bypassable:

- direct candidate status mutation bypass;
- candidate-not-found fallback in backtest/gates API;
- hardcoded G7-G10 frontend passes;
- simulated frontend success/chat fallbacks;
- PF 1.19 inherited APPROVED record versus current threshold;
- Gate 07 linear timestamp fallback;
- broken frontend navigation/import paths where touched by the above.

Do NOT broaden into cosmetic UI work.

## Mandatory subagents

Antigravity must orchestrate:

1. RECON / ARCHITECTURE
2. IMPLEMENTATION / P0 FIXES
3. QUANT / EXECUTION INTEGRITY
4. VERSION / LINEAGE
5. ZERO-MOCK / RED-TEAM
6. API / CERTIFICATION
7. UI / PROVENANCE
8. TEST / REGRESSION

A fixing subagent cannot be the only verifier.

## Required verification

Run real tests covering:

- portfolio output provenance;
- API startup;
- version resolution and lineage;
- evidence invalidation/revalidation semantics;
- candidate status mutation;
- missing candidate behavior;
- gate evidence integrity;
- frontend gate provenance;
- zero-mock / hardcoded-result scans;
- complete regression suite.

Any failing test must be reported; never modify tests merely to obtain green output.

## GitHub delivery contract

The entire result must be:

1. implemented in the real project workspace;
2. tested;
3. committed;
4. pushed to `origin/main`;
5. remote SHA verified;
6. handoff committed to `.agents/informe&seguimiento/03_HANDOFF_AG2-P00-002.md`;
7. exact remote SHA recorded in the handoff.

Local-only work is not delivered.

## Required handoff

Create:

`.agents/informe&seguimiento/03_HANDOFF_AG2-P00-002.md`

Include:
- start/final local commit;
- verified `origin/main` SHA;
- every subagent and result;
- files changed;
- exact commands and exit codes;
- tests;
- evidence/hashes;
- each P0/P1 disposition;
- what is proven;
- what remains unproven;
- residual risks;
- phase/rework exit assessment;
- `READY_FOR_REVIEW` or `BLOCKED`.

## Stop

After pushing and verifying the complete result on `origin/main`, Antigravity stops. It does not start Phase 01 and does not create another order.
