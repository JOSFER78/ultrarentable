# RETIRED — FORMER EXTERNAL-AGENT REVIEW PROTOCOL

This file is historical only.

There is no Antigravity handoff/review loop anymore.

Current maintenance model:

```text
ChatGPT reads main
→ repairs main directly
→ verifies evidence
→ records result
→ continues with next stabilization block
```

The scientific review standard remains:
- evidence must be reproducible;
- missing evidence is not PASS;
- ZERO-MOCK / ZERO-SIMULATION / ZERO-FORCING / ZERO-LOOKAHEAD;
- material version changes invalidate affected evidence;
- candidate status never implies certification.

See:
`.agents/informe&seguimiento/README_OPERATIVO.md`
`.agents/informe&seguimiento/15_STABILIZATION_MASTER_PLAN.md`
