# ULTRARENTABLE — CONTROL DIRECTO DEL REPOSITORIO

## Autoridad

Este directorio ya **NO es un sistema de orquestación de Antigravity**.

La ejecución, revisión y reparación del proyecto las dirige directamente ChatGPT sobre `JOSFER78/ultrarentable` / `main`.

No existe:

- cron de órdenes;
- `dispatch_id` ejecutable;
- watcher de Antigravity;
- handoff de agentes;
- avance automático entre fases.

## Flujo único

```text
PLAN MAESTRO
    ↓
AUDITORÍA DIRECTA DE main
    ↓
REPARACIÓN DIRECTA EN EL REPOSITORIO
    ↓
PRUEBAS / CI / EVIDENCIA
    ↓
REVISIÓN DEL RESULTADO
    ↓
SIGUIENTE BLOQUE
    ↓
... hasta el cierre del programa
```

## Fuente de verdad

```text
GitHub: JOSFER78/ultrarentable
Branch: main
```

El estado que no esté publicado en `main` no se considera entregado.

## Plan y ejecución

`15_STABILIZATION_MASTER_PLAN.md` contiene la secuencia actual de estabilización.

Después de cerrar la estabilización se continúa con el plan científico completo: datos → estrategia canónica → ejecución → evidencia → discovery → research → portfolio/meta → ULTRA → FONDEO FUTURES → 24/7.

No se salta una capa si la anterior no está demostrada.

## Evidencia

Una reparación sólo se considera válida cuando existe evidencia reproducible:

```text
commit exacto
+ código revisado
+ pruebas reales
+ resultado observado
+ provenance
```

`TESTS VERDES != SISTEMA CERTIFICADO`.

## Reglas absolutas

```text
ZERO-MOCK
ZERO-SIMULATION
ZERO-FORCING
ZERO-LOOKAHEAD
REAL-ONLY
EVIDENCE-GATED
```

Nunca fabricar trades, datasets, hashes, métricas, curvas, certificaciones, fills o resultados.

## Estado actual

`STABILIZATION_REQUIRED`

La siguiente prioridad es cerrar R0.1–R0.10 del plan de estabilización antes de continuar con discovery o rentabilidad.
