# ORDER AG2-P02-004 — PHASE 02 REAL RUNTIME SEMANTICS & ENGINE BINDING REWORK

## Status
`ISSUED`

## Trigger
Auto-start when `00_DISPATCH.md`, `01_CONTROL_STATE.md` and `02_CURRENT_ORDER.md` all reference `AG2-P02-004`, Phase `02`, and `ISSUED`.

## STRICT SCOPE
Execute ONLY this Phase 02 rework. Do not start Phase 03, Discovery Factory, Genome, Meta-Strategy, FONDEO or ULTRA work.

## Required corrections

### P02-004-01 — Remove runtime identity defaults
`engine_version` and `policy_version` must be explicit runtime inputs resolved from the authoritative execution policy/version source. Missing values fail closed.

### P02-004-02 — Remove indicator fallbacks
Unknown indicators, missing parameters, missing source fields and unsupported operators must fail closed. No fallback to `close`, implicit periods or other convenient values.

### P02-004-03 — Execute exits according to canonical types
Implement/route the actual semantics for the supported `sl_type` and `tp_type` values (ATR, points, percentage, RR, etc.) through the real engine. If a type is not implemented by the real engine, reject the strategy rather than reinterpret it.

### P02-004-04 — Bind to the real execution engine
Trace the production call-site from `CanonicalStrategy` to the actual universal execution/ledger boundary. Do not create a parallel toy backtester as evidence. If an adapter is needed, it must be the single authoritative adapter used by the production path.

### P02-004-05 — Preserve all execution semantics
Demonstrate real semantics for direction LONG/SHORT/BOTH, logical composition, indicator source/shift/params, SL/TP, trailing/time stop where supported, sizing/risk, max open positions and session rules. Missing support = fail closed.

### P02-004-06 — Bind dataset identity from provenance chain
The execution result must obtain `dataset_id` and `dataset_sha256` from the canonical dataset registry/chain-of-custody, not accept arbitrary caller-supplied identity without verification.

### P02-004-07 — Independent tests
Use separate test/red-team subagents. Test each unsupported/default/fallback path for fail-closed behavior and prove semantic equivalence for supported types using real physical datasets.

### P02-004-08 — Evidence
Record exact production call-sites, exact commands, exit codes, dataset manifest/hash IDs, runtime outputs, changed files, remote SHA, proven vs unproven items, and deferred findings.

## Mandatory subagents
1. RECON / REAL ENGINE TRACE
2. RUNTIME IMPLEMENTATION
3. QUANT / EXIT SEMANTICS
4. DATA / PROVENANCE
5. TEST / INTEGRATION
6. RED-TEAM / ZERO-MOCK
7. LINEAGE / VERSIONING
8. RELIABILITY

Implementer cannot be sole verifier.

## SSH / VPS
Long-running jobs must be detached/asynchronous. Record `remote_job_id`, exact command, target SHA, logs, state and real exit code. Never block the orchestrator 10–20 minutes waiting.

## ZERO-SIMULATION / ZERO-FORCING / REAL-ONLY
Absolute. No fabricated data, fills, metrics, execution results, hashes or certification evidence. Do not rewrite tests to force PASS.

## GitHub completion
Work on `/home/ubuntu/workspace/pro/trading/01 Ultrarentable`.
Before `READY_FOR_REVIEW`:
1. execute only this order;
2. run focused tests + bounded regression;
3. record commands/exit codes and real evidence;
4. commit;
5. push `origin/main`;
6. verify exact remote SHA;
7. create `.agents/informe&seguimiento/03_HANDOFF_AG2-P02-004.md`;
8. STOP.

Do not advance Phase 03.
