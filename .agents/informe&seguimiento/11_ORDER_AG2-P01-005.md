# ORDER AG2-P01-005 — PHASE 01 PROVENANCE ELIGIBILITY & ARTIFACT SSOT REWORK

## Status
`ISSUED`

## Target
`PHASE 01 — DATA & DATASET CHAIN OF CUSTODY / REWORK 005`

## Strict scope
Execute ONLY this Phase 01 rework.
Do not start Phase 02 or any Discovery/ULTRA/FONDEO/Meta-Strategy work.
Out-of-scope findings = `DEFERRED_TO_FUTURE_ORDER`.

## Objectives
1. Make the alias registry an independent, versioned, hashable canonical artifact consumed by runtime.
2. Separate provenance evidence states: `VERIFIED`, `UNVERIFIED`, `NO_EVIDENCE`, `INVALID`.
3. Prevent `UNVERIFIED/NO_EVIDENCE/INVALID` datasets from entering operations that require verified provenance.
4. Cross-check full manifest identity: source_id, instrument_id, timeframe_id, snapshot_id and available version metadata against registry resolution.
5. Fail closed on every mismatch.

## Mandatory subagents
1. DATA / PROVENANCE
2. VERSION / ARTIFACT SSOT
3. IMPLEMENTATION / REGISTRY
4. RED-TEAM / ZERO-MOCK
5. VALIDATION / ELIGIBILITY
6. TEST / REPRODUCIBILITY
7. API/UI / PROVENANCE
8. RELIABILITY / SNAPSHOT-RECOVERY

Implementer cannot be sole verifier.

## Required tests
- alias artifact hash/version reproducibility;
- runtime consumes the artifact and not a duplicated constant;
- modified artifact causes deterministic rejection;
- UNVERIFIED/NO_EVIDENCE/INVALID cannot pass a verified-provenance eligibility gate;
- source/instrument/timeframe/snapshot/version mismatch between manifest and registry fails closed;
- exact resolution remains deterministic;
- unchanged physical bytes produce unchanged hashes.

## SSH/VPS
Long jobs must run detached/asynchronously. Record remote_job_id, exact command, target SHA, log path, state and real exit code. Never block the orchestrator 10–20 minutes.

## ZERO-SIMULATION / ZERO-FORCING / REAL-ONLY
Absolute. No fabricated provenance, hashes, versions, evidence, passes, datasets or test manipulation.

## GitHub completion
Work on `/home/ubuntu/workspace/pro/trading/01 Ultrarentable`.
Deliver only on `origin/main`.
Before READY_FOR_REVIEW:
1. implement only scoped changes;
2. focused tests + bounded regression;
3. exact commands/exit codes;
4. commit;
5. push `origin/main`;
6. verify remote SHA;
7. create `.agents/informe&seguimiento/03_HANDOFF_AG2-P01-005.md`;
8. include remote SHA, tests, evidence, deferred findings;
9. STOP.

Do not advance Phase 02.
